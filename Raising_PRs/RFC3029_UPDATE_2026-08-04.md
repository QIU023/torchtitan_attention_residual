# RFC #3029 update — draft, not posted

Framing: this is **early evidence**, published so the parallelism work is on the record before the eager reference lands. It is not a request to merge anything today. Branches are ours, complete, and will be rebased onto the upstream model folder once that PR is in.

Nothing here asks anyone to do anything except read it. That is deliberate -- we have already generated more cross-references on other people's threads than we should have.

**Format note**: the paste section is written as single-line paragraphs (tables and list items excepted -- GitHub needs their line structure), so it copies verbatim.

---

## Draft body

--- PASTE BEGIN ---

### Where this stands

Since K3's release we have been building the parallelism side against the full architecture -- KDA, Gated MLA, Stable LatentMoE, Block Attention Residuals, and the MoonViT-V2 vision pathway. That work is complete and running, and we are posting it now as early evidence rather than waiting for the eager reference (#4025) to land.

Branches (fork `QIU023/torchtitan`), all currently carrying the complete implementation; they will be sliced per-topic and rebased onto `torchtitan/models/kimi_k3/` once the eager reference lands:

| slot | scope | branch |
|---|---|---|
| A | TP: KDA NoParallel, Gated MLA, LatentMoE placements | `k3_pr_a_tp_kda` |
| B | EP + grouped GEMM | `k3_pr_b_ep_grouped` |
| C | PP + Block AttnRes cross-stage adapter | `k3_pr_c_pp_attnres` |
| D | CP: Ulysses for MLA, fla's merged KCP for KDA | `k3_pr_d_cp_ulysses` |

The dev branch also carries work outside those four slots that exists today and is not proposed for upstreaming in this pass: LoRA, Muon, quantile balancing for the router (report sec 2.3.3), and MXFP4 QAT.

### Results

Multimodal debug model, seed 42, `--debug.deterministic`, gradient accumulation on. Two model configurations: the eager reference's debug architecture, and the same with the report's trailing Gated MLA appended so the final layer is global (`full_attn_layers = [4, 8, 12, 13]` at debug depth, mirroring the released list's `[..., 92, 93]`).

Degree-2 matrix, thirteen configurations -- `single-GPU`, `fsdp2`, `pp2`, `cp2`, `tp2`, every 3-of-4 combination of FSDP/TP/PP/CP, and the same again with EP:

| | eager | compiled |
|---|---|---|
| 3 steps | 13/13 | 10/13 |
| 10 steps | 13/13 | -- |
| 100 steps | 13/13 | -- |
| 100 steps, report architecture | 13/13 | 10/13 (3 steps) |

The three compiled gaps are one upstream operator limitation, and we confirmed
that end to end rather than asserting it: with a local shim that re-strides the
empty operand exactly as the patch we are proposing upstream would, the
compiled matrix goes **13/13** (the previously failing legs land at 12.04227,
12.05974 and 12.06316 at step 1, alongside eager's 12.03851, 12.05770 and
12.06318). **That shim is ours and not upstream, so 10/13 stays the number we
report** -- the 13/13 is evidence about the operator, not a result anyone else
can reproduce today.

Max-degree cells on top of those, 10 steps, run without changing the config:

| cell | |
|---|---|
| `ep8_fsdp8` | passes -- 8 experts over 8 ranks is one expert per rank, so empty expert groups occur every step |
| `pp4` | passes -- 13 layers over 4 stages is uneven, vision tower entirely on stage 0 |
| `tp4`, `cp4` | pass -- one attention head per rank |
| `pp8` | passes, after one fix (below) |

So **18 of 18 attempted**, on both model configurations: the eager
reference's debug architecture unchanged, and the same with the report's
trailing Gated MLA at layer 93. Their step-1 bands coincide -- 12.0325 to
12.0797 against 12.0345 to 12.1005 -- which is what a single changed layer
should produce, and is the alignment claim worth making. The per-cell table for
the unmodified architecture is in the eager PR comment; this thread carries the
report-architecture run. Cells the config cannot express are recorded rather than accommodated: `tp8`/`cp8`/`tp4 x cp4` have only 4 attention heads to shard, and `pp8 x vp4` cannot host 32 virtual stages on 13 layers. High-degree coverage on our own flavors (tp8, cp8, PP8xVP4, EP@896) exists separately.

Step-1 losses span 12.0385 to 12.0797 across the degree-2 set, against `ln(163840) = 12.006`. Two independent full matrix runs agree bit-for-bit on every leg.

`pp8` was the one cell that needed a fix rather than a rerun: torch's pipeline P2P requires dense tensors, and the gradient for the partial block arrives at the stage boundary as a strided slice of the last stage's aggregation buffer (`[1, 256, 256]` with stride `[256, 768, 1]`). Fixed with a dense-grad shim on the PP inputs (identity forward, contiguous backward); `pp2` and `pp4` are bit-identical before and after, and `PP8xVP4` on our 32-layer flavor was re-run after the change because it crosses that boundary at every one of 32 virtual stages: 100 steps clean (7.70028 -> 2.83465, grad_norm 8.30 -> 0.74) and bit-identical to the pre-change tree at matched step count. **That run is seq_len 1024, not the flavor's 8192**, which exceeds this box's 15.5 GiB per GPU -- verified as a memory limit and not ours by reproducing the same OOM on the pre-change file. So it covers the gradient round-trip the change touches and does not cover long-context activation or communication volume; the 8192 configuration waits for bigger hardware.

**What these matrices are not: a cross-stack comparison.** They are our
parallelism running the eager reference's model configuration, not our numbers
against theirs. We have not rebased onto that PR yet, and until we do the two
stacks differ in initialization order and in which modules own which
parameters, so the trajectories are not expected to coincide -- a single-GPU
run starts about 0.4 nats apart. What the tables support is internal
consistency: eighteen parallelism configurations of the same model agreeing to
0.05 at step 1, all just above `ln(vocab)`. Cross-stack numerical alignment is
deferred until after the rebase and will be posted then, not claimed now.

Three qualifications, stated here rather than left for someone to find:

1. **The 100-step run overfits a smoke dataset** (12.06 -> 0.37). It shows thirteen configurations behaving consistently and stably over that window; it is not a convergence result. Real curves need a real pretraining run, which is queued.
2. **The three compiled failures are one upstream cause, not ours.** `torch._grouped_mm` rejects an operand whose contraction dimension is zero, which is the shape the weight-gradient form takes when an expert group is empty. Five-line reproduction with no model, no compile and no distributed; being filed against pytorch/pytorch separately. Empty groups themselves are fine in both modes.
3. **The model used for the matrix is the eager reference's debug architecture, not the report's.** One difference, which we are raising on that PR as a config-level question: its final layer is KDA where report sec 2.1 places an additional Gated MLA "ensuring that the final layer always performs global attention". Our own configs implement the report's layout; we ran the matrix on theirs deliberately, so the comparison is our parallelism against their model rather than two different models.

Separately from the matrices, the released 2.8T configuration was checked
against report Table 1 by building it on a meta device -- shapes only, nothing
allocated. Total comes out **2.779 T against Table 1's 2.78T** and activated
**105.8 B against 104.2B**, with the layer composition (69 KDA + 24 MLA, the
trailing Gated MLA at 93), latent 3584, MoE hidden 3072, 896 experts top-16, 2
shared, and the 401M vision tower all matching field for field. Two things are
not claimable from that: the report lists one MTP layer while the released
config ships `num_nextn_predict_layers: 0` (we build it only when asked, and
the training loss is not wired for multi-token targets), and enabling it at
2.8T costs 30 B because a backbone block there carries the full MoE, which
Table 1's total does not appear to include.

One shared-code fix already came out of running this matrix at the gradient level: on the TP-without-EP legs (`tp2`, and every FSDP/PP/CP combination with EP off), gradients below the routed experts came back systematically under-reduced -- a 3.10x whole-model grad_norm gap at dp2 x tp2, invisible in loss curves because Adam is scale-invariant. The root cause is in the shared MoE sharding config, reproduces on unmodified deepseek_v3, and is filed with the per-parameter evidence as #4054. The EP-on legs were never affected.

### The interfaces the PP and EP slices touch, and who adds them: we do

Nothing to merge yet and nothing we are asking anyone to build.

**PP (slot C).** The eager reference's decoder loop already threads the right pair -- `(h_BLD, block_residual_TND)` in and out of every layer -- so the PP slice needs only two seams: the block-residual stack is currently created inside the forward (a stage cannot pass an incoming stack in), and the loop always runs all layers. **Our PP PR adds both** -- an optional initial-stack argument and a layer-range slice, returning the pair when the range stops short -- as its first, separately reviewable commit.

**EP (slot B).** The released checkpoint stores routed experts per-expert; grouped-GEMM training wants them stacked on the expert dimension. The conversion already exists on `k3_pr_b_ep_grouped` and ships with that PR: a bidirectional key map over the full released key set, including the sliced-tensor indexing where one released key maps to one slice of a stacked tensor (`...experts.3.w1.weight <-> w1_EFD[3]`), plus the grouped experts module itself. It composes with the eager per-expert adapter rather than replacing it -- both layouts stay loadable and savable.

Stated here so the interfaces are on the record before those PRs open. TP and CP need no seam at all and rebase cleanly.

--- PASTE END ---

---

## Notes for the poster

- Post the substance here on #3029. Everything that goes on the eager PR --
  one short top-level comment plus a single bundled review of two inline
  comments -- is in `REVIEW_4025_CONSOLIDATED.md`. Do not restate the
  results there.
- Do not put issue or PR numbers in commit messages. That is what generated the
  cross-reference noise; the local rule is now to reference only in comment
  text, deliberately.
- Do not claim compile is fully green. It is 10/13 and the honest sentence is
  the one in qualification 2.
- Do not describe the branches as "ready to merge". They carry the full fork,
  including work not proposed for upstreaming; the slicing happens at rebase.
