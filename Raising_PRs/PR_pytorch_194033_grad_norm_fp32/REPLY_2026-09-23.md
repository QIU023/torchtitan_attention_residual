# PR 194033: reply to Jane's request of 2026-09-22 (point 3, the docstring under 4)

For the user to post as one comment under https://github.com/pytorch/pytorch/pull/194033#issuecomment-5783420331, after the new head is on the PR branch.

Branch: review branch `get-total-norm-dtype-review1` on the fork = `bdbf5318ba`, the four commits of `patches_2026-09-23/` on upstream main `69f2e45e` (applied cleanly, same changed lines as `full_2026-09-23.diff`). The PR branch `get-total-norm-dtype` is still `1ad4f1623f`; to update the PR: `git push --force-with-lease=get-total-norm-dtype:1ad4f1623f origin bdbf5318ba:refs/heads/get-total-norm-dtype`. Drop the powsum paragraph if the fourth commit is dropped.

--- issue comment reply (5783420331): PASTE BEGIN ---

Point 3: agreed, the test asserted CPU/CUDA rounding on every device. It now runs only on cpu and cuda (`@onlyOn(["cpu", "cuda"])`) and keeps the exact values 258 and 256, which are what both kernels give when they accumulate bfloat16 in float32. I did not take the two alternatives: `onlyNativeDeviceTypes` includes `mps` and `mtia` on current main, so it gates nothing, and the structural `whole != split` is no more portable, since a backend that accumulates in bfloat16 gives 256 on both sides.

Docstring: it now states the constraint. `dtype` must be a floating point type the inputs promote to (`torch.promote_types(input.dtype, dtype) == dtype`), the same check `linalg.vector_norm` and the foreach CUDA kernel make; anything else raises.

Point 2 is the same defect one function down, so `_foreach_powsum.Scalar` now reads `dim` and `keepdim` by schema position too, with a bfloat16 input and `dtype=torch.float32` added to `test_foreach_powsum_sharded`.

Rebased onto current main; the two red jobs were the `inductor_aoti_fallback` CPU shards, which Dr. CI lists as unstable.

--- PASTE END ---
