# PR 4765 body（draft PR，`k3_pp_offload`），2026-09-26 按"PP lower bound 这条线"重排

## 状态（不粘贴）

- **这条线怎么对应两个 draft：** 用户定的顺序是 ①pp_review_optimize → ②统一内存管理器 → ③offload 与 balance 一起做。
  - #4765 装 ① 和 ②：V4 的三个提交，加 PR A，加统一激活存储；
  - #4764 叠在上面，装 ③。
  - 原来 #4765 里的 host offload 挪到了 #4764，和 balance 共用一个计划。
- **标题建议**（标题由用户改）：`[DO NOT review, stack on #4312] [Kimi K3] PP rank store at the memory lower bound, under one activation storage`。
- **分支：** 09-26 已把 `k3_pp_offload` 同步到 `pp_offload_review1` 的 `7b43e1d01`。GitHub 显示 20 个提交、25 个文件：4312 的 15 个加上这里的 5 个。旧 head `49117a146` 备份为 `backup/k3_pp_offload_pre_20260926`。
- **数字：** 全是 8 × RTX 5060 Ti 实测，H100 待测。明细见 `PP_LOWER_BOUND_CAMPAIGN_2026-09-26.md`。

--- PR 4765 body: PASTE BEGIN ---

## Summary

Kimi K3 pipeline ranks keep each attention residual block once, as its own tensor, and free it at the backward of the stage that brought it onto the rank; every tensor autograd saves can go through one activation storage that holds those blocks pinned on the device.

- The block residual (`kimi_k3/model.py`): a list of [T, D] blocks instead of a [T, N, D] stack. A block opening appends to the list; it used to copy the stack, and the later layers' checkpoints held that copy until backward. The aggregation stacks the list inside the call, so its arithmetic is unchanged.
- The pipeline stage and the rank store (`kimi_k3/pipeline_parallel/stage.py`, `cache.py`): one tensor per block on the wire. A received block is its own receive buffer and a committed block is the model's own tensor, so the store keeps references, not copies, and drops each block at the backward of the stage that brought it, its last reader on the rank. Receive buffers are allocated when the receive is posted; forward sends are waited at the stage's backward and input gradient sends at the first forward that proves they arrived.
- `ActivationStorage` (`torchtitan/distributed/activation_storage.py`, new): routes each tensor autograd saves through `torch_remat.saved_tensors_hooks` to a backend a policy picks per tensor, decided when the stage's forward ends. A storage pinned during that forward stays on the device.
- `KimiK3Model.Config.pp_memory.manager` installs the storage, with the rank store pinning every block it holds. Off by default. No backend is configured in this PR.

## Design

The AttnRes paper stores each block exactly once across a rank's virtual stages, and the Kimi K3 report releases a micro-batch's blocks when it finishes, calling that the lower bound. A block's last reader on a rank, in backward order, is the stage that brought it there, so this PR releases it at that stage's backward, earlier than the micro-batch's end.

A list keeps a block opening from copying the stack. With one tensor per block on the wire, a received block needs no row in a shared buffer, and nothing forces a copy when a hop forwards a block it did not make.

torch's pipelining runtime waits send works only after a step's last action, and a pending work keeps its tensor alive. It also keeps one receive buffer per micro-batch for every input and input gradient. The stage overrides both. These two behaviors hold for every pipeline model; they belong in torch, and the overrides go once torch offers them.

The storage lives in `torchtitan/distributed` because nothing in it is specific to Kimi K3. The rank store pins through the storage's API, so the storage never needs to know what an AttnRes block is.

## Relation to #4312 and #4764

Stacked on #4312. #4764 adds the host and remote backends and one plan that offloads and balances together.

## Results

8 x RTX 5060 Ti, the released layout at a smaller width: 93 layers in blocks of 12, pp8 x vp4 (32 stages), dim 2048, seq 2048, 16 micro-batches, full AC. Peak allocated memory per PP rank in GiB at step 5 (steps 3 to 20 are identical):

| rank | #4312 | this PR, store only | this PR, with the storage |
|---:|---:|---:|---:|
| 0 | 10.04 | 5.14 | 5.14 |
| 1 | 10.32 | 5.67 | 5.67 |
| 2 | 10.32 | 5.64 | 5.64 |
| 3 | 10.35 | 6.37 | 6.37 |
| 4 | 11.76 | 5.87 | 5.87 |
| 5 | 11.25 | 5.92 | 5.92 |
| 6 | 11.23 | 5.87 | 5.87 |
| 7 | 10.91 | 6.82 | 6.82 |
| max | 11.76 | 6.82 | 6.82 |
| mean | 10.77 | 5.91 | 5.91 |

Loss and grad norm are identical to #4312 on all 20 steps. The traced step takes 22.85 s on #4312, 22.76 s with the store alone and 22.77 s with the storage installed; compute, exposed communication and their overlap move by less than 0.4 percent. H100 numbers go here once measured.

## Test plan

- `pytest tests/unit_tests/cpu -k "kimi_k3 or pipeline or cli or integration_test or config_manager or activation_storage" -q` (116 passed)
  - `test_kimi_k3_pp_block_grads.py`: four ranks on gloo under Interleaved1F1B and under 1F1B, cache on and off, blocks opening inside stages; every block gradient bitwise with one device; each block released by the stage that brought it; the input gradient wait points follow the receiver's use.
  - `test_activation_storage.py`: gradients bitwise with the storage moving every save and with no backend, under titan's full AC and under `remat.checkpoint`; a storage pinned during the forward never reaches the policy; a save that stays is not held past autograd.
  - `test_kimi_k3_pp_memory.py`: two ranks on gloo with Interleaved1F1B; every gradient bitwise with the storage keeping or moving the saves; the policy never sees a block the rank store holds.
- The results above: 8 x RTX 5060 Ti with the local SM120 guard lift on `kda.py`, which is not in the diff.

--- PASTE END ---
