# Reply to Tianyu on PR 4312, thread r3987651867 (`parallelize.py`, "without intrusive change to existing `pipeline_parallel.py`")

For the user to post in https://github.com/pytorch/torchtitan/pull/4312#discussion_r3987651867. The commit is `8de0c078c` (= `c4fee4afd` without the commit-message trailers), on `pp_review4` and `k3_pp_text` (fast-forward, 2026-09-13). The numerics go to the numerics thread instead (`REPLY_4312_NUMERICS_C4_2026-09-13.md`). The old/new bitwise check ran on the local 8 x 5060 Ti box (old and new head on one box, one compile cache), not on the H100.

--- PASTE BEGIN ---

Done in [this commit](https://github.com/QIU023/torchtitan/commit/8de0c078c3d82d5948ba6566fa3f0a68cd31c09b): `pipeline_parallel.py` is main's again apart from one line, where `pipeline_with_first_stage_modules` clears `pipeline_parallel_layers_per_stage` once it spells the split out (the one-way split check in `ParallelismConfig` needs it). `llm_split_with_pinned_modules` and the last-stage parameter are gone; Kimi K3 builds its split bottom up in `parallelize.py` (the vision tower with the embedding, the AttnRes aggregation with the head), passes it through `module_fqns_per_model_part`, and reads the layer-to-stage map off the same list. The split is identical to the previous one on all 10368 configurations checked, and the previous and new heads give bitwise-identical losses and step-1 gradients on a pp2 x vp2 run. This goes the other way from your earlier comment to extend core's generator for the last-stage modules; if you prefer that route, [the previous head](https://github.com/QIU023/torchtitan/commit/dbc425403d35dfbf45305d81ae5973baf4c995a7) has it and I can put it back.

--- PASTE END ---
