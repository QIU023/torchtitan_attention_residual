# Reply to Tianyu on PR 4312, thread r3987651867 (`parallelize.py`, "without intrusive change to existing `pipeline_parallel.py`")

For the user to post in https://github.com/pytorch/torchtitan/pull/4312#discussion_r3987651867. The commit is `c4fee4afd` on `pp_review4`; `k3_pp_text` is still `dbc425403`, so sync the PR branch first (approval), otherwise the link points at a commit the PR does not carry yet. Numbers: H100 logs in `phase13_k3like_48b_posttrain/pp_h100x4_c4_logs_2026-09-13/`; step 100 against the no-sync reference of each stream. The old/new bitwise check ran on the local 8 x 5060 Ti box (old and new head on one box, one compile cache), not on the H100.

--- PASTE BEGIN ---

Done in [c4fee4afd](https://github.com/QIU023/torchtitan/commit/c4fee4afd2a65c09ba07f8f4e9292c17c0d99a78): `pipeline_parallel.py` is main's again apart from one line, where `pipeline_with_first_stage_modules` clears `pipeline_parallel_layers_per_stage` once it spells the split out (the one-way split check in `ParallelismConfig` needs it). `llm_split_with_pinned_modules` and the last-stage parameter are gone; Kimi K3 builds its split bottom up in `parallelize.py` (the vision tower with the embedding, the AttnRes aggregation with the head), passes it through `module_fqns_per_model_part`, and reads the layer-to-stage map off the same list. The split is identical to the previous one on all 10368 configurations checked, and the previous and new heads give bitwise-identical losses and step-1 gradients on a pp2 x vp2 run.

Numerics are re-run on the same 4 x H100, now on `c4_test` text-only rows, since at 1024 tokens per step the reference memorised the 32-sample debug set within the 100 steps. At step 100 every pipeline cell is within 0.22% of the reference loss at 1024 tokens (order-only noise floor 0.19%), and within 2.1% at 2048 tokens with dp2 (floor 0.24%, dp2 x ep2 0.93%). Tables are in the PR body.

--- PASTE END ---
