# PR 4312 round 5 (2026-09-24): rebase onto main 9e159aed7 and the 09-24 review threads

`pp_review4` = `2141a4eeb`, sixteen commits on upstream main `9e159aed7`, on the fork as `pp_review4`; `k3_pp_text`
(the PR head) is still `9bb1b87f0`, broken as applied (see REBASE_DRAFTS_2026-09-23.md, 09-24 section), until the user
pushes. Drafts to paste: `PR_BODY_PP_v5_2026-09-24.md` (NEEDS the H100 rerun first) and `REPLY_4312_ROUND5_2026-09-24.md`.

## Rebase

`9729cbdb5` (on `b64103072`) replayed onto `9e159aed7` with no conflict. main's three commits (#4704 TP projection
backends, #4837 RegionAC AllToAll regions, #4476 graph_trainer none AC policy) touch `kimi_k3/__init__.py` and
`model.py`; per file over the 21 files 4312 touches, the replay's change equals main's own lines exactly.

## One commit per thread, on top

| thread | commit |
|---|---|
| tianyu r4089852926, the svg | `e6e29b727` kimi_k3: the cache page drops its draft figure (svg deleted, the Overview section that embedded it removed; the page keeps its text) |
| tianyu r4089919460, "similar for the other two functions" | `d3cbc92dc` `_pack_outgoing_delta`, `_split_stack_grad` (plus the test, the page) |
| tianyu r4089922488, cache.py | `ef64fab33` `PPRankLocalCache` in `kimi_k3/pipeline_parallel/cache.py`; imports in `__init__.py`, `stage.py`, both tests; the page names cache.py |
| tianyu r4089902889, "simply raise on CP" | `7690ee7b9` `if parallel_dims.cp_enabled: raise NotImplementedError("Kimi K3 does not support context parallelism yet.")` |
| tianyu r4089938897, @overload | `900b1d474` `get_module_fqns_per_model_part` (refusal of layers_per_stage, explicit split passed through as the same object, else generated with both ends pinned); `pipeline_with_first_last_stage_modules` = that + `pipeline_llm`, four return values, no `return_split`, no `Literal`/`overload` import; Kimi K3 takes the split, `dataclasses.replace`s it into the config and calls `pipeline_llm`; the "hands back the split" test becomes `test_get_module_fqns_per_model_part_is_the_split_the_pipeline_uses` |
| shuhuayu r4090046115 + tianyu r4089864338 (vp or vpp) | `ee17d6acb` one B200 pipeline cell `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4` (Interleaved1F1B, 4 microbatches, the 8-stage split spelled out = core's default for 8 stages, AdamW); the pp4 x vp4 recipe and cell removed; definitions test updated; layout test: the 8-stage split is the cell's and is pinned against `_generate_llm_fqn_per_model_part(8, 17)`, the pp4 split kept as a CPU case (`test_pp4_vpp4_runs_one_layer_per_stage`) |
| tianyu r4089864338 | `2141a4eeb` block-gradient test labels say vpp |

torchtitan's own term for stages per rank is `stages_per_rank` (`_get_pipeline_metadata`); no short form appears in any
cell name, so vpp as tianyu suggested.

## Checks (5060 box, venv_bfx9)

- CPU: `test_kimi_k3_*`, `test_pipeline_parallel`, `test_integration_test_definitions`, `test_config_manager`,
  `test_optimizer_param_groups`, `test_override`: 159 passed.
- pyrefly on 4312's source files (tests are excluded by the repo's pyrefly config): 6 errors, the same 6 as on
  `9729cbdb5`. The overloads had been added in round 4 for 4 errors; without `return_split` none come back.
- GPU: `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4`, 8 GPUs, 3 steps (KDA guard lifted locally in a throwaway
  worktree): rank 0 builds stages [0, 2, 4, 6] with the delta transport; grad norm 2.4844 / 5.0000 / 5.8438.

## Open

- H100 rerun of the body tables (the rule after every rebase; #4704 changed K3's TP projection code).
- The three PP drafts sit on `3902077ca`; restacking onto `2141a4eeb` meets the moved `PPRankLocalCache` (offload),
  the private helpers and `get_module_fqns_per_model_part` (DEP's `_with_vit_dep_split` wraps the old call).
