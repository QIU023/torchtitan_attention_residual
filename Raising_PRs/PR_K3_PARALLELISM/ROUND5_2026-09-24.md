# PR 4312 round 5 (2026-09-24): rebase onto main 9e159aed7 and the 09-24 review threads

`pp_review4` = `2141a4eeb`, sixteen commits on upstream main `9e159aed7`, on the fork as `pp_review4`; `k3_pp_text`
(the PR head) is still `9bb1b87f0`, broken as applied (see REBASE_DRAFTS_2026-09-23.md, 09-24 section), until the user
pushes. Drafts to paste: `PR_BODY_PP_v5_2026-09-24.md` (NEEDS the H100 rerun first) and `REPLY_4312_ROUND5_2026-09-24.md`.

## Rebase

`9729cbdb5` (on `b64103072`) replayed onto `9e159aed7` with no conflict. main's three commits (#4704 TP projection
backends, #4837 RegionAC AllToAll regions, #4476 graph_trainer none AC policy) touch `kimi_k3/__init__.py` and
`model.py`; per file over the 21 files 4312 touches, the replay's change equals main's own lines exactly.

## One commit per thread, on top

| thread | commit |
|---|---|
| tianyu r4089852926, the svg | `e6e29b727` kimi_k3: the cache page drops its draft figure (svg deleted, the Overview section that embedded it removed; the page keeps its text) |
| tianyu r4089919460, "similar for the other two functions" | `d3cbc92dc` `_pack_outgoing_delta`, `_split_stack_grad` (plus the test, the page) |
| tianyu r4089922488, cache.py | `ef64fab33` `PPRankLocalCache` in `kimi_k3/pipeline_parallel/cache.py`; imports in `__init__.py`, `stage.py`, both tests; the page names cache.py |
| tianyu r4089902889, "simply raise on CP" | `7690ee7b9` `if parallel_dims.cp_enabled: raise NotImplementedError("Kimi K3 does not support context parallelism yet.")` |
| tianyu r4089938897, @overload | `900b1d474` `get_module_fqns_per_model_part` (refusal of layers_per_stage, explicit split passed through as the same object, else generated with both ends pinned); `pipeline_with_first_last_stage_modules` = that + `pipeline_llm`, four return values, no `return_split`, no `Literal`/`overload` import; Kimi K3 takes the split, `dataclasses.replace`s it into the config and calls `pipeline_llm`; the "hands back the split" test becomes `test_get_module_fqns_per_model_part_is_the_split_the_pipeline_uses` |
| shuhuayu r4090046115 + tianyu r4089864338 (vp or vpp) | `ee17d6acb` one B200 pipeline cell `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4` (Interleaved1F1B, 4 microbatches, the 8-stage split spelled out = core's default for 8 stages, AdamW); the pp4 x vp4 recipe and cell removed; definitions test updated; layout test: the 8-stage split is the cell's and is pinned against `_generate_llm_fqn_per_model_part(8, 17)`, the pp4 split kept as a CPU case (`test_pp4_vpp4_runs_one_layer_per_stage`) |
| tianyu r4089864338 | `2141a4eeb` block-gradient test labels say vpp |

torchtitan's own term for stages per rank is `stages_per_rank` (`_get_pipeline_metadata`); no short form appears in any
cell name, so vpp as tianyu suggested.

## Checks (5060 box, venv_bfx9)

- CPU: `test_kimi_k3_*`, `test_pipeline_parallel`, `test_integration_test_definitions`, `test_config_manager`,
  `test_optimizer_param_groups`, `test_override`: 159 passed.
- pyrefly on 4312's source files (tests are excluded by the repo's pyrefly config): 6 errors, the same 6 as on
  `9729cbdb5`. The overloads had been added in round 4 for 4 errors; without `return_split` none come back.
- GPU: `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4`, 8 GPUs, 3 steps (KDA guard lifted locally in a throwaway
  worktree): rank 0 builds stages [0, 2, 4, 6] with the delta transport; grad norm 2.4844 / 5.0000 / 5.8438.

## Open

- H100 rerun of the body tables (the rule after every rebase; #4704 changed K3's TP projection code).
- The three PP drafts sit on `3902077ca`; restacking onto `2141a4eeb` meets the moved `PPRankLocalCache` (offload),
  the private helpers and `get_module_fqns_per_model_part` (DEP's `_with_vit_dep_split` wraps the old call).

## Round 5b (same day): the user's follow-ups

- The cell's `test_descr` is "Kimi K3 FSDP, TP, EP and PP with 4 VPP stages" (folded into the cell commit).
- shuhuayu's r4080717964 (deposit count check, non-blocking, the user promised a comment): commit "kimi_k3: the
  deposit count check names the split it assumes", a four-line comment above `if count != expected:` in
  `_collect_into`. The condition in it was measured, not guessed: see `IDEA_deposit_check_2026-09-24.md` (split P
  with an empty stage that forwards a block passes; split F with empty stages and empty deltas fails). The longer
  view (count reports, not tensors) is recorded there only.
- `pp_review4` = `2dbe9d370`, seventeen commits on `9e159aed7`, on the fork as `pp_review4`; `k3_pp_text` still
  `9bb1b87f0`. CPU: stage, block-grads and definitions tests 23 passed after the comment (159 for the full set before
  it, unchanged code otherwise). Reply for r4080717964 added to `REPLY_4312_ROUND5_2026-09-24.md`.

## Round 5c: the round 5 commits squashed into three, by kind (the user, 2026-09-24)

After `267c5f93e` (the PR head's `9bb1b87f0`, rebased) the eight round 5 commits are three; the tree is identical
(`git diff` against `2dbe9d370` is empty) and each of the three passes the pipeline CPU tests on its own (53 passed at
the first two, the full set at the head):

- `09c739324` kimi_k3: review fixes in the pipeline package and the model (the draft figure dropped, the other two
  stage helpers private, `PPRankLocalCache` in `cache.py`, CP raises directly, the comment at the deposit count check)
- `59898375b` pipeline_parallel: the split comes from get_module_fqns_per_model_part (the `@overload` thread)
- `8a3fe2954` kimi_k3: one B200 pipeline cell, fsdp2 x tp2 x ep2 x pp2 with 4 VPP stages (shuhuayu's cell thread,
  the pp4 x vp4 cell removed, vpp naming in the cell and the tests)

`pp_review4` = `8a3fe2954`, twelve commits on `9e159aed7`, on the fork; `k3_pp_text` still `9bb1b87f0`.

## Round 5d: pushed to k3_pp_text; the check's note is a two-line docstring

- The user said push: `k3_pp_text` moved `9bb1b87f0` -> `8a3fe2954` (old head pinned as
  `backup/k3_pp_text_pre_20260924`), then -> `d98abd58d` after the user asked for the four-line comment at the
  deposit count check to become a docstring of one or two lines at the top of `_collect_into` (L229 def, L230-231
  docstring). Folded into `b65a21b60` "kimi_k3: review fixes in the pipeline package and the model", whose message
  bullet now names the docstring. Tree otherwise identical; stage tests 7 passed. PR 4312 head = `d98abd58d`.
- Twelve commits on `9e159aed7`: `b65a21b60`, `b149bb45c`, `d98abd58d` after `267c5f93e`.

## Round 5e: the check's note is a two-line comment again; the body tables stand

- The user asked for the docstring to go back to a comment, with their wording, at the top of `_collect_into`
  (L230-231): folded into `8101fa54b` "kimi_k3: review fixes in the pipeline package and the model" (message bullet
  updated), force-pushed to `pp_review4` and `k3_pp_text`: PR 4312 head = `e592286b4`.
- The user asked whether the body tables need an H100 rerun for main + fixes. They do not: `IDCHECK_2026-09-24.md`
  (ten cells, 100 steps, old head `ce67cece4` against `8a3fe2954`, same cache, AdamW pinned, all identical). Body v5
  now says so and names AdamW (the recipe's optimizer before #4596); the kit needs the AdamW line and a spelled-out
  16-stage split to be rerun on the current head.

## Round 5f: the cell's split comes from core's generator

The user asked why the cell hand-wrote a split that is core's default. No good reason: the removed pp4 x vp4 recipe's
split was also exactly `_generate_llm_fqn_per_model_part(16, 17)`, and the round 5 cell copied that style. The recipe
now calls `_generate_llm_fqn_per_model_part(4 * pp, len(config.model.layers), first_stage_less_layers,
last_stage_less_layers)` (the arguments `_get_pipeline_metadata` passes) and pins `KimiK3Model.pipeline_first_stage_
module_fqns` / `pipeline_last_stage_module_fqns`; the derived split equals the old literal exactly and every other
field is unchanged. The layout test reads the cell's split from the recipe and takes the pp4 case from the generator;
no literal split with `layers.16` is left in 4312's files. CPU: layout, definitions, pipeline and stage tests 51
passed. Amended into the cell commit; `pp_review4` = `k3_pp_text` = PR head = `443ccad2e`. The rule went into
/workspace/CLAUDE.md: a value a known util computes is called, never pasted, since a copy cannot be maintained.

## Round 5g: tianyu's live comments on the helper (r4090993464, r4090997460), pp_review4 only

tianyu: the rename needs `protocols/model.py#L120` to change, and "if it's not none, this function just calls
pipeline_llm; the if-else should happen at model.py#L120". First attempt deleted the helper outright; the user stopped
it (it is main's `pipeline_with_first_stage_modules`, public, used through `BaseModel.pipeline` by kimi_k2_7,
muse_glimmer, qwen3_5), and only the if-else moved:
- `BaseModel.pipeline`: `pipeline_with_first_last_stage_modules` only when no split is configured and the model pins
  modules, else `pipeline_llm`.
- `pipeline_with_first_last_stage_modules`: kept, no branch, always derives through `get_module_fqns_per_model_part`.
- `get_module_fqns_per_model_part`: no pass-through; refuses a configured split as it refuses
  `pipeline_parallel_layers_per_stage` (tianyu's round-3 ask r4056259393, "o/w overriding user config").
- Kimi K3's `pipeline_kimi_k3`: the same choice at its call site, derives the split itself for its routing tables.
- Tests: the explicit-split case of the helper became a `BaseModel.pipeline` test (derived when none is configured,
  the configured object passed through otherwise); the function's explicit case now asserts the refusal.
Checks: CPU 102 passed; pyrefly 0 errors on the three source files before and after; GPU smoke of both paths on the
5060 (pp2 with no split, 3 steps; the vpp4 cell with its configured split, 3 steps). Folded into
`908dd4533` (message updated); `pp_review4` = `dbd13d397` pushed; `k3_pp_text` stays `443ccad2e` until the user says.
Replies: `REPLY_4312_ROUND5_2026-09-24.md`, "Round 5b" section. Body v5: the Core sub-bullet says who chooses, and the
refusal sentence no longer claims the derived split is sized by the default stage count (`_get_pipeline_metadata`
honors `layers_per_stage`), it states the refusal as tianyu asked for it.

- Synced on the user's word: `k3_pp_text` = `pp_review4` = PR 4312 head = `dbd13d397`.

## Round 5h: jinsooihm r4097059930 (09-24 18:27), the layers_per_stage refusal, pp_review4 only

jinsooihm: "What is the reason for rejecting `pipeline_parallel_layers_per_stage` already being set? It seems `_get_pipeline_metadata` can handle the option of `if layers_per_stage is not None`." He is right. The refusal replaced the clearing tianyu flagged in round 3 (r4056259393); the clearing itself only existed for the `ParallelismConfig.__post_init__` both-set check tianyu suggested on 09-11 (r3984964516). That check went in on 09-12 (`ab982d587`); `ae2e7b9c4` dropped it on 09-13, and `6e1f5e41c` restored it 16 minutes later. Round 4 removed it on 09-23. (Corrected 09-25: this note first said "removed on 09-13 (`ae2e7b9c4`)", which missed the restore.) With the check gone, neither clearing nor refusing is needed, and the refusal was a regression against main for `qwen3_5` (and `qwen3_6`, `qwen3_8`, which build `Qwen35Model`), `kimi_k2_7` and `muse_glimmer` under PP with the knob (CPU probe `scratchpad/lps_probe.py`: main 4 stages with the knob kept, `dbd13d397` ValueError, `901ef34de` main's split). CI never reached it: only llama3 recipes set the knob.

`pp_review4` = `901ef34de`, one commit for the thread on top of `dbd13d397`: the check keeps only the configured-split half; `test_layers_per_stage_sizes_the_derived_split` (the stage count comes from `_get_pipeline_metadata` on the handed copy and differs from the knob-less count; the knob stays on the copy; the caller's config untouched), which fails on `dbd13d397` as a control. CPU 114 passed (pipeline, K3 PP layout/stage/block grads, config manager, CLI list, skip_dp, test definitions); pre-commit passes on the two files; pyrefly 0.45.1 the same 2 environment errors as main, no hook edits. Reply: `REPLY_4312_JINSOO_r4097059930_2026-09-24.md`. Body v5 Core sub-bullet updated. `k3_pp_text` stays `dbd13d397` until the user says.

GPU smoke on the 5060 (`901ef34de` plus the uncommitted KDA guard lift, `/workspace/venv_bfx9`, the 4780 kit's AdamW flavor `probe_attnres:attnres_debug` since DistMuon refuses a stage without matrices, pp2 Interleaved1F1B, 4 micro-batches of 512 tokens, seed 42 deterministic, 3 steps, every cell on a copy of one warm cache filled by the default split). Scripts: `scratchpad/lps_smoke/cell2.sh`.

| cell | stages | step 1 loss / grad norm | step 3 loss / grad norm |
|---|---:|---|---|
| default split | 4 | `7.95599` / `2.2500` | `5.53384` / `5.0000` |
| `layers_per_stage=5` (the same split through the knob) | 4 | `7.95599` / `2.2500` | `5.53384` / `5.0000` |
| `layers_per_stage=2` | 10 (last stage without layers) | `8.05771` / `2.0781` | `5.34785` / `4.8750` |

The knob path is bitwise to the default when it yields the same split. The 10-stage run trains; its step-1 gap comes with the different split and was not checked against a seed checkpoint (without one, a different split initialises the parts in another RNG order, memory probe-cells-need-the-seed-checkpoint). Not for any draft.

Pushed: `pp_review4` `dbd13d397..901ef34de` to the fork; `k3_pp_text` stays `dbd13d397`.

## Round 5i: jinsooihm r4097299275 (09-24 18:54), combining option 1 and 2 to lower the delta peak, reply only

No code change. His 8-rank table is a parity rule (adjacent hop for offset 1 and even offsets, direct sends for odd offsets from 3); as written the hop 2 to 3 should carry {1, 2}, not {0, 2}. The PR's tables miss nothing and send nothing twice (volume equals option 1's minimum, zero duplicate deliveries, all six shapes of the 09-11 reply). Closed forms (one block per stage, p even), verified in `transport_model_2026-09-24.py`: this PR peak p-1 for every v, volume p(p-1)(2v-1)/2, 0 added P2P ops; his rule peak p/2, same volume, plus (p-2)(2pv-p-2)/4 non-adjacent sends per direction. torch's `PipelineScheduleMulti` (base of every looped schedule) raises on non-adjacent stage communication; torchtitan's pipeline code issues no P2P of its own (GraphPP only waits on the runtime's recvs). Memory concerns point to the drafts #4765 (offload) and #4764 (balance), which still sit on `3902077ca` and need a restack. Reply: `REPLY_4312_JINSOO_r4097299275_2026-09-24.md`.

## 2.8T sizing and the per-PP-rank memory timeline (09-24 late, analysis only)

`K3_2P8T_PP_VP_MEMORY_2026-09-24.md` with `pp_memory_timeline_2026-09-24.py` (torch's own Interleaved1F1B order, the PR's layout tables) and its output. Proposed: H100 PP8 x VP4, EP64 over IB, TP1, CP 1/8; GB300 PP4 x VP8, EP64 inside NVL72. Findings that change what the drafts say: the PR's block memory is 3 to 5x the lower bound the K3 report claims (a fresh stack per stage and a payload copy, both held to backward, plus torch's per-micro-batch receive buffers, `_setup_forward_recv_info` / `_prepare_backward_infra`); the hybrid transport changes little and only through on-demand buffers; #4765's offload saves no device memory in these layouts (its parked blocks alias torch's receive buffers or stacks AC holds to backward), so the jinsooihm reply now cites only #4764.

## Round 5j: both jinsooihm replies re-reviewed (09-25), drafts only

No new comment on the PR since 09-24 18:55. The head is still `dbd13d397`.

**r4097059930** (`REPLY_4312_JINSOO_r4097059930_2026-09-24.md`):
- The reply names tianyu's None checks: r3984964516 (the `__post_init__` both-set check) and r4056259393 (the assertion). It points at r4090997460 for the split branch in `BaseModel.pipeline`.
- It corrects the reason the round 3 reply gave.
- It lists every model the knob refusal blocks: `qwen3_5`, plus `qwen3_6` and `qwen3_8`, which build `Qwen35Model`; `kimi_k2_7`; `muse_glimmer`; and Kimi K3, which has no PP on main.
- It asks tianyu to confirm. It still posts only after `pp_review4` is synced to `k3_pp_text`.
- The check's history is corrected in 5h above: the check was restored in `6e1f5e41c` and lived until round 4.

**r4097299275** (`REPLY_4312_JINSOO_r4097299275_2026-09-24.md`):
- The paste is trimmed to three paragraphs and one table. The closed forms from 5i are unchanged.
- The #4764 pointer is dropped until `k3_pp_offload` and `k3_pp_balance` carry the 09-25 rewrite.
- The memory sentence claims only the stack. K3_2P8T models a buffer effect of the message size on the PR head.

**Draft PRs synced (09-25, on the user's word: "这两个是draft PR，直接同步"):**
- `k3_pp_offload` `c73e17c03` became `49117a146` (17 commits on GitHub); `k3_pp_balance` `005cf4aee` became `46692171b` (18 commits). The old heads are pinned as `backup/k3_pp_*_pre_20260925`.
- Before the push: commit messages clean; greps on the diff clean; CPU 96 passed on the offload head and 104 on the balance head, no skips.
- The new bodies are in `PR_BODY_PP_OFFLOAD.md` and `PR_BODY_PP_BALANCE.md`, and #4764's title now says it stacks on #4765.
- The r4097299275 reply points at both drafts again; post it after the bodies are pasted.
- Both drafts carry `901ef34de`, which `k3_pp_text` does not have yet.
- Open before an H100 run: `RemoteBackend` always initializes mooncake with `"tcp"` but puts the pool on the GPU when HCAs exist.

**r4097059930, second pass (09-25), after the user's review** ("目前的回复太墙头草，不客观"):
- The draft now traces why the knob was cleared at each point. First, K3's own split consumed it and core would re-validate it; then the both-set check forced it; then tianyu made the silence an error.
- It takes one position: refuse only what the function would overwrite.
- It shows the cost of the refusal for Kimi K3: at 93 layers, pp8, `layers_per_stage=3` gives vp4 on `901ef34de` and a `ValueError` on `dbd13d397`. It shows the same cost for the models that regress against main.
- It asks tianyu to look again at the change, without offering a flip back.
- The rule is added to both CLAUDE.md files.

## Round 5k：r4097059930 的最终设计与 pp_review5（09-25）

**用户定的设计：**
- jinsooihm 在 #4560 写的 helper 恢复 main 的行为：用 knob 推导 split，并把 knob 保留在 copy 上。
- tianyu 在 r3984964516 要的检查（split 和 knob 同时给定就报错）放进 `TrainingEngine.Config.__post_init__`。这个钩子只校验用户配置，helper 对 `ParallelismConfig` 的 `dataclasses.replace` 不会重跑它。探针实测：`ParallelismConfig.__post_init__` 在构建用户配置时跑 1 次，走完 helper 变成 2 次。
- 检查按用户的决定放进 4312，不单开 core PR。

**raise 的由来**（依据 git log 和 09-13 的会话记录）：
- 09-10：K3 自己的 split 把 knob 静默清空。
- 09-11：tianyu（r3984964516）说清空没必要，建议两者同时给定时在 post_init 里报错。
- 09-13 09:15：`b1f82ba4a` 把检查放在 `Trainer.Config`，这是对的，helper 不受影响。
- 09-13 09:16：我推荐方案 A，改放 `ParallelismConfig.__post_init__`，理由是按字面满足他的原话；用户选了 A。随后 `6e1f5e41c` 只好在 #4560 的 helper 里清空 knob。
- 09-20：r4056259393 之后，清空改成了 raise。
- 09-23：检查随它的测试一起被删掉。
- 结果（`dbd13d397` 上用 `both_given_probe.py` 实测）：只给 knob 会报错，两者同时给反而直接进 `pipeline_llm`，没有任何检查。拦截正好反了。

**分支：**
- `pp_review5` = `90ca18903`（09-25 按用户要求去掉 engine 检查上方两行注释后 amend，原为 `d9b84e505`），09-25 按用户要求推到 fork（旧线备份为远端分支 `backup/pp_review5_pre_20260925`），PR 分支暂不同步。从 `k3_pp_text` `dbd13d397` 切出，cherry-pick 了 `901ef34de` 和 `d9b84e505`（fast-forward，SHA 不变）。
- 旧的 `pp_review5`（`ff769884f`，09-18 误做 23 个 commit rebase 的那条线）：本地 tag `pp_review5_pre_20260925`，fork 上备份为 `backup/pp_review5_pre_20260925`。
- fork 上的 `pp_review4` 已恢复到 `d9b84e505`。之前推过一次 rebase 版本，被用户叫停（"rebase暂时不是需要的"）；旧 head 存在 `backup/pp_review4_pre_rebase_20260925`。
- rebase 到 main `56f04c702` 的版本只留作本地分支 `pp_review4_rebased_local` = `3c5e415b0`：
  - 没有冲突，CPU 115 个通过；
  - B200 格 `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4` 在 8 × 5060 上跑了 3 步，loss 8.10676 / 7.29003 / 5.40627；
  - 在那个 base 上，main 的 #4871、#4872 把 K3 的 expert 参数改成了 fused `w13`（meta 模型对比有 80 个 key 变化），旧 seed checkpoint 加载不进去，body 的表格需要 H100 重跑；
  - #4868 已让 KDA 在 SM 9.0+ 上可用，那个 base 上不再需要本地 guard lift。

**pp_review5 上的检查：**
- body 里那条 CPU 命令：109 个通过，1 个跳过（`test_qwen3_5`，缺 `fla`）。本地运行需要加 `--ignore=tests/unit_tests/cpu/test_torch_checkpointing.py`，因为 `venv_bfx9` 里没有 `torch_checkpointing` 包。
- 两个文件的 pre-commit：flake8、µfmt、pyrefly 都过了。pyrefly 波及全仓库的 25 个文件已逐个回退，KDA guard lift 保留。

**草稿：**
- `REPLY_4312_JINSOO_r4097059930_2026-09-24.md`：第三版，按最终设计写。
- `PR_BODY_PP_v5_2026-09-24.md`：改了状态行、Core 子条目，测试数改为 109。
- 两份都等 `k3_pp_text` 同步到 `pp_review5` 之后再发。

**09-25 squash 并同步 PR：**
- 按用户要求，`901ef34de`（knob 决定推导出的 split）和 `90ca18903`（engine config 的检查，已去掉注释）squash 成 `4abc12940`，tree 与 `90ca18903` 完全一致。
- `k3_pp_text` 从 `dbd13d397` 快进到 `4abc12940`（旧 head 备份为 `backup/k3_pp_text_pre_20260925`），`pp_review5` 也是 `4abc12940`。GitHub 上 PR 4312 是 13 个 commit、22 个文件。
- 现在可以发：先粘贴 `PR_BODY_PP_v5_2026-09-24.md`，再在 r4097059930 下粘贴 `REPLY_4312_JINSOO_r4097059930_2026-09-24.md`。
- 两个 draft PR（`k3_pp_offload`、`k3_pp_balance`）仍叠在 squash 之前的 `901ef34de` 上，需要时再按新的 4312 head 重新叠。
- fork 上的 `pp_review4` 停在 `d9b84e505`（带注释的旧版），已过时。

**09-25 删掉包说明 md：**
- 按用户要求在 `pp_review5` 上加了一个 commit `714267617`（"kimi_k3: the pipeline package drops PP_ATTN_RES_CACHE.md"），删掉 `torchtitan/models/kimi_k3/pipeline_parallel/PP_ATTN_RES_CACHE.md`（38 行）。
- `k3_pp_text` 同步快进到 `714267617`，PR 4312 现在是 14 个 commit、21 个文件。树里、body 粘贴段里都没有引用它的地方。
- 这个文件是 09-23 应 shuhuayu 的 review 主体（id 5285396974，要一张 MOE_SHARDING.md 式的图）加进来的，当时已回复"已加"；删掉等于撤回了那次回应，如需要可以另行说明。

## Round 5l：tianyu r4106905048，检查挪进 ParallelismConfig（09-25）

- tianyu（17:15，`training_engine.py:143`）："can be raised in post init of ParallelismConfig"。
- `pp_review5` = `8979c28ba`，已推到 fork，`k3_pp_text` 未同步：
  - 检查挪进 `ParallelismConfig.__post_init__`；
  - helper 和 K3 入口在 copy 上写入推导出的 split 时，把已经用掉的 knob 清成 None。`dataclasses.replace` 会重跑检查，不清的话 knob 用户就会回归。
- 验证：
  - CPU 测试 109 passed；
  - 组合格在 8 × 5060 上跑 10 步，rc=0，loss 从 8.00045 到 3.51385；
  - K3 knob 探针 `k3_knob_probe_2026-09-25.py`：改动前后都是 4 个 stage、split 相同，copy 上的 knob 从 5 变成 None。
- 回复草稿：`REPLY_4312_TIANYU_r4106905048_2026-09-25.md`。里面明确写了清空 knob 这一点，因为那正是他在 r4056259393 标过的那一行。
- **09-25 晚更正：** 用户指出"把 knob 清成 None 不又是静默覆盖？套娃了"，并说明检查位置按 tianyu 的意思不能再改。于是 amend 成 `7814d1f8b`，pp_review5 已强推到 fork，`k3_pp_text` 仍是 `714267617`。
  - helper 和 K3 入口改用 `copy.copy` 加字段赋值来生成带推导 split 的 copy，不再用会重跑 `__post_init__` 的 `dataclasses.replace`；copy 保留 knob，和 main 一样。
  - 验证：CPU 测试 109 passed；K3 knob 探针显示 4 个 stage、copy 上 knob=5。
  - 组合格 10 步 rc=0。这个格子没有固定 seed，各次第 1 步 loss 不同，只作跑通检查。
  - 回复草稿已按新做法重写。
