# PR 4312 (Kimi K3 text PP): every review round, what is answered, what is open

61 inline comments from tianyu-l over three rounds. Round 1 is design, round 2 is the transport shape, round 3 is the line-by-line pass. The one question he has asked in EVERY round is the numerics gap.

## Round 1 — 2026-09-03, 18 comments (design)

| theme | comments | state |
| --- | --- | --- |
| debug model shape: no defaults in `_debugmodel`, make the layer pattern irregular like the real 90+3 ((3 KDA + 1 MLA) x k then (2 KDA + 1 MLA)), block size always 12, the pattern deducible, one debug model shared by PP and non-PP | 3921824279, 3921834648, 3921845321, 3921851558 | the 33-layer model answers it (35 units, no pp shape divides it); round 3 reopened the size question (#1), so the depth is still the open decision |
| one integration cell is enough | 3921864040 | done, one cell |
| why the `model.py` import changed | 3921867809 | reverted |
| "even split" in the Interleaved1F1B note: why would it matter | 3921906845 | answered in the round-2 reply |
| **step-10 numerics gap too large; same global batch size across settings?** | **3921918643** | asked again in round 3 (#19); answered now with the three controls and the per-layer profile |
| a PP-infra flag living in `model.py` | 3921925657 | moved |
| `first_layer_in_block` can be computed at init (a suggestion block) | 3921992175 | **only applied in round 3** — he had to repeat it |
| why no `// 2` as in the AttnRes paper; why not `cat` at the start of the layer | 3922255759, 3922280054 | answered in the round-2 reply |
| calling a private function across files | 3922481936 | the private import came back in the pp8 x vp4 recipe and is being removed again now |
| PyTorch PP already caches (with @sanketpurandare) | 3922515656 | answered: the schedule's buffers are per edge, the block store is per rank and keyed by micro-batch |
| "inject sounds hacky" — build K3's split bottom up instead of reusing a util | 3922570890 | done: the split is generated, not patched |
| how do you evenly divide 93 layers | 3922580018 | you do not; the split takes any count |
| the report's cache-based communication: (1) only new blocks on the wire, (2) released when the micro-batch finishes | 3922690440 | both, answered with the routing table and the store's release rule |
| activation lifecycle: does PP=1 keep stacks of 1..93 (quadratic)? | 3922719284 | quadratic in the block count, not the layer count (1..8 for 93 layers = 36 rows) |

## Round 2 — 2026-09-10, 4 comments (transport shape)

- "the refactor looks much cleaner" (3976922238).
- Two options he and @sanketpurandare considered: option 1 skip connections (rank 0 sends to 1, 2, 3), option 2 adjacent relay (rank i sends 0..i to i+1); he reads the PR and the zhihu post as option 1 and prefers it short-term (3976922942, 3976936998).
- #4486 is not the route, at least not for MTP + PP (3976936998).
- The activation save/release policy, with a concrete example: 2 blocks of 4 layers (3976976576).

Answered in `REPLY_4312_2026-09-10.md`: the PR is a third shape — option 2's topology (adjacent edges only, no new communicator) with option 1's volume (no hop carries a block the receiver already holds); `attn_res_cache=False` IS his option 2 and is the A/B of every table; the 4-layer example is written out per layer.

## Round 3 — 2026-09-11, 19 comments (line by line)

18 code comments (handled on `pp_review4`: flag at init, split-knob exclusivity, the core split generator instead of a K3-specific function, no collective in the layout, `PPRankLocalCache`, docstrings, transport and warm-up out of the PR) and #19, the numerics, answered in `REPLY_4312_NUMERICS_2026-09-11.md`.

## Open, needing the user

1. **Debug model depth** (#1): 33 layers = 1.40 B parameters. 17 layers = 0.89 B, 13 = 0.77 B. Changing it re-runs every number in the PR.
2. **Does the transport stay out** (now `k3_pp_transport`), pending Elfie's two-node evidence.
3. **Numerics on his hardware**: the reply offers the same four rows on H100.
