# Reply to tianyu-l's 2026-09-05 review of 4492 (declarations), drafted 2026-09-06

Review comment: "numerics difference look too huge, are you testing with identical global batch size across?" Inline: "what's the gap for TP?" (the user answers that one and opens the TP/SP draft).

## Reply (numbers to fill from `decl_pairs` and `decl_dump`)

Yes: every cell trains on 8192 tokens per step in 256-token micro-batches, from one seed checkpoint.

The gap is between the rows, not the backends. The loader shards documents by dp rank, so the dp1 and dp2 rows read different samples: their step-1 losses already differ (12.52977 against 12.53137) with identical weights and a forward that reorders nothing, and from step 2 on they train on different data. The dp1 pair, the one comparison the table made on the same data, is bitwise across the two backends.

I have rebuilt the table around same-data pairs: each cell under `partial_dtensor` and under `spmd_types`, on one compile cache.

| cell | world | partial_dtensor (step 1 / 3 / 10) | spmd_types (step 1 / 3 / 10) |
|---|---|---|---|
| dp1 | 1 | FILL | FILL |
| dp2 | 2 | FILL | FILL |
| dp2 x ep2 | 2 | FILL | FILL |

Step-1 gradients of the same cell under the two backends (every parameter, rank 0): FILL (N/M parameters bitwise, largest relative norm difference X, sign flips Y%).

The remaining step-3 / step-10 movement is this flavor's own: FILL (the noise-floor row: the same dp1 cell on two compile caches / the same-data dp2 vs dp2 x ep2 pair) -- bf16 end to end at lr 8e-4 with a 2-step warm-up, so Adam's first update is lr times sign(g) and elements below bf16 rounding flip between any two reduction orders. Step 1 and the step-1 gradients are the correctness bar in every table from here on; step 10 is shown, not argued from.

Note also why the last three rows exist: they are 4446's own CI cell (`kimi_k3_debugmodel_mm_fsdp2`: `_use_spmd_types(typechecking=True)`, which turns activation checkpointing off because the checker rejects selective AC with FlexAttention), the cell this PR unbreaks. Relabelled as such in the body.
