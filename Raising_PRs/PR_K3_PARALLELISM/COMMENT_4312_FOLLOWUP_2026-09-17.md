# PR 4312 follow-up comment (to post on 2026-09-17 or 18, one week after the second round)

Context (not for pasting): Tianyu's rounds were 2026-09-03 (changes requested, 18 comments) and 2026-09-10/11 (24 comments); Elfie left one comment on 09-11; every thread was answered by 09-13 with "have done all the responses and checked again on the numerical results", and the last activity on the PR is 09-14. The tracking issue #4272 names #4312 in its P0.5 item "Add pipeline and virtual pipeline parallelism with attention residuals". TP (#4499) merged on 09-16 and main `810e62786` is 54 commits ahead of the PR's base `de6f29514` (37 ahead the other way); `git merge-tree` lists five conflicting files (checked 2026-09-16 night); the user's rule is to rebase only when asked. Tianyu's public activity: 09-15 and 09-16 he reviewed about fifteen other PRs (#4596, #4684, #4685, #4704, #4676, #4637, #4703, #4499 among them), none on #4312, so the PR is in his queue, not forgotten. The comment below adds information (the branch still applies on main after the TP merge; the two follow-up drafts stacked on it) and offers the rebase timing as his decision, so it reads as a status note, not a nudge. Post it together with the two draft PRs (`k3_pp_offload`, `k3_pp_balance`), whose bodies reference #4312, so the activity is visible from the PR's timeline. No @mention of anyone but Tianyu.

--- PASTE BEGIN ---

@tianyu-l a short status note before your next pass. Every comment from the 09-10 and 09-11 rounds has a reply in its thread, and the numerical results were rechecked on 09-13 (the table in the body is the current one). Since then #4499 merged and main is 54 commits ahead of this base; a dry-run merge conflicts in five files (`kimi_k3/__init__.py`, `model.py`, `parallelize.py`, `config/configs.py`, `test_integration_test_definitions.py`), all against the TP and CP merges, and the resolution is the one my integration tree already carries. I have kept the PR at its current base so the review diff does not move under you. Tell me whether you prefer the rebase before or after your next round and I will do it that day.

Two follow-ups from the report's memory section now sit as draft PRs stacked on this one, marked DO NOT review: the rank-store offload to pinned host memory and the activation balance across PP ranks through the Mooncake Transfer Engine. They only need this PR's `pipeline_kimi_k3` entry and change nothing here; I mention them so the stacking is visible, not to widen this review.

--- PASTE END ---
