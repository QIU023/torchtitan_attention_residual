# Replies for PR 4313 (drafts, English, one paragraph each; post as the user)

## To fegin's conversation comment (Ulysses interface, 2026-09-03 20:56Z)

Agreed, going with your first option: the packed exchange now lives in `MLAUlyssesCPFlexAttention`, a subclass of the kernel in 4450 -- the inner attention keeps the `(q, k, v)` interface, the kernel splits the expanded key on its rope slice, moves `(q | k_nope | v)` in one all-to-all and the rope slice once as `[T, R]`, and expands after the exchange. The copy is one packed tensor per layer; if it ever shows in a profile, the `MLAAttention` interface with `k_nope` and `k_rope` handed over separately is the next step. The branch is rebased onto your stack (4322 / 4449 / 4450) and the copies from 4322 are gone; I will push it to the PR once the stack lands, so the diff here is only the K3 side.

## To fegin's inline reply on all-gather KV (2026-09-03 21:00Z)

Yes -- the generic kernel from 4322 runs on this model as is, and `MLAAllGatherCPFlexAttention` is the extend version: it gathers the packed nope key and v and the rope slice once instead of the expanded K and V (`H * R - R` fewer values per token), with the `reduce_dtype` from 4449. Both are selectable from the recipe, and the results table will carry each packed kernel against its generic counterpart on the same seed and batch.

## To tianyu-l's "I'll wait for an attn-gym version then?"

Done on the branch: the KDA layers run `attn_gym.linear.kda.context_parallel_kda` with `context_parallel_conv_history` for the conv history, and fla is gone from the CP path. The one thing left on the K3 side is a transform for the KDA layers (`torchtitan_recipes/kimi_k3.py`), since KDA is not a `BaseAttention` config and `ContextParallelTransform` / `validate_context_parallel` do not see it; if you would rather have the generic transform take a kernel per attention type (the TODO in `transforms/context_parallel.py`), I am happy to send that as a small separate PR.

## To tianyu-l's text-only variant comment (fsdp.py)

Happy to follow the muse_glimmer pattern (a text variant and an `_mm` variant, the latter assuming multimodal input is present); it removes the zero-valued dependency and the CP shard selection from the splice entirely. Since it changes the registry that the EP PR merged with one multimodal debug flavor, could you confirm that is the direction before I split the flavors?
