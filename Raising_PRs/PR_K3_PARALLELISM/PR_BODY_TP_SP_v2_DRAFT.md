# PR title: [Kimi K3] Tensor parallelism with sequence parallel

Body for PR 4499, round 3 (2026-09-15). Review branch `tpsp_review4` = `c0dad3e91`: twelve commits on upstream main `d34a13fdf` (the four PR commits rebased, the round-3 commits on top, two of them pushed from the other box). The H100 tables were measured on `48632ad41`; `c0dad3e91` only renames `_tp_unsharded_weight_config`'s argument (the 3016 sharding configs of the four sp/ep combinations are identical), so they stand for it. The rebase moved main's own tp=1 reference (#4535 made the fused `w13` the default for K3's dense FFN; tp=1 step-1 loss on a 5060 went from `12.50616` on `56a721b64` to `12.60343` on `d34a13fdf`), so every table below is a fresh run against `d34a13fdf`.

Notes, not for pasting: the Results columns stay at steps 1 / 10 / 20 (user, 2026-09-15). Both references memorise the debug set before step 100 (dp1: `1.41617` at 60, `1.04615` at 90; dp2: `1.39518` at 60, `0.71059` at 90, `0.56454` at 100), so no later step is reported; the 100 steps back only the "identical" claims. "identical" is the logged values (loss to 5 decimals, grad norm to 4) on all 100 steps; "same" is one step. Tests on the H100 box: `test_integration_test_definitions.py` and `test_no_new_cli_options.py` 17 passed; `tests/unit_tests/gpu/test_kimi_k3.py` and `test_kda_attention.py` 3 passed, 2 skipped (the KDA kernel tests skip below capability 10.0; the model cells above ran with the guard widened locally). Pinned pyrefly on `sharding.py`: 0 errors. The `routed_down` before/after comparison was not run on H100 (user, 2026-09-15: not needed); the 5060 smoke showed the routed_down commit identical at print precision. Logs: `phase13_k3like_48b_posttrain/tp_h100x4_logs_2026-09-15/` (tables regenerated by `tp_body_tables.py` there).

--- PASTE BEGIN ---

### Summary

Tensor parallelism, with and without sequence parallel, for Kimi K3 on spmd_types: KDA and MLA become head-parallel, the latent MoE takes core's expert sharding, and the MoonViT tower reuses Kimi K2.5's plan. At tp=1 nothing changes: loss and grad norm match main on all 100 steps.

- Before: `parallelize_kimi_k3` refuses tensor parallel, and MLA / KDA reshape to the global head count (`view(num_tokens, n_heads, ...)`). After: `local_head_split` splits the rank-local heads; `wq_b`, `wkv_b`, `gate` and KDA's `q_proj`, `k_proj`, `v_proj`, `forget_b`, `beta`, `output_gate` are colwise; `wo` and `output_proj` are rowwise, reduce-scattering under SP.
- Two Kimi K2.5 commits sit under it. `kimi_k2_7/vision_encoder.py`: the packed position tables retype only the dp axis under type checking; retyping tp as well is refused at tp > 1, where the tables are already invariant on tp. `kimi_k2_7/sharding.py`: K2.5's MoonViT plan is public as `set_moonvit_sharding_config`, with the projector norm named by the caller (`pre_norm` in K2.5, `post_norm` in K3). The first can go as its own PR.
- No core file changes.

### Design

- `kimi_k3/sharding.py`: `set_kimi_k3_sharding_config(config, *, enable_sp, enable_ep)` fills every sub-config unconditionally, as DeepSeek V3 does; `Module.parallelize` drops the disabled axes.
  - MLA: DeepSeek V3's plan without RoPE; `wq_a`, `wkv_a` and their norms replicated; inner attention through core's `set_gqa_inner_attention_local_spmd`; the headless rope key is broadcast to the local heads under `spmd.local()`.
  - KDA: Qwen3.5 GatedDeltaNet's head-sharded plan; `forget_a` and `output_norm` replicated; the conv weights, `A_log` and `dt_bias` shard with the heads; the kernel is one `local_spmd` region on the local heads.
  - Latent MoE: core's `set_moe_sharding_config`, plus core's padding-mask sharding on the MoE layers. Under EP the routed path follows the router: `routed_down` takes the rank's token shard (without SP it drops the other TP ranks' tokens on entry), the experts' output stays token-sharded, and `routed_norm` and `routed_up` run on those tokens with Replicate weights, so their gradients reduce over TP; without SP, `routed_up` re-enters Partial and the MoE exit reduces once with the shared experts. Without EP the experts are TP-sharded: `routed_down` is replicated, and without SP the experts' Partial output is reduced at `routed_norm`.
  - Multimodal: `tok_embeddings` stays TP-replicated for the vision scatter; layer 0's input boundary puts the stream and the block-residual stack back in the decoder's layout (sequence shard under SP, invariant otherwise).
- `kimi_k3/model.py`: `update_from_config` runs core's first, then checks that tp divides the tower's head count (as K2.5 does), then declares; the batch uses core's `multimodal_input_sharding()`.
- Not covered: TP x CP (#4500's); tp above 2 on the debug model, whose tower has 6 heads (the released tower has 12).

### Results

```bash
torchrun --nproc_per_node=2 -m torchtitan.train --module kimi_k3 --config kimi_k3_debugmodel \
  --debug.seed 42 --debug.deterministic --training.steps 100 \
  --training.num-tokens-per-train-step 256 --training.num-tokens-per-microbatch-per-dp-rank 256 \
  --parallelism.tensor_parallel_degree 2   # --parallelism.no-enable-sequence-parallel for SP off
```

dp1, 256 tokens per step, against tp=1 on main; "identical" means the same logged loss and grad norm on every one of the 100 steps.

| cell | loss, step 1 | step 10 | step 20 | grad norm, step 1 | step 10 | step 20 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| tp=1, main | `12.44473` | `4.12222` | `3.31114` | `27.2500` | `6.0625` | `6.1875` |
| tp=1, this PR | identical | identical | identical | identical | identical | identical |
| tp=1, this PR, fresh inductor cache | identical | identical | identical | identical | identical | identical |
| tp=2, SP on | `12.45268` (+0.064%) | `4.44351` (+7.79%) | `3.20842` (-3.1%) | `27.8750` (+2.29%) | `6.9062` (+13.9%) | `6.2188` (+0.51%) |
| tp=2, SP off | `12.44544` (+0.0057%) | `4.24770` (+3.04%) | `3.38211` (+2.14%) | `27.5000` (+0.92%) | `8.8125` (+45.4%) | `6.6875` (+8.08%) |

dp2, **512 tokens per step** (256 per rank; a multimodal row does not fit in 128), against dp2 on this PR; "same" means the same logged value at that step.

| cell | loss, step 1 | step 10 | step 20 | grad norm, step 1 | step 10 | step 20 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| dp2 | `12.45935` | `4.07423` | `3.38009` | `24.5000` | `6.0625` | `4.4375` |
| dp2 x ep2 (no TP) | same | `3.72571` (-8.55%) | `3.40608` (+0.77%) | same | `4.7188` (-22.2%) | `4.2500` (-4.23%) |
| dp2 x tp2 | `12.50688` (+0.38%) | `3.88132` (-4.73%) | `3.41127` (+0.92%) | `24.7500` (+1.02%) | `5.0625` (-16.5%) | `4.7500` (+7.04%) |
| dp2 x ep2 x tp2 | `12.52374` (+0.52%) | `4.07369` (-0.013%) | `3.45136` (+2.11%) | `24.3750` (-0.51%) | `4.5938` (-24.2%) | `4.6250` (+4.23%) |
| dp2 x ep2 x tp2, SP off | `12.48901` (+0.24%) | `4.02450` (-1.22%) | `3.42512` (+1.33%) | `24.1250` (-1.53%) | `8.4375` (+39.2%) | `5.3438` (+20.4%) |

Kimi K2.5, dp2, tp=1, 4096 tokens per step (its debug config), this PR against main; K2.5 refuses tp > 1 on main (DistMuon, #3353).

| cell | loss, step 1 | step 10 | step 20 | grad norm, step 1 | step 10 | step 20 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| main | `8.01201` | `4.60605` | `3.24834` | `8.8055` | `4.5538` | `2.9925` |
| this PR | identical | identical | identical | identical | identical | identical |

Type checking, 3 steps with `--debug.spmd_typechecking` and activation checkpointing off (as the b200 recipe sets it): the b200 cell `kimi_k3_debugmodel_mm` (fsdp 2 x tp 2 x ep 2) with SP on and off on 4 GPUs, and dp1 x tp2 on 2 GPUs, pass.

4 x H100 80GB HBM3 (SXM), main `d34a13fdf`, torch `2.15.0.dev20260906+cu130`, `attn-gym` 0.0.8 (main's pin), spmd_types 0.2.5; KDA's capability guard widened locally to admit SM 9.0, not part of this PR. For scale, #4500's CP=2 rows on H100: +3.8% and +15.1% loss at step 10.

### CI/CD Coverage

`kimi_k3_mm` in the b200 suite: `kimi_k3_debugmodel_mm`, fsdp 2 x tp 2 x ep 2 with SP and type checking, 4 GPUs; it replaces `kimi_k3_mm_fsdp`. `test_integration_test_definitions.py` and `test_no_new_cli_options.py` pass on this head.

--- PASTE END ---
