# Replies to shuhuayu's 18 inline comments on PR 4312 (2026-09-21 to 2026-09-23)

All addressed on `pp_review4` = `1b361b8e4`, six typed commits on upstream main `7349a2282` (the sixth is the composability cell); the PR branch `k3_pp_text` stays at `498dc5731` until the user moves it. Line numbers are the reviewer's, on the PR head `498dc5731`. Paste each block into its thread.

## 2026-09-22, already addressed before this round (one line each)

- `model.py:485` (remove the alias) -- Removed.
- `model.py:505` (the `if` instead of the ternary) -- Taken as suggested.
- `model.py:515` (remove the comment) -- Removed.
- `layout.py:19` (the redundant `num_blocks` argument) -- Removed; the table derives it.
- `layout.py:34` (drop the `expected_blocks` check) -- Dropped.
- `layout.py:42` (`self.num_blocks = -(-n_layers // layers_per_block)`) -- Taken as suggested.
- `layout.py:88` (redundant check) -- Removed.
- `layout.py:12` (note the loop-style assumption) -- Added to the class docstring, and a runtime guard now refuses the other shape; see the reply on `pipeline_parallel/__init__.py:67`.

## 2026-09-21 and 2026-09-23

--- pipeline_parallel/__init__.py:67 ---
Guarded now. With `attn_res_cache` on, `pipeline_kimi_k3` checks the schedule's stage-to-rank map against the loop-style assignment, stage `s` on rank `s % pp`, and raises a `ValueError` naming the schedule and the first stage that breaks it. A CPU test covers both shapes. Without the cache the transport carries the whole stack every hop and any assignment works, so the guard is scoped to the cache.

--- stage.py:117 (the TODO on the stage) ---
"my comment would be not at least in the current form since there are many kimi k3 specific features." Agreed; the TODO is gone.

--- stage.py:79 (`route_payload`) ---
Renamed to `pack_outgoing_delta`, with its two call sites and the test.

--- torchtitan_recipes/tests/b200.py:34 (composability) ---
Added `kimi_k3_fsdp2_tp2_ep2_pp2` to the B200 suite, eight GPUs, FSDP 2, TP 2 with sequence parallel, EP 2 and PP 2 under 1F1B with four micro-batches, type checking off as the other pipeline recipes have it. On 8 x RTX 5060 Ti it runs three steps (`8.21962 / 7.23810 / 5.35913`, peak 0.49 GiB); the B200 run is CI's.

--- tests/unit_tests/cpu/test_pipeline_parallel.py:83 (the layers_per_stage refusal test) ---
Kept, because the refusal it covers was asked for: tianyu's comment on `pipeline_parallel.py:184` ("should assert `pipeline_parallel_layers_per_stage` being None at the beginning of this function? o/w overriding user config"). The helper derives its own split, so a `layers_per_stage` it would silently override is refused with a `ValueError`, and this test is the one that pins that. The broader config-level check that both fields describe the split is removed, per the next comment.

--- tests/unit_tests/cpu/test_config_manager.py:660 (the "one way" test) ---
Removed, with the `ParallelismConfig.__post_init__` check it tested. The refusal in the helper is enough for this PR.

--- tests/unit_tests/cpu/test_kimi_k3_pp_exact_block_grads.py:1 (rename) ---
Renamed to `test_kimi_k3_pp_block_grads.py`.

--- tests/unit_tests/cpu/test_kimi_k3_stage_swap.py:32 (the fixed port) ---
The gloo group now initialises on an in-process `HashStore`, so there is no port to collide on.

--- tests/unit_tests/cpu/test_kimi_k3_stage_swap.py:1 (merge, and codex's point) ---
Merged into `test_kimi_k3_pp_stage.py` as `TestStageSwap`. `setUp` records whether it created the process group, and `tearDown` destroys it only in that case.

--- tests/unit_tests/cpu/test_kimi_k3_pp_layout.py:153 (the recipe import) ---
The test spells the pp4 x vp4 split out itself and imports no recipe module.
