# Replies to Shuhua's review of #4499 (2026-09-11)

Review branch `tp_sp_on_main` (the PR branch `k3_tp_sp` is untouched until the user updates it). New stack on upstream main `da2f82670`: `ddb306332` K2.5 tables, `fc44fbb80` the declarations, `bb2dad9da` the spmd_types requirement, `d4d6e774c` the b200 cell. Each block below is one reply to paste on the thread named; "TODO-NUMBERS" marks where the GPU cells go once the matrix finishes.

## Top-level reply

Thanks for the review. All 18 comments are addressed on the rebuilt stack, rebased onto current main (`da2f82670`) so CI can run:

- The vision tower is tensor-parallel now: it takes Kimi K2.5's MoonViT plan through the common helpers (`set_vision_transformer_block_sharding_config`, `vision_colwise_config`, `vision_scaled_bias_rowwise_config`, `invariant_norm_config`), and K3 customises only its projector (norm after the second linear). The invariant override, `_set_vision_encoder_tp_invariant_sharding`, is gone.
- The multimodal splice follows Kimi K2.5 / Qwen3.5: the token embedding stays replicated on TP when a tower is present, the scatter indexes the whole sequence on every rank, and layer 0's input boundary restores the decoder's layout (sequence shard under SP, invariant otherwise). No process-group attribute on the model, no DTensor branches in `_prepare_multimodal_embeds`, no re-shard in `forward`.
- The core changes are reverted: `common/attention.py` (the invariant-tower head split) and `common/linear.py` (RouterGateLinear on local shards) are back to main's; every tp cell runs without them on spmd_types.
- `set_tensor_parallel_sharding_config` is folded into `set_kimi_k3_sharding_config(enable_ep, enable_tp, enable_sp)`, called after `Decoder.Config.update_from_config`; CP is out of this stack; the KDA capability commit is dropped; MLA uses `local_head_split` and the two suggestions are applied; the SP splice unit test is deleted, the b200 cell `kimi_k3_mm_tp2` is the coverage.
- The one commit that stays at the bottom is the K2.5 tables retype, because K2.5 itself needs it: on `da2f82670` the K2.5 debug model at dp2 x tp2 with type checking fails at the shared position tables with `mutate_type: expected current type R on axis mesh_tp, got I`; with the commit it gets past them (and then hits K2.5's own `QK clip scales do not match the MLA weight shape` under TP, a separate problem). Happy to split it into its own PR if you prefer.

On our 8-GPU box (3 steps, seed 42, deterministic, spmd_types, 4096 tokens per step, loss at steps 1 / 3; the type-checked cells at the recipe's batch): tp=1 is bitwise with main (12.53584 / 6.63797 at this batch; 12.53909 / 11.37444 / 10.01040 at the recipe's, both with main's grad norms); tp=2 SP on 12.56396 / 6.53527, tp=2 SP off 12.55333 / 6.81346, dp2 x tp2 12.53445 / 7.02513, dp2 x ep2 x tp2 12.52623 / 7.00102; with `--debug.spmd-typechecking` the b200 cell (tp=2, SP on) reads 12.49262 / 11.38085 / 10.23243 (deterministic across two runs), tp=2 SP off 12.48463 / 11.41571 / 9.89427, dp2 x tp2 12.56306 / 11.38314 / 9.64091. With type checking at the same 4096-token batch and a seed-42 init per cell (no shared seed checkpoint): tp=1 12.51269 / 9.93869 / 7.14302, tp=2 SP on 12.45116 / 9.86473 / 7.14307, tp=2 SP off 12.44463 / 9.90965 / 7.12468. tp=4 with the multimodal debug model is now refused by the head-count check (the tower has 6 heads); the previous head's tp=4 rows ran with the tower replicated. The 100-step tables for the PR body will be re-measured on this stack.

## Per-comment replies

1. `kda.py:90` (3984103739), the capability guard — Reverted; that commit was for our own box and not part of TP. (Commit dropped.)
2. `kda.py:266` (3984448939), the comment block — Removed.
3. `model.py:59` (3984477843) / 4. `model.py:132` (3984513458) / 5. `model.py:145` (3984515396), `local_head_split` — Done: `_local_head_split` is gone, MLA's q and kv splits go through core's `local_head_split(t, head_dim)` exactly as KDA's.
6. `model.py:162` (3984566641), the rope expand — Applied as suggested (`k_rope_TK.unsqueeze(1).expand(-1, k_nope_THK.shape[-2], -1)`). The expand-and-cat still sits in a `spmd.local()` region with the head-sharded assert, because the checker cannot propagate a replicated headless slice through the cat with the head-sharded nope part.
7. `model.py:176` (3984574580), `flatten(-2)` — Applied; `h_local` and `num_tokens` are gone.
8. `model.py:386` (3984662548) / 13. `sharding.py:292` (3984713305), one `set_kimi_k3_sharding_config` — Done: one entry point with `enable_ep`, `enable_tp`, `enable_sp`; the TP declarations are a private helper it calls.
9. `model.py:374` (3984667863), the ordering — Done: `Decoder.Config.update_from_config` (the validation) runs first, then the sharding declarations.
10. `model.py:428` (3984677858), `_sp_group` — Gone. The splice uses the declaration pattern K2.5 and Qwen3.5 use: `tok_embeddings` replicated on TP when a tower is present, layer 0's input boundary (on `x_TD` and the empty `block_residual_TND` cut from it) restores the decoder's layout.
11. `model.py:473` (3984695935), `_prepare_multimodal_embeds` / `forward` — Done the Qwen3.5 way: `_prepare_multimodal_embeds` is the plain embed / encode / scatter; `forward` keeps only the DP assert.
12. `sharding.py:47` (3984700503), CP — Dropped from this stack.
14. `sharding.py:390` (3984739669), the tower — Done: K2.5's plan through the common helpers, the tower's linears colwise / rowwise, norms and tables invariant, the encoder's output boundary I -> R; K3 adds only its projector's `post_norm`. The invariant helper is gone. Two things followed from sharding the tower: the vision inputs are typed through `multimodal_input_sharding()` (`{dp: V, tp: I}`, as K2.5 and Qwen3.5 do) instead of K3's own dp-only layout, which the type checker caught at the tower's first boundary; and K2.5's check that the tensor-parallel degree divides the tower's head count is ported, since the debug tower has 6 heads (so tp=2 runs and tp=4 is refused with `tensor_parallel_degree (4) must divide vision num_heads (6)`; the previous head's tp=4 rows ran with the tower replicated).
15. `kimi_k2_7/vision_encoder.py:123` (3984820549), the tables — K3 reuses K2.5's tower sharding now, and this file's change is still needed, by K2.5 itself: on `da2f82670` the K2.5 debug model at dp2 x tp2 with `--debug.spmd-typechecking` fails at `_compute_learned_pos_embeds` with `mutate_type: expected current type R on axis mesh_tp, got I` (the table is declared invariant on tp by `pos_embed`'s state layout, and `mutate_type(src=R, dst={"dp": V, "tp": I})` asserts R there); with the change it gets past the tables and then hits `QK clip scales do not match the MLA weight shape`, K2.5's own TP problem. The commit only mutates the dp axis and leaves the tp declaration as it is. Kept at the bottom of the stack; can be its own PR.
16. `common/attention.py:116` (3984832175) / 17. `common/linear.py:113` (3984833557) — Both reverted to main; the tp cells run without them.
18. `tests/unit_tests/cpu/test_kimi_k3_sp_splice.py:1` (3984838119) — Deleted; the b200 cell `kimi_k3_mm_tp2` (SP on, type checking) covers the splice.
