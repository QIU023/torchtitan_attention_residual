# PR 4312: replies to tianyu's and shuhuayu's comments of 2026-09-24 (round 5)

For the user to post after pushing `pp_review4` to `k3_pp_text`. One block per thread, pasted as a reply under the comment id named in its header. No commit hashes in the text, since the branch may be rebased again before the push.

--- r4089852926 (tianyu, the svg; reply under the user's own reply 4090035291): PASTE BEGIN ---

Removed. The page keeps its text, and the figure will come in a change of its own.

--- r4089864338 (tianyu, "vp" or "vpp"): PASTE BEGIN ---

Not an established term. torchtitan's own is stages per rank (`stages_per_rank` in `_get_pipeline_metadata`), with no short form in any cell name, so the names now say vpp, as suggested. The one pipeline cell left is `kimi_k3_fsdp2_tp2_ep2_pp2_vpp4`; the pp4 x vp4 cell is gone, per the thread with shuhuayu on the composability cell.

--- r4089902889 (tianyu, "simply raise on CP"): PASTE BEGIN ---

Done. `parallelize` raises `NotImplementedError` when context parallelism is on, without the list.

--- r4089919460 (tianyu, private helpers in stage.py): PASTE BEGIN ---

Done for all three: `_assemble_stack`, `_pack_outgoing_delta` and `_split_stack_grad`. The stage test imports them under those names.

--- r4089922488 (tianyu, a cache.py): PASTE BEGIN ---

Moved. `PPRankLocalCache` is in `kimi_k3/pipeline_parallel/cache.py`, and `stage.py` keeps the stage and its three helpers.

--- r4089938897 (tianyu, why @overload): PASTE BEGIN ---

It was only there to type `return_split=True`, the extra split Kimi K3 read back, and it is gone. The split now has a function of its own, `get_module_fqns_per_model_part`, which `pipeline_with_first_last_stage_modules` calls, so that function returns the same four values as `pipeline_llm`. Kimi K3 takes the split first, since it routes along it, and hands it to `pipeline_llm`.

--- r4089969281 (tianyu, get_present_modules): PASTE BEGIN ---

Applied, with its two call sites renamed as well.

--- r4078050002 (shuhuayu's composability thread; reply under 4090046115): PASTE BEGIN ---

Done. The cell is `kimi_k3_fsdp2_tp2_ep2_pp2_vpp4` ("Kimi K3 FSDP, TP, EP and PP with 4 VPP stages"): `Interleaved1F1B` with four stages per rank on eight GPUs, so its hops include ones that carry no new block and its blocks have three later readers on a rank. The pp4 x vp4 cell is removed, and the layout test keeps that split as a CPU case.

--- r4080717964 (shuhuayu, the deposit count check; reply under the user's 4080853091): PASTE BEGIN ---

Left a comment at the check. The expected count covers every later stage on the rank that holds the block, and a stage deposits only if its stack reaches its backward: through a transformer layer, the output aggregation, or a non-empty outgoing delta. Core's split meets that; a stage with none of the three fails there.

--- PASTE END ---
