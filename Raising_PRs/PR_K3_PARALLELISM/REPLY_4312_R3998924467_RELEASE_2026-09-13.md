# Follow-up to r3998924467 (per-stage tables): when a micro-batch's block activations are freed

The posted reply (r3998924467) ends at the backward table and the gradient-entry count; the release paragraphs of the draft (`REPLY_4312_ACT_MEMORY_2026-09-12.md`, written for the 2-block example) were not posted. Checked against the code at `k3_pp_text` = `6e1f5e41c`: `AttnResPipelineStage.forward_one_chunk` calls `store.release(mb)` on the rank's last stage; torch's `backward_one_chunk` pops `fwd_cache[mb]` (the stage's saved inputs and outputs) in that stage's backward; `PPRankLocalCache.collect` pops a deposit and `backward_one_chunk` raises if one is left after the rank's first stage. Post as a reply under r3998924467.

--- PASTE BEGIN ---

One thing the tables above leave out: when a micro-batch's block activations are freed, taking the 4 blocks x 4 layers pp4 x vp4 example.

- Rank cache (forward): a block enters the rank's cache in the forward of the stage that brings it onto the rank (the commit, or the first receive), as a detached view of that stage's stack or received payload, not a copy. The rank drops all of a micro-batch's blocks right after the forward of its last stage (r0 at s12, r1 at s13, r2 at s14, r3 at s15), before any backward of that micro-batch.
- Backward: what the backward needs is not in the cache. Each stage keeps its own input stack (assembled from the payload and the cache) and its saved activations in the pipeline's `fwd_cache`, and frees them in its own backward of that micro-batch, the same as with the cache off. So a block's forward values on a rank are freed stage by stage as the backward goes s15 -> s0.
- Gradient deposits: a deposit lives from the backward of the stage that read the block from the cache to the backward of the stage that brought the block onto the rank, which adds it in and drops it. On r3: `dx12` from s15 to s11, `dx8` from s15 and s11 to s7, `de` and `dx4` from s15, s11 and s7 to s3. Each rank checks that no deposit is left after its first stage's backward.

--- PASTE END ---
