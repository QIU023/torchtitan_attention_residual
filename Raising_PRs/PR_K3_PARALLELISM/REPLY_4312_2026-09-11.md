# Replies for PR 4312, Tianyu's 2026-09-10/11 round (drafts, post as the user)

发之前必须先做的三件事（2026-09-11 复核：分支未动，三条都还开着）：

1. `pp_review4` = `c6cea394e` 的基还是 `6e2ac3dcd`，落后 upstream/main 60+ 个提交；先把 6 个
   新提交挪到 `pp_review5`（`6042863a4`，已 rebase 到 `d398a8fb9`）上，或重做一次 rebase。
2. **pp8 x vp4 recipe 丢了 `vision_encoder`**（在 `c6cea394e` 上复核仍然存在）：recipe 显式写死
   `module_fqns_per_model_part`，里面只有 `last_stage_modules`；而 `pipeline_llm` 只在
   `module_fqns_per_model_part is None` 时才应用 pinned 模块 → `vision_encoder` 不在任何一段 →
   `_split_module` 每段置 None → 多模态 debug flavor 第一段拿到 `pixel_values` 就
   `raise ValueError("pixel_values were provided without a vision encoder.")`。8 卡 CI 格必红。
   补 `first_stage_modules=("vision_encoder",)`。
3. 下面 "L144" 那条的回复按 **改用上游 `pipeline_with_first_stage_modules`** 写的；#4560 已于
   09-10 合入（`310e2a66e`），把 L144 的 `pipeline_vlm` 变成了公开入口。现在分支上的做法是给
   `_generate_llm_fqn_per_model_part` / `pipeline_llm` 加 `first_stage_modules`，与它重复，
   rebase 必冲突。要么改代码再发这条回复，要么把这条回复改成提案（"打算这样改，先确认"）。
   注意 Tianyu 当初点的就是 L144，顺着 #4560 改反而更好卖。

**第 3 节（数值）已作废，不要贴**：GPU 盒子实测 cache on 与 cache off 在 pp2 上十步逐位相同
（V=1 时 delta 就等于整栈，本来就没有 deposit），而我那段把两种传输写成累加顺序不同——Tianyu
一条命令就能证伪。以 `REPLY_4312_NUMERICS_2026-09-11.md` 的 PASTE 块为准，它有 dp1 反序累加的
噪声底行（无 PP、同样量级的漂移），正好回答他"举个累加顺序的例子"。

两份附录末尾那句"where the two disagree the version above is the one to post"对第 3 节和
带实测数字的行不成立：凡是 GPU 盒子有测量的（参数量、逐位表、K2.7 报错原文），以附录为准。

---

## 1. Design options 1 vs 2, and #4486

评论：`pipeline_stage.py` (2026-09-10 08:07 / 08:09) — 两个方案的比较，结论是 PR 走的是 option 1；
并且 "I don't think we'll go with the design in #4486, at least not for MTP + PP support."

--- PASTE BEGIN ---

Yes, this is option 1, and your reading of the trade-off matches ours. Option 2 (each rank forwarding the accumulated stack) is the `attn_res_cache=False` path on this branch: it is one line of routing (`layout.py`, `cache=False`), every hop carries the whole stack, and it is what the matrix in the body compares against. It costs `[PP degree]` times the payload on every hop rather than only on the first, because the stack a rank forwards keeps growing; at pp8 x vp4 on the debug flavor that is the difference between one block and up to eight per hop. We kept it in the PR precisely as the simple fallback, and the two transports are numerically the same up to summation order.

Noted on #4486 -- we will not build on it.

--- PASTE END ---

## 2. The activation save / release policy (the question that needs a worked example)

评论：`model.py:222` (09-10 08:13) — "yes we would save all previous blocks' result for each layer,
despite the redundancy" 对不对；以及 "be very specific about the activation save / recomputation
policy... every block has 4 layers, there are two blocks -- what exactly are you saving in forward,
after each layer, and when exactly would things be released?"

--- PASTE BEGIN ---

Correct, with one refinement: the stack is not re-saved per layer, it is one tensor that grows once per block and is read by every layer.

The worked example, 8 layers, `attn_res_block_size = 4`, so two blocks. `block_residual_TND` is `(T, N, D)` and travels with the hidden states.

- Layer 0 opens block 0: the incoming stream is concatenated onto the stack, so `N` goes 0 -> 1 and the stack holds `[x_0]`. Layers 1, 2, 3 do not grow it; each carries the block's running partial sum as a separate `(T, D)` and reads the same `N = 1` stack.
- Layer 4 opens block 1: the partial sum block 0 committed is concatenated, `N` goes 1 -> 2, stack `[x_0, s_0]`. Layers 5, 6, 7 read `N = 2`.
- The stage that owns `lm_head` runs the final aggregation over the stack plus the last partial sum; that is why `output_res_proj` / `output_res_norm` are pinned to it.

What autograd saves in forward, per layer, is the concatenation `_apply_attention_residual` forms: `(T, N+1, D)`, for the softmax backward. Summed over the model that is quadratic in the *block* count, not the layer count -- for the 93-layer model, `N` at layer `i` is `i // 12`, so the sum is `sum over blocks of (block index + 1) * 12`, about 36 stacks of `(T, D)`, not 93. This is what the report means by wrapping the AttnRes computation in checkpointing: with that region recomputed, only the `N` committed block entries stay live (8 for 93 layers, 2 in the example above) and the `(T, N+1, D)` concatenations are rebuilt in backward. That AC wrap is a follow-up PR, not this one; this PR saves them.

Release, under PP: a block entry is held by the rank that produced or received it, in the rank-local cache, keyed by micro-batch id. It is dropped when the last stage on that rank that reads it has run its backward -- that is the `PPRankLocalCache` release path, and it is why the cache is per rank rather than per stage. With `attn_res_cache=False` nothing is held: the whole stack rides every hop and dies with the hop's autograd graph.

--- PASTE END ---

## 3. The numerics challenge — 作废，见 `REPLY_4312_NUMERICS_2026-09-11.md`

评论：`parallelize.py:267` (09-11 02:12) — "I'm not convinced that the numerics gap can be this
large. Could you give simple examples of how accum order would change with this flag off / on vs.
without PP? How do you prove it's not caused by bugs?"

**这一节我写的版本不要贴。** 它把 cache on / cache off 说成两种不同的累加顺序；GPU 盒子实测
pp2 上两者十步逐位相同（V=1 时 delta 就是整栈，同一 rank 上没有 deposit，算术完全一样），
Tianyu 一条命令就能证伪。

要贴的是 `Raising_PRs/PR_K3_PARALLELISM/REPLY_4312_NUMERICS_2026-09-11.md` 顶部的 PASTE 块，
它比这一节强的地方：

- 把"flag 怎么改累加顺序"直接答成"它不改"，并给出 cache on/off 十步逐位的行；
- 噪声底行 d：dp1 完全不开 PP，只把四个 micro-batch 梯度的累加顺序反过来，就和 pp2 拉开同一
  量级（步 2/3/10 −0.07% / −1.1% / −3.2%，pp2 是 +0.14% / +2.2% / +2.5%），两个 micro-batch 时
  反序逐位相同——这正是他要的"simple example of how accum order would change"；
- 步 1 梯度逐张量剖面（1002 个张量，差异从第 31 层 `wq_b` 的 2/786432 个元素起，沿反向平滑
  增长，台阶在 block 起点而不在 stage 边界）；
- fp32 参数/优化器态下差异只是推迟不是消失；
- 上游自己的 llama3 / deepseek_v3 在原生 `pipeline_llm` 上的 1.7e-5 / 2.1e-4 对照。

我这一节里唯一值得并过去的，是最后那句"跨 pipeline 配置按构造无法逐位"（块残差贯穿全深度，
split 决定求和树）——GPU 盒子那份没有明说，但它的表已经隐含了；要不要加一句由你定。

## 4. Inline comments

| where | comment | reply |
|---|---|---|
| `__init__.py:554` | 33 layers might be too large for a debugmodel, how many params? | See the separate section below -- it is a decision about which cells exist, not just a number. |
| `model.py:228` | `first_layer_in_block` can be saved during init | Done, `b5bb7b315`. |
| `parallelize.py:244` | setting `pipeline_parallel_layers_per_stage=None` is unnecessary; better to error out in `ParallelismConfig.__post_init__` when both are given | **不要按现状回复** —— `b9e7cf8e7` 的 post_init 检查会打断 core 自己注入 split 的写法,见下面第 6 节;先修再回。 |
| `parallelize.py:184` | extend `pipeline_parallel.py#L144` instead of making this model specific | Agreed, and the model-specific split function is gone. #4560 landed that function as `pipeline_with_first_stage_modules` while this was in flight, so the first-stage half is now the core entry point and K3 calls it. What K3 still needs and core does not have is the symmetric case: the AttnRes aggregation (`output_res_proj`, `output_res_norm`) has to ride with `lm_head` on the last stage. Proposal is to add `last_stage_module_fqns` to that same function; the generator change stays a default-preserving addition (checked identical on 1065 stage/layer/weight shapes). |
| `layout.py:220` | `gather_layer_to_stage`: you can get this from the split, why another collective | Removed, `0f189b93c`. The map is read off the split every rank already computes (`layer_to_stage_from_split`); the all-gather is gone. |
| `layout.py:202` | `local_layer_to_stage` not needed either | Removed in the same commit. |
| `utils.py:531` | why the eager init | Withdrawn from this PR -- see below. |
| `pipeline_parallel.py:540` | is `_warmup_pp_edge_communicators` needed by this PR | No. Withdrawn. |
| `parallelize.py:247` | you already cast in `_schedule_stages`, so `stage_class` is not needed? | The cast only narrows the type for the checker; the stages have to *be* `AttnResPipelineStage` because the class overrides the forward and backward that route the block stack and holds the per-stage routing tables. `pipeline_llm` is what constructs them, so it has to be told the class -- that is the whole core change here, `+4/-1`, default unchanged. |
| `pipeline_parallel.py:46,53,57,60,111`, `parallel_dims.py:566` | env var, docstrings, why create cross-rank communicators, not convinced we need this | All withdrawn from this PR: the two transport commits now live on a separate branch (`k3_pp_transport`) and this PR no longer touches `parallel_dims.py` or `distributed/utils.py`. They came from a two-node GB200 hang @elfiegg hit on the previous tree, where per-micro-batch metadata objects shared the PP communicator with the tensor batches; the new stage class sends no metadata, so the hang is out of scope here and belongs to whatever the runtime decides to do about the STATIC-mode warm-up gap it already has a TODO for. |
| `parallelize.py:269` | can we call it `PPRankLocalCache` | Renamed, `9e6e5b9a8`. |
| `pipeline_stage.py:55` | this is on GPU right? | Yes -- the blocks stay on the device they were produced or received on, no host copy. The docstring now says so, and says when a micro-batch's blocks are released. A pinned-host variant is a follow-up behind a switch, not in this PR. |
| `pipeline_stage.py:176` | these two attributes feel like enough to do smart things | That is the intent: `set_routing(layout, store)` is the entire interface between the model's split and the stage. Everything else the class does is derived from those two -- what a hop carries, what the rank already holds, where a gradient is deposited. |

---

# Appendix: the GPU box's draft of the same replies (PP), written 2026-09-11 before the two lines were merged

Written independently on the GPU box (the reply drafts were committed there but never pushed, which is why they were missing here). Kept verbatim for the measured numbers, branch SHAs and test counts. Precedence: wherever this appendix carries a measurement (parameter counts, the bitwise table, the K2.5 error text, the numerics rows), the appendix is the one to post; the version above is the one to post only where neither has measured anything.

# Replies to Tianyu's 2026-09-11 comments on PR 4312 (review round 3)

The code lives on the fork's review branch `pp_review4` (head in the table below; the PR branch `k3_pp_text` moves when the numerics thread is settled). Stack: `a3be242bf` (round-2 head, the 17 runtime commits) + the round-3 commits; the two transport commits are no longer on it. Paste each block as the reply to its comment; the top-level comment goes on the PR.

## Top-level reply

Round 3 is in on the review head: the split is core's (`pipeline_llm` / `_generate_llm_fqn_per_model_part` take modules pinned to the first and last stage, defaults unchanged for every other model), the layer-to-stage map is read off the split with no collective, `ParallelismConfig` refuses the two split knobs together, the pipelining entry no longer rewrites the config, `RankStore` is `PPRankLocalCache`, the first-layer flag is computed at init, and the pipeline files type-check under the pinned pyrefly. The two transport commits (the communicator warm-up and the neighbour-group port) are out of this PR: the warm-up was a no-op (communicators are created at `init_process_group` and by `ncclCommSplit` at mesh build; the INIT log shows nothing lazy during the schedule), and the neighbour-group transport is a two-node opt-in that this runtime does not need on one node; it sits on `k3_pp_transport` for @elfiegg's two-node evidence. Every cell of the previous head is bitwise on the new one (table in the last reply). Rebased onto the PR's base as before; say if you want it on today's main for CI and I will move it.

## Per comment

| # | id | file:line (at 75045fed5) | change (commit on `pp_review4`) |
| --- | --- | --- | --- |
| 1 | 3984841792 | kimi_k3/__init__.py:554 | none yet; numbers below, the size is the maintainers' call |
| 2 | 3984853855 | kimi_k3/model.py:228 | `b5bb7b315` first-layer flag at init |
| 3 | 3984964516 | kimi_k3/parallelize.py:244 | `b9e7cf8e7` post_init check; no `layers_per_stage=None` rewrite (`2b9b1a788`) |
| 4 | 3984970518 | kimi_k3/parallelize.py:184 | `2b9b1a788` core generator + `pipeline_llm` take pinned modules; K3's split function gone |
| 5, 6 | 3985176358, 3985177373 | kimi_k3/layout.py:220, :202 | `0f189b93c` map read off the split; both functions and the all-gather gone |
| 7, 12, 13, 14, 18 | 3985185896, 3985265415, 3985276311, 3985282356, 3985315090 | distributed/utils.py:531, pipeline_parallel.py:46/:60/:111, parallel_dims.py:566 | transport commit off the PR (branch `k3_pp_transport`) |
| 8 | 3985211975 | pipeline_parallel.py:540 | warm-up commit dropped |
| 9 | 3985229621 | kimi_k3/parallelize.py:247 | kept, explained below |
| 10, 11 | 3985261950, 3985263875 | pipeline_parallel.py:57/:53 | off the PR with the transport |
| 15 | 3985294154 | kimi_k3/parallelize.py:269 | `9e6e5b9a8` `PPRankLocalCache` |
| 16 | 3985307483 | kimi_k3/pipeline_stage.py:176 | acknowledged, no change |
| 17 | 3985311057 | kimi_k3/pipeline_stage.py:55 | `9e6e5b9a8` docstring: device-resident |
| 19 | 3985333653 | kimi_k3/parallelize.py:267 | numerics (#19, id 3985333653): the reply text and the three-control tables are in `REPLY_4312_NUMERICS_2026-09-11.md` (paste block at its top) |
| — | — | — | `c6cea394e` the pipeline files typed against the pinned checker (four errors the round-2 head carried) |

### #1 (3984841792) — debug model size

Parameter counts of the debug model (meta build, width and vocab as in `_debugmodel`): 33 layers = 1.40 B parameters, of which the embedding and the head are 335.5 M (the vocabulary is the real one) and each layer 32.2 M; 17 layers (19 units, which pp 2/4/8 do not divide either) = 0.89 B; 13 layers (15 units) = 0.77 B. 33 was chosen so the unit count reproduces the 93-layer model's uneven split (two full blocks of 12 plus a partial block of 9) and no pipeline shape divides it; 17 keeps the "no shape divides" property but has a single partial block. If 1.4 B is too large for CI, 17 layers is the smallest shape that still exercises an uneven split with a partial block; say the word and I move every number in the PR to it.

### #2 (3984853855) — `first_layer_in_block`

Done: computed once in `__init__` (`b5bb7b315`).

### #3 (3984964516) — `pipeline_parallel_layers_per_stage=None`

Done both ways: `ParallelismConfig.__post_init__` raises when `module_fqns_per_model_part` and `pipeline_parallel_layers_per_stage` are both given (`b9e7cf8e7`, with a config test), and the pipelining entry no longer rewrites the config at all — the split is core's now (#4), so there is nothing to set to None.

### #4 (3984970518) — extend core's generator

Done: `_generate_llm_fqn_per_model_part` and `pipeline_llm` take `first_stage_modules` / `last_stage_modules`, module names pinned next to `tok_embeddings` / after `norm, lm_head`, counted as no layer. With the defaults every existing split is byte-identical (checked on 441 combinations of stages, layers and the two weights against the previous function, and the new `test_pipeline_llm_split.py` pins three shapes). Kimi K3 passes `vision_encoder` (when the model has a tower) and `output_res_proj`, `output_res_norm`; its own split function is gone. The pp8 x vp4 recipe (35 units over 32 stages, a count no `layers_per_stage` yields under the ceiling rule) spells its split with the generator (`2b9b1a788`).

### #5, #6 (3985176358, 3985177373) — no collective for the layer map

Done: `layer_to_stage_from_split` reads the map off the split core applies — the config's FQNs, or the ones generated from the same metadata with the same pinned modules — a pure function of the config every rank computes identically; `gather_layer_to_stage`, `local_layer_to_stage` and the all-gather are gone (`0f189b93c`).

### #7 (3985185896) — the eager init, and @sanketpurandare's question

That code is off this PR (the transport commit now lives on `k3_pp_transport`). To the question: `init_process_group(device_id=...)` makes NCCL create the default communicator at init instead of at the first collective; main's `torchtitan/distributed/utils.py` does not pass `device_id` today, so the default communicator is created lazily at the first collective and the sub-group communicators at mesh build (`ncclCommSplit`), which is what the INIT log of a pp8 run on this branch shows — and nothing lazy during the schedule. The port passed `device_id` only under its opt-in flag; on one node it changes nothing observable.

### #8 (3985211975) — `_warmup_pp_edge_communicators`

Not needed; dropped. The INIT log shows every communicator the schedule uses created before step one (default at init, sub-groups by `ncclCommSplit` at mesh build), so the warm-up's P2P round found them already there.

### #9 (3985229621) — `stage_class`

Kept: `AttnResPipelineStage` overrides `forward_one_chunk`, `backward_one_chunk`, `_retrieve_recv_grads`, `_compute_outputs` and `_compute_input_grads`, so the stage object has to be constructed as that class by `_pipeline_module_split`; the `cast` in `_schedule_stages` is a typing assertion, it does not change the objects' class. If you prefer, the stage class can become a field of the model spec rather than an argument, but some parameter has to reach the constructor.

### #10, #11, #12, #13, #14, #18 — the transport code

Off this PR. The neighbour-group transport is @elfiegg's fix for a two-node hang on the reviewed head; on one node the vanilla P2P path runs every cell here, and the socket-transport simulation on one node (two agents x four GPUs, NCCL P2P and SHM disabled) passed with and without it. It stays on `k3_pp_transport`, stacked on this head, for the two-node run that can show whether it is needed; if it is, it comes as its own PR with that evidence and the docstrings you asked for.

### #15 (3985294154) — `PPRankLocalCache`

Renamed (`9e6e5b9a8`): one instance per rank, shared by every stage the rank runs.

### #16 (3985307483)

Yes — the layout tables and the rank cache are the whole state the stage needs; the methods on the stage only read them.

### #17 (3985311057) — on GPU?

Yes: blocks stay on the device they were produced or received on, no host copy; a micro-batch's blocks are released once the rank's last reading stage has run its backward (now in the class docstring). The host-offload variant is a separate branch.

### #19 (3985333653) — numerics

See the numerics reply (separate).

## Bitwise check of the rebuild (3 steps, seed 42, deterministic, 33-layer debug model, text recipe)

Both heads on the same box and seed, the review head `c6cea394e` against the PR head `75045fed5`; loss / grad norm per step. Pipeline cells run 8 micro-batches, dp1 one, so dp1 and the pipeline rows are not the same accumulation and are not compared with each other here.

| cell | PR head `75045fed5` | review head `c6cea394e` |
| --- | --- | --- |
| dp1 | 12.44637 / 36.0000, 11.59698 / 43.2500, 10.05624 / 22.8750 | bitwise |
| pp2, cached transport | 12.48974 / 17.3750, 10.31703 / 16.2500, 8.34951 / 12.1250 | bitwise |
| pp2, whole-stack transport | same as pp2 cached, bitwise | bitwise |
| pp4 (1F1B) | 12.57297 / 16.7500, 10.45522 / 13.7500, 7.98585 / 10.9375 | bitwise |
| pp4, Interleaved1F1B (2 stages per rank) | 12.48087 / 15.8125, 10.22818 / 11.3750, 8.43380 / 12.7500 | bitwise |

Every round-3 change is a refactor of the split, the routing tables' source and names; nothing in the tensors moved.


## 5. The debug model depth (#1, `__init__.py:554`) — 用户方案：小模型跑 pp2 x vp2，33 层只留给 pp8 x vp4

背景数据（本机复算 + GPU 盒子的 meta build）：

- upstream/main 现在的共享 debug model 是 **24 层**（`full_attention_layers={3,7,11,15,19,23}`，
  块大小 12 → 正好两个整块，没有残块），约 **1.11 B**。
- 本 PR 把它改成 33 层（2 x 12 + 9，对应真实模型的 93 = 7 x 12 + 9），**1.40 B**；
  17 层 0.88 B，13 层 0.75 B。每层 32.2 M，嵌入 + head 335.5 M（词表是真实的 163840）。
- **共享的**：`kimi_k3_debugmodel` 同时被上游自己的 b200 `kimi_k3_mm_fsdp2` 格和新的
  `kimi_k3_mm_tp2` 格用。所以这个 PR 把深度从 24 提到 33，等于把上游已有的格加深 37% ——
  这多半才是他问"是不是太大"的真正原因。
- 硬约束：pp8 x vp4 = 32 个虚拟 stage，split 的单元数不能少于 stage 数；24 层只有 26 个单元，
  本机复算直接 `ValueError: Number of stages (32) cannot be greater than effective layers (26)`。
  **要 pp8 x vp4 就必须 ≥ 30 层。**

各格实际测到什么（本机用 core 的生成器复算）：

| 形状 | 每段层数 | 不均匀 | 块跨越 stage 边界 | V>1（rank cache + deposit） |
|---|---|---|---|---|
| 24 层，pp2 | [12, 12] | 否 | **否**（块与 stage 正好对齐） | 否 |
| 24 层，pp2 x vp2 | [6, 7, 6, 5] | 是 | 是 | **是** |
| 33 层，pp8 x vp4 | 1-2 层/段，末段只有 head | 是 | 是（外加尾部残块） | 是 |

要点：**pp2 在 24 层上是退化格**——两个块正好各落一个 stage，没有任何块跨边界，而且 V=1 时
delta 恒等于整栈，本 PR 的机制基本没跑到。真正覆盖机制的最小格是 **pp2 x vp2**（不均匀 + 块跨界 +
V=2 所以 `PPRankLocalCache` 和 deposit 都走到），两卡即可。pp8 x vp4 是压力格。

--- PASTE BEGIN ---

1.40 B on a meta build: 335.5 M of that is the embedding and the head (the vocabulary is the real 163840) and 32.2 M is one layer. Main's 24-layer debug model is 1.11 B, 17 layers would be 0.88 B, 13 layers 0.75 B.

The part worth deciding is not the number, it is that `kimi_k3_debugmodel` is shared: your b200 `kimi_k3_mm_fsdp2` cell and the TP cell run on it too, so this PR as it stands makes those 37% deeper. That is easy to undo. What the depth buys is one cell: pp8 x vp4 is 32 virtual stages, a split cannot have fewer units than stages, and 24 layers is 26 units -- core refuses it outright.

What each shape actually exercises, from the split core generates:

- 24 layers, pp2: stages hold 12 and 12, so each block sits entirely on one stage, nothing crosses a boundary, and with one stage per rank the delta transport is the whole stack by construction. It is a smoke test, not a test of this PR.
- 24 layers, pp2 x vp2: 6 / 7 / 6 / 5 layers, uneven, a block starts in the middle of a stage, and with two stages per rank the rank-local cache and the gradient deposits are on the path. This is the smallest cell that exercises the mechanism, and it needs 2 GPUs.
- 33 layers, pp8 x vp4: 32 stages, one to two layers each, the last holding only the head, plus the trailing partial block the 93-layer model has. This is the stress cell.

So the proposal, if it suits you: put the shared debug model back to main's 24 layers exactly as it is today -- this PR then does not touch it at all, and your existing cells keep their cost -- and make `kimi_k3_debugmodel_pp2_vp2` on that model the pipeline cell in the features suite. The 33-layer shape then exists only for the 8-GPU pp8 x vp4 cell; it can be a second registry entry used by that recipe alone, or we drop that cell and keep only the 2-GPU one. Our preference is to keep both, because the 32-stage cell is what found the uneven-split and partial-block cases in the first place, but the 2-GPU cell is the one that has to exist. Tell us which of the three you want and we will move every number in the PR onto it.

--- PASTE END ---

注意：这个方案会重新引入"两个 debug 形状"，而 round 1 的 3921851558 正是"one debug model shared
by PP and non-PP"。上面的措辞把它说成"共享模型不动、深的那个只服务一个 CI 格"，并把选择权交给他，
这是能自洽的讲法；如果他坚持只要一个形状，退路是只保留 24 层 + pp2 x vp2（机制仍被覆盖，
只是没有 32 段的压力格）。

## 6. `b9e7cf8e7`（#3 的 fix）核对结论：一半对，会打断 core 自己的 split 注入

**做对的**：Tianyu 要的就是"在 `ParallelismConfig.__post_init__` 里 error out"，提交正是这么做的，
错误信息也说清了两者都描述 split；同时 `2b9b1a788` 把 K3 入口里 `dataclasses.replace(...,
pipeline_parallel_layers_per_stage=None)` 那段删掉了，所以"不再改写用户配置"这半句成立。

**问题**：`dataclasses.replace()` 会重新走 `__init__`，因而**重新触发 `__post_init__`**（本机验证过）。
而 core 注入 split 用的就是这个写法：

```python
if parallelism.module_fqns_per_model_part is None:
    ... fqn_per_part = _generate_llm_fqn_per_model_part(num_virtual_stages, ...)
    parallelism = dataclasses.replace(parallelism, module_fqns_per_model_part=fqn_per_part)
```

- 分支现在的基（`6e2ac3dcd`）上，这段在 `pipeline_vlm`（`pipeline_parallel.py:196`）和
  `muse_glimmer/parallelize.py:201`。
- **rebase 到今天的 main 之后更广**：#4560 把它统一成了 `pipeline_with_first_stage_modules`，
  `kimi_k2_7`、`muse_glimmer`、`qwen3_5`、`qwen3_6`、`qwen3_8` **五个模型**都走它。

于是：**任何一个这五个模型的 pipeline 运行，只要用户设了
`--parallelism.pipeline_parallel_layers_per_stage=N`，就会在 replace 那行抛
`ValueError: ... both describe the pipeline split`。** 那个 N 恰恰是 `_get_pipeline_metadata`
用来算 `num_virtual_stages` 的输入，也就是说这条路径本来完全合法。

**CI 抓不到**：全树只有两个 recipe 设 `layers_per_stage`
（`llama3_debugmodel_fsdp2_pp2_1f1b_layers_per_stage`、
`llama3_debugmodel_pp4_interleaved_1f1b_layers_per_stage`），llama3 直接走 `pipeline_llm`，
不经过 replace；`tests/unit_tests/cpu/test_pipeline_parallel.py` 里也没有把
`pipeline_with_first_stage_modules` 和 `layers_per_stage` 组合起来的用例。新加的
`test_pipeline_split_is_given_one_way` 只测直接构造。所以这是**静默回归**：CI 全绿，用户报错。

**建议的改法**（保留 Tianyu 要的 post_init，不动语义）：注入 split 的同时把已经被消费掉的旋钮清掉，
每个站点一行——

```python
parallelism = dataclasses.replace(
    parallelism,
    module_fqns_per_model_part=fqn_per_part,
    pipeline_parallel_layers_per_stage=None,
)
```

这是对的：FQN 一给出，`pipeline_llm` 本来就不再看 `layers_per_stage`（`_get_pipeline_metadata`
算出的 `num_virtual_stages` 在这条分支上没人用）。再补一个覆盖
`pipeline_with_first_stage_modules` + `layers_per_stage` 的单测。

备选：把检查从 `__post_init__` 挪到 `ConfigManager` 的用户输入校验（概念上更干净——这本来就是
"用户不能同时给两个"），但不是他点名的位置。

回复里可以顺手把这个交互告诉他（是我们自己发现并修的，不是他发现的），措辞例如：

--- PASTE BEGIN ---

Added in `__post_init__` as you suggested, with one follow-on: `dataclasses.replace` re-runs `__post_init__`, and that is how core injects a generated split (`pipeline_with_first_stage_modules`, which `kimi_k2_7`, `muse_glimmer` and the three Qwen models route through). So the check as first written made any of those models raise when the user set `pipeline_parallel_layers_per_stage` -- a legal configuration, since that knob is what `_get_pipeline_metadata` turns into the stage count. The injection sites now clear the knob in the same `replace`, which is what they mean anyway (once the FQNs exist, `pipeline_llm` no longer reads it), and there is a test for that path. Nothing in CI covered it: the only two recipes that set `layers_per_stage` are llama3's, which go through `pipeline_llm` directly.

--- PASTE END ---
