# Replies for PR 4499, Shuhua's 2026-09-14 round (drafts, post as the user)

State: review branch `tpsp_review4` rebased onto upstream main `d34a13fdf` (the PR was `dirty`; the only conflict was `kimi_k3/sharding.py` against the padding-mask sharding of #4594, resolved by calling `set_moe_block_padding_mask_sharding` on K3's MoE layers). Round 3 commits on top of the four rebased ones: `0495e766c` (unified b200 cell), `b82579344` (K2.7 comment), `ee5b16b0a` (names and docstrings), `1dec3ee17` (routed_down), `2b7086980` (pushed from the other box 2026-09-15: the b200 recipe states `enable_sequence_parallel = True`; SP is the default, so no numbers move), `297993190` (other box: drops the routed_down comment), `48632ad41` (the routed path stays token-sharded through `routed_norm` and `routed_up` under EP), `c0dad3e91` (`_tp_unsharded_weight_config` takes `token_sharded`; the 3016 sharding configs of the four sp/ep combinations are identical before and after, so the H100 run on `48632ad41` stands for it). Review head is `c0dad3e91`. Not pushed to `k3_tp_sp` yet: needs the user's approval. GPU numbers: read 2026-09-15 from `logs_tpsp_r3_2026-09-15/results.txt` (script alongside). tps is not quoted: the before cell's cache was warmed by one step only, so its step 2/3 throughput is not comparable.


Body is stale after the rebase (2026-09-15, 1 x RTX 5060 Ti smoke, `logs_tpsp_r3_2026-09-15/rebase_reference_results.txt`): tp=1 on the old base `56a721b64` reads step-1 loss `12.50616`, on the new main `d34a13fdf` `12.60343` (#4535 made the fused `w13` the default for K3's dense FFN, with its own init), so every body row measured against the old main no longer holds. The PR head at tp=1 matches the new main on all 3 steps. Before this reply goes out: rerun the body's tables on H100 against `d34a13fdf` (tp=1, tp2 SP on/off, dp2 rows, dp2 x ep2 x tp2, K2.5, type checking, plus the new `kimi_k3_mm` cell), and update the Design sentence on `routed_down`, the CI/CD section (`kimi_k3_mm`, 4 GPUs) and drop the changed-files block. 5060 numbers are smoke only and never go into the body.

## Top-level

--- PASTE BEGIN ---

Thanks, all addressed and rebased onto current main (the only conflict was the MoE padding-mask sharding from #4594, now called on K3's MoE layers too).

- Under EP the routed path now follows the router end to end: `routed_down` takes the rank's token shard (without SP it drops the other TP ranks' tokens on entry instead of projecting all of them), the experts' output stays token-sharded, and `routed_norm` and `routed_up` run on the rank's tokens; their weights are Replicate so the gradients are reduced over TP. Without SP, `routed_up` re-enters Partial and the MoE exit reduces once with the shared experts, so the latent all-reduce before `routed_norm` and the repeated norm and up on every TP rank are gone. Without EP nothing changes.
- The FSDP and TP cells are one `kimi_k3_debugmodel_mm` cell: fsdp 2, tp 2, ep 2 on 4 GPUs, SPMD type checking on.
- Names, docstrings, the forward-order move and the K2.7 comment as suggested.

H100_ROUTED_TABLE_PLACEHOLDER (4 x H100, before `ee5b16b0a` vs after `48632ad41`, the b200 cell with SP on and off, shared warm cache, plus a noise-floor row; the 5060 smoke table is not posted)

--- PASTE END ---

## Inline

| where | comment | reply |
| --- | --- | --- |
| `torchtitan_recipes/tests/b200.py:23` | merge with `kimi_k3_debugmodel_mm_fsdp2` into `kimi_k3_debugmodel_mm` with fsdp=2, tp=2, ep=2 | Done: one `kimi_k3_debugmodel_mm` cell, fsdp 2 x tp 2 x ep 2 on 4 GPUs, type checking on; the two older cells are gone. |
| `sharding.py:66` | nit: remove | Removed. |
| `sharding.py:80` | move these two lines after the layerwise sharding | Moved after the layer loop. |
| `sharding.py:276` | rename to `_set_multimodal_decoder_boundary_sharding` | Renamed. |
| `sharding.py:280` | docstring suggestion | Applied. |
| `kimi_k2_7/vision_encoder.py:121` | comment suggestion | Applied. |
| `tests/integration_tests/b200.py:21` | update once there is a unified test | Updated: `kimi_k3_mm`, 4 GPUs. |
| `test_integration_test_definitions.py:105` | same | Updated. |
| `sharding.py:257` | rename to `_tp_unsharded_weight_config` | Renamed. Its argument is now `token_sharded`: the routed path under EP is token-sharded without SP too, so SP alone no longer decides it. |
| `sharding.py:258` | docstring suggestion | Applied, with the condition stated as the code has it now: "R when tokens are TP-sharded, for gradient reduction; otherwise I", since `routed_up` under EP without SP also takes R. |
| `sharding.py:138` | nit: remove | Removed. |
| `sharding.py:229` | routed_down: same as the router; without SP, discard unused local tokens and keep tokens sharded on TP | Done, for the whole routed path: under EP `routed_down` takes the rank's token shard (Replicate to Shard(0) on entry without SP), the experts' output stays token-sharded, and `routed_norm` and `routed_up` run on the rank's tokens with Replicate weights, so the gradients are reduced over TP. Without SP, `routed_up` re-enters Partial for the single reduction at the MoE exit, which also removes the latent all-reduce that used to sit before `routed_norm`. Without EP it stays as the router is, on all tokens. |
