# PR 4312：回复 tianyu 的 r4106905048（2026-09-25 17:15，`torchtitan/training_engine.py:143`）

原话："can be raised in post init of ParallelismConfig"

## 状态（不粘贴）

- **改动：** `pp_review5` = `8979c28ba`（已推到 fork），在 `714267617` 之上一个 commit："config: a pipeline split and layers_per_stage are refused together in ParallelismConfig"。
  - 检查从 `TrainingEngine.Config.__post_init__` 挪到 `ParallelismConfig.__post_init__`。
  - `pipeline_with_first_last_stage_modules` 和 Kimi K3 的 `pipeline_kimi_k3` 在 copy 上写入推导出的 split 时，把 `pipeline_parallel_layers_per_stage` 清成 None。
- **为什么非清不可：** `dataclasses.replace` 会重跑 `__post_init__`。不清的话，给了 knob 的 qwen3_5/6/8、kimi_k2_7、muse_glimmer 和 K3 都会报错，就是刚修掉的那个回归。
  - 这正是 tianyu 在 r4056259393 标过的那一行清空。区别在于现在检查放在他要的位置上，被清掉的只是内部 copy 上已经用过的 knob：split 就是按它切出来的，用户原来的配置对象不受影响。所以回复里要把这一点说明白。
- **验证（09-25）：**
  - body 里那条 CPU 测试命令：109 passed，1 skipped（本地缺 `fla`）；`test_pipeline_parallel.py`：23 passed。
  - flake8、µfmt 通过。pyrefly 对改动的文件没有报错；它顺带改了 24 个其他文件，还删了 `pipeline_parallel.py` 里一行上游原有的 suppression，都已回退或补回。
  - 组合格 `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4` 在 8 × 5060 上跑 10 步，rc=0。loss 依次为 8.00045 / 7.28220 / 5.35247 / 4.99816 / 4.61077 / 4.06932 / 3.95011 / 3.76261 / 4.11102 / 3.51385。这个格子用的是显式 split，不经过 knob 路径。
  - K3 的 knob 路径用 CPU 探针验证（`k3_knob_probe.py`，17 层，pp2，Interleaved1F1B，knob=5）：
    - 改动前后都得到 4 个 stage，split 相同；
    - copy 上的 knob 改动前是 5，改动后是 None；
    - 用户配置里始终是 5。
- **发帖前提：** `k3_pp_text` 同步到 `pp_review5` 之后才能发，由用户决定。

--- r4106905048 (tianyu, raise in ParallelismConfig post_init): PASTE BEGIN ---

Moved: `ParallelismConfig.__post_init__` now refuses a split together with `pipeline_parallel_layers_per_stage`, and `TrainingEngine.Config` no longer checks it. Since `dataclasses.replace` re-runs that check, the two places that write a split derived from the knob into a copy, `pipeline_with_first_last_stage_modules` and Kimi K3's entry, now clear the knob on the copy. The split on the copy is the one the knob sized and the caller's config keeps the knob, so nothing is lost; `test_layers_per_stage_sizes_the_derived_split` checks both.

--- PASTE END ---
