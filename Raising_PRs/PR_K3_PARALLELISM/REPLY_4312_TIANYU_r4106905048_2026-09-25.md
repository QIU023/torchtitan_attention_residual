# PR 4312：回复 tianyu 的 r4106905048（2026-09-25 17:15，`torchtitan/training_engine.py:143`）

原话："can be raised in post init of ParallelismConfig"

## 状态（不粘贴）

- **改动：** `pp_review5` = `7814d1f8b`（已推到 fork，`k3_pp_text` 仍是 `714267617`），在 `714267617` 之上一个 commit："config: a pipeline split and layers_per_stage are refused together in ParallelismConfig"。
  - 检查从 `TrainingEngine.Config.__post_init__` 挪到 `ParallelismConfig.__post_init__`。
  - `pipeline_with_first_last_stage_modules` 和 Kimi K3 的 `pipeline_kimi_k3` 在生成带推导 split 的 copy 时，改用 `copy.copy` 再给 split 字段赋值，不再用 `dataclasses.replace`。
- **为什么不用 replace，也不清空 knob：**
  - `replace` 会重跑 `__post_init__`，碰到 copy 上"推导出的 split 加上 knob"就会报错。
  - 前一版 `8979c28ba` 的做法是在 copy 上把 knob 清成 None，这又是 r4056259393 里被标为覆盖用户配置的那种做法。用户指出这会绕回原点，于是改了。
  - `copy.copy` 不调用 `__init__`，copy 保留 knob，和 main 的 helper 一样；用户原来的配置对象也不受影响。
  - `ParallelismConfig` 是 `slots=True`，Python 3.12 下 `copy.copy` 能正确复制，实测其余字段都相同。
- **验证（09-25）：**
  - 单测：body 里那条 CPU 命令 109 passed，1 skipped（本地缺 `fla`）。
  - flake8、µfmt 通过。pyrefly 在临时 worktree 里跑，对改动的文件没有报错；它删掉的那一行上游 suppression 不是我们的改动，提交里保留着。
  - K3 的 knob 路径（`k3_knob_probe_2026-09-25.py`，17 层，pp2，knob=5）：4 个 stage，copy 上的 knob 是 5，用户配置也是 5。
  - 组合格 `kimi_k3_debugmodel_fsdp2_tp2_ep2_pp2_vpp4` 在 8 × 5060 上跑 10 步，rc=0，loss 8.13095 → 3.53782。
    - 这个格子没有固定 seed，各次 smoke 的第 1 步 loss 本来就不同（8.00045、8.10676、8.13095），只能看能否跑通，不能比数值。
    - 它用的是显式 split，不经过 knob 路径。
- **发帖前提：** `k3_pp_text` 同步到 `pp_review5` 之后才能发，由用户决定。

--- r4106905048 (tianyu, raise in ParallelismConfig post_init): PASTE BEGIN ---

Moved: `ParallelismConfig.__post_init__` now refuses a split together with `pipeline_parallel_layers_per_stage`, and `TrainingEngine.Config` no longer checks it. The two places that write a split derived from the knob into a copy, `pipeline_with_first_last_stage_modules` and Kimi K3's entry, now make the copy with `copy.copy` instead of `dataclasses.replace`, which would re-run the check. The copy keeps the knob that sized its split, as on main, and the caller's config is untouched.

--- PASTE END ---
