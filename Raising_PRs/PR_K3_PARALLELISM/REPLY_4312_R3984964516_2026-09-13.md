# Reply to Tianyu on PR 4312, thread 3984964516 (`parallelize.py`, "unnecessary to set this to None ... error out in post_init")

The earlier reply in this thread (3995773662, posted 2026-09-12) says the `__post_init__` check was added and that the split is core's; both are out of date. Post this as a new reply in the thread rather than editing 3995773662, so the history stays readable; optionally prepend "(Superseded, see below.)" to 3995773662. It is now taken out again, together with the one line it forced into `pipeline_parallel.py`, so that `pipeline_parallel.py` carries no change. `k3_pp_text` = `ae2e7b9c4` since 2026-09-13, so it can be posted.

--- PASTE BEGIN ---

Update, superseding my reply above: I dropped the `__post_init__` check from this PR again, and the split is no longer core's but Kimi K3's own (see the thread on `pipeline_parallel.py`). On main, `pipeline_with_first_stage_modules` writes the split into the config while leaving `pipeline_parallel_layers_per_stage` set, so the check would reject a valid config for every model that goes through it unless `pipeline_parallel.py` changes too. Kimi K3 now also leaves the knob alone, as you suggested, since `pipeline_llm` ignores it once the split is given. If the check is still wanted, it can come as a separate PR that changes both places together.

--- PASTE END ---
