# Reply to Tianyu on PR 4312, thread 3984964516 ("error out in post_init of `ParallelismConfig` when both are given")

Post as a new reply in https://github.com/pytorch/torchtitan/pull/4312#discussion_r3984964516, under the earlier reply 3995773662. `k3_pp_text` = `6e1f5e41c` since 2026-09-13.

--- PASTE BEGIN ---

Done in [6e1f5e41c](https://github.com/QIU023/torchtitan/commit/6e1f5e41c3a78b0be9115aa44403e090d2f899c3). The explicit `pipeline_parallel_layers_per_stage=None` is still needed, in the K3 entry and in `pipeline_with_first_stage_modules`: `dataclasses.replace` re-runs `__post_init__`, so the new check would reject the split while the knob is still set.

--- PASTE END ---
