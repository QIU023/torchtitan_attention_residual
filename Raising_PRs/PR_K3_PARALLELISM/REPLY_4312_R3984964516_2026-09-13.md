# PR 4312, thread 3984964516: no new reply needed

The posted reply 3995773662 (2026-09-12) is accurate again about the check: it sits in `ParallelismConfig.__post_init__` and `pipeline_with_first_stage_modules` clears the knob in the same `replace` (`6e1f5e41c`). Its other clause, "the split is core's now", is out of date; the r3987651867 reply says the split is Kimi K3's own, which covers it.
