# Reply to Tianyu on PR 4312, thread 3984964516 (`parallelize.py`, "unnecessary to set this to None ... error out in post_init")

The earlier reply in this thread (3995773662) said the `__post_init__` check was added. It is now taken out again, together with the one line it forced into `pipeline_parallel.py`, so that `pipeline_parallel.py` carries no change. Post after `pp_review4` is synced onto `k3_pp_text`.

--- PASTE BEGIN ---

Update: I dropped the `__post_init__` check from this PR again. On main, `pipeline_with_first_stage_modules` writes the split into the config while leaving `pipeline_parallel_layers_per_stage` set, so the check would reject a valid config for every model that goes through it unless `pipeline_parallel.py` changes too. Kimi K3 now also leaves the knob alone, as you suggested, since `pipeline_llm` ignores it once the split is given. If the check is still wanted, it can come as a separate PR that changes both places together.

--- PASTE END ---
