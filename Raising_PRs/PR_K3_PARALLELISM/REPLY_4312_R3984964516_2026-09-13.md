# Reply to Tianyu on PR 4312, thread 3984964516 (`parallelize.py`, "unnecessary to set this to None ... error out in post_init")

Post as a new reply in the thread, under the earlier reply 3995773662 (posted 2026-09-12), which says the check sits in `ParallelismConfig.__post_init__` and that the split is core's; both have changed. The check is kept, moved to `Trainer.Config.__post_init__` in `b1f82ba4a` on `pp_review4`; `k3_pp_text` is still `ae2e7b9c4`, so sync first (approval), otherwise the link points at a commit the PR does not carry yet.

--- PASTE BEGIN ---

Update on my reply above: the check is still there, but it moved from `ParallelismConfig.__post_init__` to `Trainer.Config.__post_init__`, next to the `num_pp_microbatches` check ([commit](https://github.com/QIU023/torchtitan/commit/b1f82ba4adffc09e8670e5b1a2f4f589d346eb49)). In `ParallelismConfig` it re-runs on every `dataclasses.replace`, and on main `pipeline_with_first_stage_modules` writes its split while leaving `pipeline_parallel_layers_per_stage` set, so it rejected valid configs for every model on that entry unless `pipeline_parallel.py` changed as well. In the trainer config it runs once on what the user gave, and `pipeline_parallel.py` stays untouched. Kimi K3 also no longer clears the knob, as you suggested, and the split is now Kimi K3's own rather than core's ([thread](https://github.com/pytorch/torchtitan/pull/4312#discussion_r3987651867)).

--- PASTE END ---
