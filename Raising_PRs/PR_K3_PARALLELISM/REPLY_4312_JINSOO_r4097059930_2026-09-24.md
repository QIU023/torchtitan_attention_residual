# PR 4312: reply to jinsooihm's r4097059930 (2026-09-24 18:27, `distributed/pipeline_parallel.py:163`)

The question: "What is the reason for rejecting `pipeline_parallel_layers_per_stage` already being set? It seems `_get_pipeline_metadata` can handle the option of `if layers_per_stage is not None`."

The fix is `pp_review4` `901ef34de`, "pipeline_parallel: a configured layers_per_stage sizes the derived split", pushed to the fork's review branch only. Post the reply after the user syncs `pp_review4` to `k3_pp_text`, since it says the refusal is gone. No commit hash in the text, as in round 5.

History behind the answer (not for pasting):

- 09-11, tianyu r3984964516: error out in `ParallelismConfig.__post_init__` when both the split and `layers_per_stage` are given. Added; since `dataclasses.replace` re-runs `__post_init__`, the helper then had to clear `layers_per_stage` on the copy it handed to `pipeline_llm`.
- 09-13 evening: the `__post_init__` check was removed (`ae2e7b9c4`); the clearing stayed.
- 09-20, tianyu r4056259393: "should assert `pipeline_parallel_layers_per_stage` being None at the beginning of this function? o/w overriding user config". Answered 09-21 by refusing the knob instead of clearing it.
- Main's `pipeline_with_first_stage_modules` honors the knob: the same CPU call (a model pinning `vision_encoder`, pp2, Interleaved1F1B, `layers_per_stage=2`, 6 layers) gives 4 stages on main `9e159aed7` with the knob kept on the copy, raises `ValueError` on `dbd13d397`, and gives main's split on `901ef34de`. Only llama3 recipes set the knob, so CI never reached the refusal.

--- r4097059930 (jinsooihm, why refuse pipeline_parallel_layers_per_stage): PASTE BEGIN ---

> It seems `_get_pipeline_metadata` can handle the option of `if layers_per_stage is not None`.

Right, so the refusal is removed. It dated from an earlier revision that cleared the knob on the copy handed to `pipeline_llm`. That clearing served a `ParallelismConfig.__post_init__` check that no longer exists, and tianyu flagged it as overriding the user config.

The derived split now follows the knob and the copy keeps it, as with main's `pipeline_with_first_stage_modules`, so `qwen3_5`, `kimi_k2_7` and `muse_glimmer` take it under PP again. A configured split is still refused, since the function would replace it. A unit test covers a split sized by the knob.

--- PASTE END ---
