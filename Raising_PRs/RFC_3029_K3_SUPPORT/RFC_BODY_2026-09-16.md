# RFC 3029 body, rewritten 2026-09-16

Replaces the whole body of pytorch/torchtitan#3029. Format follows Megatron-Bridge's K3 roadmap (NVIDIA-NeMo/Megatron-Bridge#4910): summary, status legend, status table, architecture facts, what exists to reuse, deliverables with checkboxes, parallelism matrix, open questions, acceptance criteria, references. Scope split: items tracked in #4272 get one status line each and a link; the RFC's own scope (everything #4272 does not list: MoonViT parallelism, the report's memory items, post-training, checkpoint interoperability, tooling) gets the deliverable checklists.

Facts checked 2026-09-16 against upstream main `810e62786`, the fork, and the logbook; every number is one already recorded there. Audit of the rows against the evening's state: `phase13_k3like_48b_posttrain/RFC3029_SCOPE_STATUS_2026-09-16.md`, last section (two rows edited there: MoonEP now #4751, the 5D row names the current tree). Not for pasting: this header. Open decisions for the user are listed in the chat, not here.

--- PASTE BEGIN ---

# [RFC] Kimi K3 on torchtitan: pre-training and post-training support

## Summary

Track end-to-end Kimi K3 support in torchtitan: the model and its parallelisms on main, the training-side infrastructure the Kimi K3 technical report describes (cache-based pipeline communication for Attention Residuals, recompute of the AttnRes math, dynamic context parallelism and pipeline stages for the vision tower, balanced expert parallelism, MXFP4 / MXFP8 quantization-aware training), and post-training (SFT, LoRA / QLoRA, GRPO) on the same model code.

Scope and ownership:

- #4272 is the tracking list for the production path and holds the P0 / P0.5 / P1 checkboxes; this RFC does not duplicate them.
- This RFC records status per component with links, and carries the deliverables for the scope #4272 does not list.
- Every row names its PR; the PR carries its own review thread.

## Where things stand (2026-09-16)

### Tracked in #4272

| Component | Status | Where | Notes |
|---|---|---|---|
| Eager reference model, FSDP2 / HSDP, MoonViT-V2 tower | landed | #4025 | The model code, the FSDP2 path and the Hugging Face parity script. |
| KDA operators and kernels | landed | #4164, #4351, #4389 | Attention Gym kernels, fused on SM100 / SM103 and portable elsewhere. |
| Sample packing in KDA | landed | #4347 | KDA state reset at document boundaries. |
| spmd_types backend, multimodal type checking | landed | #4446, #4527 | The declaration backend every later parallelism is written on. |
| Expert parallelism, grouped experts | landed | #4314 | Grouped experts on core's dispatcher seam. |
| Tensor parallelism with sequence parallel | landed | #4499 | Head-parallel KDA and MLA, core's expert sharding for the latent MoE, Kimi K2.5's plan for the tower. |
| Quantile balancing | landed | #4577 | Supersedes #4412. |
| Numerical validation | in review | #4731 | Moonshot BF16 parity, a KDA FP64 oracle and a B200 baseline. |
| Pipeline parallelism with Attention Residuals (1F1B and interleaved VPP) | in review | #4312 | A `PipelineStage` subclass that carries the block stack's delta between stages and routes its gradients back. |
| Context parallelism (KDA and MLA) | in review | #4639 | Supersedes #4500 and #4313; #4313's packed Ulysses for MLA is a follow-up on its interface. |
| Per-head Muon | in review | #4596, #4693 | DistMuon with per-head layouts for both attention variants. |
| MXFP4-weight / MXFP8-activation QAT, quantized checkpoint interoperability | in review | #4269, and any torchtitan-wide MXFP4 / MXFP8 support PRs | The issue that tracks the QAT path and the quantized checkpoint interoperability across the open PRs. |
| 2.78T configuration, released MXFP4 checkpoint load | planned | #4272 P0.5; #4269 and any torchtitan-wide MXFP4 / MXFP8 support PRs | The released configuration and its quantized weights. |
| MoonEP as an optional EP backend | draft | #4751 | A dispatcher and an expert module on core's seams, moving expert rows with MoonEP's own prefetch and grad-reduce kernels; the transport runs on Hopper with NVSwitch. |
| Full-scale 2.78T run | planned | #4272 P1 | Maintainers' hardware. |

### Beyond #4272

| Component | Status | Where | Notes |
|---|---|---|---|
| Vision tower: dynamic context parallelism (report sec 5.2.3) | draft | #4380, stacked on #4639 | Large images split over a sub-CP group, small ones replicated. |
| Vision tower: its own pipeline stage (DEP, report sec 5.2) | draft | #4381, stacked on #4312 | The vision tower as a pipeline stage of its own. |
| AttnRes math recomputed in backward (report sec 5.2.2) | draft | #4656 | The residual math under a `torch_remat` checkpoint when no activation-checkpointing policy covers the block; values unchanged, and the saving grows with tokens and depth. |
| PP rank store offload, PP activation balance (report sec 5.2) | draft | drafts stacked on #4312 | The rank store on pinned host memory; saved activations parked on a peer through the Mooncake Transfer Engine. |
| `torch.compile` of the decoder blocks | draft | not filed | Core's `apply_compile` on the decoder blocks. |
| Released checkpoint format | draft | not filed | The released log-decay layout, packed weights refused as plain tensors, the quantized reader. |
| Multi-token prediction layers | draft | not filed; on torchtitan's general MTP support (#4451, #4486, #4488) | K3's MTP layers on the shared MTP module; the released config ships them disabled. |
| LoRA export and QLoRA (NF4 and packed MXFP4 bases) | draft | #4576 | Adapter export and 4-bit frozen bases on core's LoRA transform. |
| SFT and GRPO with veRL's torchtitan engine | outside the tree | veRL side, not filed | The torchtitan engine training K3 through veRL. |
| K3 in torchtitan's own RL stack | in review | #4680 | A batch-invariant KDA path for rollout and training parity. |
| Grad-norm dtype under PP / EP | in review | pytorch/pytorch#194033, then #4135 | `get_total_norm(dtype=)`, so the total norm does not depend on how the model is cut. |
| Full 5D preview tree | draft | #4281 | The pre-rebase tree; the current full tree is [`k3_on_4025`](https://github.com/QIU023/torchtitan/tree/k3_on_4025), which tracks main. |


## The released model (per the model card and `torchtitan/models/kimi_k3/README.md`)

2.8T total, 104B activated; 93 decoder layers (1 dense), hidden size 7168, 96 attention heads; 69 KDA layers and 24 Gated MLA layers; Attention Residuals in blocks of 12 layers; Stable LatentMoE with latent dimension 3584, 896 experts, top-16, 2 shared experts, SiTU-GLU; 160K vocabulary; MoonViT-V2 vision encoder (401M); 1M-token context; MXFP4 weights and MXFP8 activations with quantization-aware training. The debug flavour keeps the layer pattern at 24 layers.

## Building blocks on main

- Model code and its seams are on main: `models/kimi_k3/` (model, `kda.py`, `moe.py`, `sharding.py`, `parallelize.py`, `state_dict_adapter.py`, `vision_encoder.py`), core's `GroupedExperts`, `TokenChoiceTopKRouter`, `set_moe_sharding_config`, `pipeline_llm`, `RegionAC` / `torch_remat`, the LoRA and quantization config transforms.
- Sibling models set the pattern each K3 piece follows: Kimi K2.5 for the MoonViT tower (its TP plan is now shared), Qwen3.5 for the hybrid linear-attention decoder, DeepSeek V3 for MLA and MoE.
- Every new piece extends one of these; a private helper, hook or side structure is a review finding, not a design.

## Work items beyond #4272

### Report infrastructure

- [ ] AttnRes recompute in backward: #4656
- [ ] Vision tower dynamic CP: #4380 restacked on #4639 when it lands
- [ ] Vision tower as a pipeline stage: #4381 restacked when #4312 lands
- [ ] PP activation offload and balance: filed once #4312's transport is decided
- [ ] The "block representation generated once at the boundary layer" sentence of sec 5.2.2 (today every block boundary re-concatenates the stack): design decision, touches #4312's stage assembly and CP's stack sharding

### Checkpoints

- [ ] Released layout round trip (HF to DCP to HF) on the debug downscale and on the released index
- [ ] Quantized weights (packed MXFP4, MXFP8 scales): #4269 and any torchtitan-wide MXFP4 / MXFP8 support PRs
- [ ] LoRA adapter export in the released key spelling: #4576

### Post-training

- [ ] SFT and GRPO recipes on the veRL torchtitan engine
- [ ] LoRA and QLoRA (NF4, packed MXFP4) training with export: #4576
- [ ] QAT: #4269 and any torchtitan-wide MXFP4 / MXFP8 support PRs
- [ ] Alignment with torchtitan RL (#4680): one model code path for both stacks
- [ ] Inference-side parity of Block AttnRes: log-prob parity of the serving path against the training model

### Tooling and numerics

- [ ] `get_total_norm(dtype=)`: pytorch/pytorch#194033, then #4135
- [ ] Compile: locate the compiled-vs-eager gap below module level before filing

### Docs

- [ ] `models/kimi_k3/README.md` lists the parallelisms main supports (today only FSDP2 / HSDP; EP and TP / SP are merged)
- [ ] Hardware notes: which KDA kernel path runs on which architecture

## Parallelisms

| Parallelism | Status | Where |
|---|---|---|
| FSDP2 / HSDP | landed | #4025; B200 CI |
| Tensor parallelism, sequence parallel on and off | landed | #4499; B200 CI cell `kimi_k3_mm` |
| Expert parallelism | landed | #4314; B200 CI cell `kimi_k3_mm` |
| Pipeline parallelism, VPP (1F1B, Interleaved1F1B) | in review | #4312 |
| Context parallelism (KDA, MLA all-gather KV, Ulysses) | in review | #4639, #4313 |
| Vision tower dynamic context parallelism | draft | #4380 |
| Vision tower pipeline stage (DEP) | draft | #4381 |
| All five together on the 2.78T configuration | planned | #4272 P1; the only full tree so far is the preview #4281, refreshed from [`k3_on_4025`](https://github.com/QIU023/torchtitan/tree/k3_on_4025) |

## Questions

1. PP: does the cross-stage protocol stay a `PipelineStage` subclass in the model folder (#4312 today) or move toward `torch.distributed.pipelining`? A library-side version needs dense receive buffers and a per-micro-batch key.
2. Post-training venue: torchtitan RL (#4680) and the veRL engine both train K3; which one the recipes target, and where LoRA / QLoRA export lives (#4576 on the core LoRA transform).

## Links

- Kimi K3 technical report and model card: [report](https://github.com/MoonshotAI/Kimi-K3/blob/main/k3_tech_report.pdf), [moonshotai/Kimi-K3](https://huggingface.co/moonshotai/Kimi-K3)
- Tracking list: #4272; baseline: #4025
- The 2026-08 version of this RFC, superseded by the tables above: [RFC_OLD_2026-08.md](https://github.com/QIU023/torchtitan_attention_residual/blob/main/Raising_PRs/RFC_3029_K3_SUPPORT/RFC_OLD_2026-08.md)

--- PASTE END ---
