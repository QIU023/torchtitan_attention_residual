# veRL fork: what the K3 work changed, and how it splits into upstream PRs (2026-09-16)

Branch `kimi_k3_integration_rebased` = `a3661ae3`, 46 commits on upstream verl `00cd5b44` (2026-09-15). Upstream `verl-project/verl` main is `67858929` (2026-09-16), four commits ahead, two of them touching the engine file (#7873 gc during refit, #7874 detach model_output); the rebase is small. Upstream's torchtitan engine (`verl/workers/engine/torchtitan/transformer_impl.py`, 884 lines) has no LoRA, no EP, no CP, no Kimi, and refuses pipeline parallelism.

## Inventory: 16 files, +1819 / -65

| group | files | lines (approx.) | goes upstream |
| --- | --- | ---: | --- |
| K3-specific | `engine/torchtitan/utils.py` (model_type map `kimi_linear` / `kimi_k3` to the package, the `experiments/` import fallback, `VERL_TORCHTITAN_FLAVOR`, the no-op dataloader's `dataset` field); `transformer_impl.py` three flags keyed on `torchtitan_name == "kimi_k3"` (contiguous rank-ordered CP shards for the head-tail balancer, module-internal CP, folded `[T]` token stream), the packed document offsets and the model's own mask builder, the token-dispatcher note; `utils/tokenizer/tokenizer.py` (`KimiK3Processor` case, no rope index); `utils/dataset/multiturn_sft_dataset.py` (`patch_size` read tolerant of `media_proc_cfg`) | about 100 | yes, last, after the engine PRs; smaller still once the three flags become capability probes |
| generic engine capability | `transformer_impl.py`: pipeline parallelism (`_PipelineLossBridge`, `_pp_forward_backward_batch`, token-budget padding, one verl micro-batch per stage, every rank ships every stage's tensors in the sync, the FSDP2 grad-upcast guard), tensor parallel on the packed stream (padding to the TP degree, vocab-sharded logits gathered without gradient scaling), context parallel over the packed no-padding stream through verl's ulysses helpers, expert stacks gathered whole before the HF split, LoRA sync on torchtitan core's LoRA (`peft_config`, adapter-only sync, merge before sync, wrapper-segment naming; ten helpers), fake-quantized expert stacks for QAT models, `ParallelismConfig` compat kwargs, flavors defined as `config_registry` functions, dp-mesh lookup without an fsdp mesh, checkpoint interval from `save_freq` | about 1000 | yes, as model-agnostic PRs, each testable on upstream's own models |
| tests | `tests/workers/test_torchtitan_engine_{peft_config,lora_sync,expert_stacks,receiver_names,pp_sync}.py` | 610 | with their PRs |
| environment | BFX9 matmul-emulation opt-out (`_torch_accepts_bfx9`, `_fp32_matmul_emulation_optional`), `VERL_VLLM_VERSION` override (`third_party/vllm/__init__.py`, `vllm_async_server.py`), `FlexibleArgumentParser` import fallback, `partial_dtensor` in `config/engine.py`, the sft e2e script flags | about 40 | no; a local patch, or one separate small PR for the version override if wanted |
| diagnostics | `KIMI_GRPO_DUMP_LOGPROBS` / `KIMI_GRPO_DUMP_VLLM` hooks (`utils/debug/metrics.py`, `rollout/vllm_rollout/utils.py`, `trainer/ppo/v1/trainer_base.py`), the sync checksum and freeze notice on stderr | about 45 | no; strip before any PR. The log-probability rollout-vs-actor diff metric next to the probability one (`metrics.py`, 4 lines) is generic and can go on its own |

So the K3-specific surface in verl is about 100 lines, and it sits at the verl-to-torchtitan interface (model package lookup, flavor lookup, the three model traits, the processor). The other 1000 lines are engine capabilities the model needed but that name no model.

## Before a draft PR

1. Rebase onto `67858929` (two engine-file commits to merge over).
2. Strip the diagnostics group and park the environment group in a local patch (`matrix_scripts/` or a kit patch file), the way the KDA guard lift is parked on the torchtitan side.
3. Replace the three `torchtitan_name == "kimi_k3"` checks with capability probes on the torchtitan model or its spec (does the model take a folded `[T]` stream; does it run CP inside its modules; does its CP need contiguous rank-ordered shards). Then the engine PRs carry no model name and the K3 PR is the map, the processor case and the patch-size read.
4. The CP piece is written for the pre-4639 module-internal Ulysses (the `13a84361` / `27b72cc3` line) and refuses packed micro-batches under CP; on main tip K3's CP is 4639's (`preprocess_inputs`, `kda_cp_routing`). The CP PR either waits for that port or is written against main's CP API from the start; shipping the old path upstream and then replacing it is worse than shipping it later.

## Stack

1. engine: tensor parallel on the packed stream, and the flavor / `ParallelismConfig` / dp-mesh compat pieces (tests: receiver names).
2. engine: pipeline parallelism (`_PipelineLossBridge`, token budget, sync from every rank; test: pp_sync). Verifiable on upstream's llama / qwen debug models, which is what the reviewer will run.
3. engine: expert-stack gather in the sync, LoRA sync on torchtitan core's LoRA, fake-quantized expert stacks (tests: expert_stacks, lora_sync, peft_config).
4. engine: context parallel over the packed stream, once on main's CP API.
5. Kimi K3: the model-type map, `KimiK3Processor`, the patch-size read, the folded-stream and mask hooks as capability probes (with the trees named: torchtitan main after #4025 / #4499, the fork tree for PP / CP / LoRA).
6. optional: the log-probability diff metric.

Each engine PR's body states which torchtitan tree it was run against; the K3 PR names the open torchtitan PRs it needs for PP / CP / LoRA and stays a draft until they merge.

## Addendum, 2026-09-17

- The engine commits of the night (`7b78ad99` CP port, `bfb5a5e6` DCP initial load, `dd292978` / `2e3b127f` / `a2ef3b93` the sequence_parallel, context_parallel_backend and initial_load_path fields, `580b2ef0` adapter-only naming through `to_hf`) carry no trailers; 32 of the 46 older commits of the branch carry `Co-Authored-By` / `Claude-Session` lines from earlier sessions. The split rewrites every commit anyway (step 2 of "Before a draft PR"), which is where those lines go.
- The engine cells that pass on the ported engine: cp2, fsdp2 x pp2, tp2 x ep2, cp2 x tp2 (all 3 steps, rc 0, fsdp2 grad-norm class); the QLoRA packed-base sync needs `initial_load_path` (a packed DCP from `scripts/quantize_lora_dcp.py`) and the merged sync, and the tree's merge had to learn a locally sharded packed base (`K3_INT_20260916.md`, the 09-17 section).
- Two findings that belong to the K3 PR, not the engine PRs: the image-free placeholder path (`add_zero_valued_dependency`, tree `43ad3bfc2`) and the fused `w13` LoRA target (`0be1fee6f`).

## What remains on the veRL side (2026-09-17)

Before any PR:
1. Strip and split (this plan's stack): drop the diagnostics, park the environment hacks, rewrite the history without trailers, one model-agnostic PR per capability.

    A gap in the kit's own verification, found 2026-09-18: every stage is checked with `ast.parse` and `ruff --select F821,F811,F822`, and no stage has ever run a test. The defect above lived in the group that becomes patch 05 and it turned four of seven tests red in an upstream test file (`tests/workers/rollout/test_vision_placeholder_tokens_on_cpu.py`, added upstream in `7bc80c00`), which a reviewer runs on the first patch that touches it. Two things follow: the verification should run the upstream test files each patch touches, not only parse them, and that file needs `VERL_VLLM_VERSION` in the environment before pytest will collect it at all, which is why nothing on this box noticed.
2. Done 2026-09-17, verified 2026-09-18. Qwen3-0.6B through the same e2e runner on the torchtitan engine: `model_engine=torchtitan` on the command line and `TorchtitanEngineConfig` in the resolved config, real gsm8k parquet rather than the synthetic set. Four cells, logs in `/workspace/grpo-qwen3-*.log`: fsdp2, tp2, cp2 and pp2 each reach `training/global_step:3` with a rollout log-prob diff of 0.0145 to 0.0153 and no error of any kind once the DataLoader's exit-time noise is excluded; the 24 `errors` a naive grep counts are all that teardown warning repeated. The LoRA cell reaches step 1 with complete metrics and then aborts, and that result is **not** carried forward: it ran on `/tmp/wt_verl_0915` at `549c1e21`, which the current rebased branch leads by 73 commits, so the LoRA sync on an upstream model is still open and needs a rerun on the current tree. The rerun of 2026-09-18 did not reach the abort: it died earlier, inside the vLLM server launch, and the cause is a defect this branch introduced rather than anything about LoRA. `processor_takes_medias` reads `inspect.signature(processor.__call__)` and caught only `TypeError` and `ValueError`, so the `AttributeError` a missing `__call__` raises escaped: a text-only model hands the rollout no processor at all, and `get_vision_placeholder_token_ids(None)` raised instead of returning the empty list its upstream docstring promises. Every text-only run through the vLLM rollout died in `launch_server` before step 1. Fixed 2026-09-18 by widening the except clause, one token; `tests/workers/rollout/test_vision_placeholder_tokens_on_cpu.py` goes from four red of seven to seven green. With that fix in, the cell was run again on 2026-09-18 against `/tmp/wt_verl_new` and it does not abort: five steps with rollout log-prob diff mean 0.01494, 0.01474, 0.01492, 0.01441, 0.01466 and grad norm 0.0631 to 0.0676, no `Aborted` anywhere in the log (`/workspace/grpo-qwen3-lora-v3-stopped-at-step4.log`). It was stopped deliberately at step 5, not run to completion: this launcher leaves `total_training_steps` at the inner script's default of 100, which would have held two cards for five hours, and the point the old cell left open was whether it survives past step 1. The four reference cells sit at 0.0145 to 0.0153 and this one at 0.0144 to 0.0149, so step 4 reads marginally under the reference band. Item 2 is closed.
3. Done 2026-09-17 (`133e2ba8`): `tests/workers/test_torchtitan_engine_cp_gloo.py`, the gather's scaling and the model's `preprocess_inputs` shards under cp=2 on gloo.
4. Config fields with tests. Done 2026-09-17: the pipeline token budget, `initial_load_path` and the config-level check of `context_parallel_backend`. `initial_load_path` was decided inline in `__init__`, where no test can reach it without a distributed engine, so it became `initial_checkpoint_source(initial_load_path, model_path) -> (load_in_hf, path)` with three cases covered (a DCP path wins, no path reads the HF weights, an empty string is not a path); `tests/workers/test_torchtitan_engine_config_fields.py` also pins the config's own refusal of an unknown backend, the gate that fires before the transform's. `sequence_parallel` keeps only its default and yaml coverage: its one behavioural use is `ParallelismConfig(enable_sequence_parallel=...)` inside `__init__`, and factoring a single field read out of a constructor buys nothing. Both new test files belong to the split's engine patches (the budget to 02, the config fields to 01). The pipeline token budget is done (2026-09-17, `6f71c346`): the inline parse in `prepare_model_inputs` is now the module-level `pipeline_token_budget(configured, tokens)`, the config field takes precedence over `VERL_PP_TOKEN_BUDGET`, both refusals name the config field, and `tests/workers/test_torchtitan_engine_pp_token_budget.py` covers precedence, the fallback, an exact fit and the two refusals (5 passed). It belongs to patch 02 in the split.
5. Located 2026-09-17, and the parenthesis it used to carry was wrong. The `allgather_kv` backend shards the flex BlockMask under `torch.compile(fullgraph=True)` and the call raises "found no compiled frames" in `context_parallel/api.py` `shard`; `ulysses` keeps the mask global and never reaches it. Dynamo is **not** off in the worker: the engine's probe measures `dynamo.config.disable=False`, `is_dynamo_supported=True`, `TORCHDYNAMO_DISABLE=None`. The one measured difference is `thread=AsyncIO Thread, main=False`, the Ray async actor thread the colocated worker runs its forward on. No mechanism is claimed from a single observation; the next step is one worker calling the same compile from both threads. The body states the behaviour only: under the colocated worker `ulysses` runs and `allgather_kv` does not.

**Correction, same session:** the thread is not the cause. A minimal comparison in one process (`matrix_scripts/dynamo_thread_probe.py`, CPU only, no GPU needed) calls the same `torch.compile(fullgraph=True)` from the main thread, a plain worker thread and an asyncio event-loop thread, and all three compile and run. So running off the main thread does not by itself break `fullgraph=True`, and the cause of the `allgather_kv` failure is still open. The behavioural statement is unchanged and is the only thing the body should carry: under the colocated worker `ulysses` runs and `allgather_kv` does not.

    **Strongest candidate, 2026-09-18, from logs already on disk, and not yet confirmed by a direct test.** `torch._dynamo` raises that exact sentence in one specific case. `eval_frame.py` has two branches for a `fullgraph=True` call that compiled nothing: with skip reasons it prints them, and with none it prints "Compilation was not attempted and no cached compiled code was found", which is the sentence the failure carries, so dynamo had no skip reason to report. The second probe log (`/workspace/img5d-agkv_probe3.log`) says why: at 15:38:01 it prints `torch._dynamo hit config.recompile_limit (8)` for frame `[0/8]`, and that run then dies as `FailOnRecompileLimitHit` at `_attention.py:1304`, the same `compiled_create_block_mask(...)` call the first probe failed on. The defaults are `recompile_limit` 8 and `accumulated_recompile_limit` 256. Three wrappers compile the same `create_block_mask` code object with different settings: `torchtitan/models/common/attention.py:604` and `models/common/vision_encoder.py:35` wrap it as a plain `torch.compile`, while torch's context-parallel path wraps it with `dynamic=False, fullgraph=True`. Once that code object exhausts its recompile budget dynamo stops compiling the frame, and the `fullgraph=True` call reports exactly what it sees. The two probe runs are the cause and the consequence of one thing rather than two failures. This retires the cached-wrapper reading of 2026-09-17: `_compiled_create_block_mask` has exactly one writer in the whole environment, `_create_cp_block_mask` itself, so no foreign call can fill it and it was never the asymmetry. It explains the cases that did not fail as well, the seven passing `allgather_kv` cells under plain torchrun and the fresh wrapper the probe builds, since both reach that code object after far fewer distinct shapes. The falsification test ran on 2026-09-18 and came back inconclusive, so the body still carries only the behavioural sentence. Its reading was registered before it returned, with three outcomes. What happened is a fourth: both ranks printed the raised-limit banner, and the same processes then logged `hit config.recompile_limit (8)` and failed with `found no compiled frames` exactly as before. The registered case for a banner plus a failure was that the account is refuted, but that case assumes the limit was in force, and the warning prints the live value, which was 8. A limit that never took effect cannot refute anything, so the account is where it was: the best explanation of every measurement, with no direct test behind it. What would settle it is reading `torch._dynamo.config.recompile_limit` back at the shard call itself rather than trusting a banner printed earlier in the same process. Getting the raised limit into the process that matters took two attempts and the failures are worth recording, since both looked like they had worked. A `sitecustomize.py` on `PYTHONPATH` does not reach the WorkerDict actors: the first cell ran with the outer `PYTHONPATH` silently replaced by the launcher's own (fixed by `VERL_PYTHONPATH_EXTRA`), and the second reached the driver and the vLLM server processes but printed its banner not once from a WorkerDict, with 41 WorkerDict processes in the log. Both cells are archived under names that say so rather than deleted. The hook that does reach the worker is the engine's own, `_dynamo_probe_once()` at `transformer_impl.py:1905` inside `prepare_model_inputs` under the `cp_enabled` gate, which is the same path the failing shard call is on and which four older logs show printing from a WorkerDict. `fb991180` puts the limit raise there behind `VERL_TORCHTITAN_RECOMPILE_LIMIT` and makes it announce itself on stderr, so a cell claiming the raised limit can be checked instead of believed. It belongs to the diagnostics group, patch 00, and never goes upstream.

A caveat that applies to items 6 to 9 alike: every one of those cells ran on `/tmp/wt_verl_0915` at `549c1e21`, the worktree the branch now leads by more than 73 commits. A pass on an older tree says the capability worked then, not that it works today, which is a weaker claim than the old LoRA cell's failure needed but not a free one. Item 9 is the one to re-run first, since the step parse it exercises was lifted into `checkpoint_step_from_path` on 2026-09-18, after that cell ran. The parse itself is settled: fed the exact path run B printed, the named function returns 2, and 4 for that run's `global_step_4`, -1 for a path naming no step and 7 for one whose other components carry digits. That covers the lift, not the rest of the path (the checkpointer call, the offload around it), so the cell is still worth its rerun. The launcher scripts now take `VERL_TREE` so a re-run lands on the current tree by default.

Engine coverage still missing:
6. LoRA + CP (transform ordering: the flavor's `LoRAConverter` runs before the engine's `ContextParallelTransform`). The static half is closed 2026-09-18 (`ec18c50e`): `apply_transforms` does not run transforms in append order, it sorts on each transform's `run_after`, so the engine's comment that LoRA is ordered after CP rested on `LoRATransform.run_after` naming `ContextParallelTransform` and nothing failed if that declaration changed. `tests/workers/test_torchtitan_engine_transform_order.py` now pins the declaration and the resulting order from both append orders. The runtime half is on record too, and was already run on 2026-09-17: GRPO with `lora.rank=8` at `context_parallel_size=2`, `data_parallel_shard_size=1`, three steps, pg loss 0.08633 / 0.03877 / 0.02266, grad norm 0.0862 / 0.0913 / 0.0911, log-prob diff mean 0.1556 / 0.1442 / 0.1453, `rc=0` (`/workspace/.smoke_int0916/grpo-k3-lora-transform-cp2.log`, copied to `/workspace/grpo-k3-lora-cp2.log`; the one traceback in it is the DataLoader's exit-time noise). So the ordering the test pins is the ordering a cell actually ran.
7. PP + CP and PP + EP together (the bridge's CP gather, the sync from every stage with expert stacks); three-axis cells. Both halves ran 2026-09-17, three steps and `rc=0` each: pp2 x cp2 (`/workspace/.smoke_int0916/grpo-k3-pp2cp2-port.log`, pg loss 0.10380 / 0.06598 / 0.06173, the bridge's CP gather on the last stage) and fsdp2 x pp2 x ep2 (`grpo-k3-pp2ep2-port.log`, pg loss 0.10245 / 0.06332 / 0.05690, the sync from every stage with the expert stacks). What was left is fsdp2 x tp2 x ep2, which the recorded `tp2 x ep2` cell does not cover. It ran 2026-09-18 on `/tmp/wt_verl_new`, three steps in 34 minutes, `rc=0`, with `data_parallel_shard_size=2`, `tensor_parallel_size=2` and `expert_parallel_size=2` read back from its own log: rollout log-prob diff mean 0.15508 / 0.14913 / 0.14622, pg loss 0.064887 / 0.019213 / 0.025932, grad norm 1.09057 / 1.12839 / 1.12843, in the class the other K3 cells read (`/workspace/grpo-k3-fsdp2tp2ep2-v2.log`). Its one traceback is the DataLoader's exit-time noise, printed after the progress bar reads 3/3. Item 7 is closed, and this one ran on the current tree rather than the older worktree.
8. Done 2026-09-17. QLoRA with the fused `w13` projection packed. The split `w1` / `w3` serialization and the `w13.*` packed keys meet in `FeedForward._FUSED_ENTRIES`, which lists `base_qdata` and `base_scale` next to `weight` and `bias`, so the save hook splits the packed pair the same way it splits the fused weight and the load hook merges it back; rows stay interleaved in the packed layout, so one split serves both. `merge_lora_state_dict` then drops every packed entry whatever name a hook gave it, and `scripts/quantize_lora_dcp.py` derives its key map by walking the packed model built on meta rather than guessing from suffixes, so it reads `w1.base_qdata` / `w1.base_scale` and never has to know the projection was fused. The cell: `kimi_k3_rl_qlora_mxfp4`, fsdp2, packed DCP repacked from the rl LoRA flavor's one-step checkpoint, merged sync, three steps, pg loss 0.06780 / 0.02748 / 0.01886, grad norm 0.0866 / 0.0920 / 0.0919, log-prob diff mean 0.1495 / 0.1501 / 0.1508 (`/workspace/.smoke_int0916/grpo-k3-qlora-w13-port.log`; its one traceback is the DataLoader's exit-time noise, printed after the progress bar reads 3/3).
9. Save and resume through the engine (verl `save_freq`, titan interval 1): a resume cell. The step parse is closed 2026-09-18 (`886edfa4`): `load_checkpoint` read verl's `global_step_N` out of the path inline, where no test could reach it and a wrong reading would only surface as a run resuming from the wrong step. It is now `checkpoint_step_from_path`, returning -1 for a path that names no step, which is what the checkpointer already took to mean the latest; `tests/workers/test_torchtitan_engine_checkpoint_step.py` covers the step verl names, a path without one, and a path whose other components carry digits. The resume cell ran 2026-09-17, fsdp2, as a pair. Run A with `trainer.save_freq=2` over two steps wrote `global_step_2` and `latest_checkpointed_iteration.txt`; run B over four steps with `resume_mode=auto` printed `Resuming from .../global_step_2, setting global step to 2` and carried the trajectory on through steps 3 and 4 (pg loss 0.01910 / 0.01638), writing `global_step_4`. Both `rc=0`. The log paths that sentence used to name are no longer the right ones: the runner wrote fixed file names, so the rerun of 2026-09-18 overwrote `grpo-k3-resumeA.log` with its own run A before that was noticed. The 09-17 run B log was copied aside in time and is `grpo-k3-resumeB-20260917.log`; the 09-17 run A log is gone, and the numbers quoted above are what remains of it, transcribed here when it was read. The runner now writes `grpo-k3-resume{A,B}-<date>.log`. **Re-verified on the current tree, 2026-09-18.** Run A wrote `global_step_2` under `/tmp/wt_verl_new`, and run B logged `Resuming from /tmp/wt_verl_new/.../global_step_2, setting global step to 2`, which is the step `checkpoint_step_from_path` returns for that exact string, then ran steps 3 and 4 (pg loss 0.016400 / 0.015436, grad norm 1.12455 / 1.12549) and wrote `global_step_4`; the progress bar reads 4/4 and the only traceback is the DataLoader's exit-time noise after it (`grpo-k3-resumeA-20260918.log`, `grpo-k3-resumeB-20260918.log`). The runner's own `B rc=` reads 127 and is not usable: the script was edited while its shell was still executing it, so bash resumed at a shifted byte offset and ran a fragment, which is also why that archive first landed with an empty date in its name. Run B's outcome rests on the progress bar, the metrics and the two checkpoint directories instead. Item 9 is closed on the current tree. The engine's own checkpoint interval of 1 leaves the decision to verl's `save_freq`.
10. Multimodal GRPO: done as far as this box can take it. The verl side landed 2026-09-17 (`aee5027f` + `549c1e21`: generic VL render for `kimi_k3`, `media_features`, `collapse_media_blocks`, the banned pad, the dataset fallback), and image cells then ran across the parallelism axes (`matrix_scripts/verl_img5d.sh`, ten cells, `VERL_MULTIMODAL_GRPO_PLAN_2026-09-17.md`): each axis alone (tp 2, cp 2, fsdp 2 x ep 2, pp 2), every pair that fits (tp x ep, cp x tp, pp x cp), two triples (fsdp x pp x ep, pp x tp x cp) and one quadruple (fsdp 2 x cp 2 x tp 2 with ep 2 dividing their product). All three steps in every cell, rollout-vs-actor log-prob diffs in the text cells' class, and `prompt_length` 111 everywhere, which is the expanded media block, so each cell carried its images rather than dropping them. **No engine change was needed for any of it**; the `spmd_types` risk on the vision tensors outside context parallel did not fire. Four axes at once is the ceiling on eight cards, since `dp x cp x tp x pp` must equal the device count.

    Evidence for the vision path itself, in increasing strength: the prompt length above; a drop-images pair that widens the actor-to-rollout gap in the right direction and by a plausible size, though its two runs sample independently so it is not controlled; and a one-process two-forward probe (`matrix_scripts/k3_vision_causal_probe.py`) whose verdict is causal rather than tolerance-based, positions before the first media pad bitwise equal and the pads onward different. The probe passes: with 107 tokens and six media pads at positions 81 to 86, the 81 positions before the first pad are bitwise identical between the two forwards and everything from the pad onward moves by 6.8.

Note on `spmd_backend` (2026-09-17): the engine config field is upstream verl's own (12 references on `upstream/main`); `_parallelism_compat_kwargs` passes it only when the torchtitan tree's `ParallelismConfig` still has it, so it stays in patch 01 as is.

Numbers and venue:
11. A real-weight cell (the released checkpoint or the 48B graft): the engine's rollout-vs-actor log-prob diff of 0.15 on the debug model means nothing; Miles quotes a 2e-3 KL floor at 2.8T.
12. Partly done 2026-09-17 (`phase13_k3like_48b_posttrain/CELL_COST_5060_2026-09-17.md`): step time and peak memory for the ten image cells and the text cells are now read off the logs they already wrote, no rerun. Those are 5060 smoke numbers and stay out of any body. What a body needs is still open and belongs to the H100 session: a real batch, more than three steps, and the card the PR targets (MFU reads 0.0 here, the debug model's shapes defeat the estimator).
13. Done 2026-09-17 (`phase13_k3like_48b_posttrain/VLLM_PIN_AND_EXPORT_2026-09-17.md`). The rollout side runs **upstream vLLM's own** K3 support, built from `6dc76a9ade` (2026-07-28), installed non editable into `venv_verl`; the checkout `/workspace/vllm_k3` is four LoRA commits ahead and those are not in the running build, which no recorded cell exercises because every LoRA cell used the merged sync. `VERL_VLLM_VERSION=0.18.0` is a parse workaround for the `0.1.dev1+g...` dev string, not a feature gate. The `-rel` export differs from the plain one in nine tensors only, the KDA `A_log` in released rank-4 spelling (`[1, 1, 16, 1]` against `[16]`); config and the other 1419 tensors are identical, and `-rel` is what every rollout bearing runner loads.
14. The post-training venue decision (torchtitan RL #4680 or the veRL engine), the RFC's open question 2, before the K3 PR is filed on verl.

## Addendum, 2026-09-17 morning: PR 4760

pytorch/torchtitan#4760 (acisseJZhong, draft, "Optimize Ulysses Comm for MLA", 1/3 of a stack on `kda_cp`) is the maintainers' version of #4313's packed Ulysses for MLA: a new `MLAFlexInnerAttention` whose forward takes MLA's packed per-head K/V channels and the head-shared rope key (`q_THK, kv_THP, k_rope_TR`), `UlyssesCPMLAFlexInnerAttention` with one all-to-all of `cat(q, kv_packed)` and one all-gather of the shared key, materialising K/V after the exchange, and an all-gather-KV twin. Same communication as #4313's kernel, without the expand-then-split copy the unified `(q, k, v)` interface cost there. When it merges, the MLA inner attention's config type changes, and the engine's `_context_parallel_transform` mapping needs the entry `MLAFlexInnerAttention.Config` to `UlyssesCPMLAFlexInnerAttention` (and the all-gather twin under `allgather_kv`); patch 04 carries it.

## Addendum, 2026-09-17 evening: rebased onto upstream verl main

`kimi_k3_integration_rebased` rebased from base `00cd5b44` onto `1a8a0f5f` (2026-09-17), 64 commits, no conflict: of the four upstream commits since the base only `verl/workers/rollout/vllm_rollout/utils.py` is a file the fork also touches, and the two changes do not overlap. Per file the branch-minus-base diff is byte-identical to before the rebase (`transformer_impl.py`, `utils/tokenizer/tokenizer.py`, the two rollout files checked). Head `2bf5fbc3`, pushed. CPU tests on the rebased tree: `test_torchtitan_engine_k3_processor.py`, `test_torchtitan_engine_cp_gloo.py`, `test_torchtitan_engine_cp_config.py` = 26 passed, 2 subtests passed.

The split patches (`split_2026-09-17/`) were regenerated once the image cells landed, so the rebase and the fixes were one pass: they are now generated against `upstream/main` = `1a8a0f5f` and head `7db90d7a`, with the extended dynamo diagnostic in the local group and items 785 and 786 following their neighbour into patch 01. Every patch applies on the previous stage, each intermediate tree parses and passes ruff's undefined-name checks, the union reproduces the head exactly, and none of 01 to 06 carries a local marker or a logbook path (`split_2026-09-17/work/verify.txt`).
