# MoonEP smoke on 4 x H200 (217.18.55.111), 2026-09-17

Takes over the MoonEP validation the PR body (`Raising_PRs/PR_K3_PARALLELISM/PR_BODY_MOONEP.md`, draft #4751, head `a706a881d` on main `b21f7d43e`) could not run on the earlier boxes: the 2 x RTX 5060 Ti (no NVSwitch), the 4 x H100 SXM of 2026-09-15 (direct NVLinks, multicast attribute 0) and the 2 x H100 NVL of 2026-08-28 (host GPUs 0 and 5, no NVLink).

## The plan being executed (from the PR body, "To run on 2 x H100 SXM")

1. `moonep_multicast_probe.py` first: every GPU must read `CU_DEVICE_ATTRIBUTE_MULTICAST_SUPPORTED = 1` and `_SymmetricMemory.has_multicast_support = True`.
2. MoonEP's own tests (`test_planning`, `test_dispatch`, `test_combine`, `test_e2e`, `test_grad_reduce`, `test_prefetch`) under torchrun, 2 ranks, then 4.
3. The Kimi K3 debug model, `kimi_k3_debugmodel_moonep` (a local flavor patch, `matrix_scripts/moonep_onbox/0002-kimi_k3-moonep-debug-flavor-a706a881d.patch`; the flavor rule keeps backend-selection flavors off the PR branch), ep2 x fsdp2 for 3 steps, next to the standard dispatcher on the same seed and one warm inductor cache (`mx3_backend` protocol: seed checkpoint, warm pass, measure pass, 10 steps, 8192 tokens per step in 256-token micro-batches per dp rank); a repeated standard cell as the noise-floor row; ep4 x fsdp4 the same way.
4. The per-parameter gradient comparison against the standard dispatcher from the same seed checkpoint (`ep_backend_probe/grad_probe.py`, one micro-batch forward and backward, `grad_diff.py`).
5. A forced-hot routing run that populates a prefetch slot: `moonep_onbox/h200/moonep_hot_probe.py`, the CPU unit test of the branch (`test_moonep_unit_matches_dense_reference_with_duplicated_experts`) rewritten for real GPUs and the real package: `MoonEPTokenDispatcher` + `MoonEPGroupedExperts` + `MoonEPTableBackendNVLink` on an EP mesh, every token routed to experts 0..K-1 (all home on rank 0, so the other ranks' slots must fill), `plan.experts_to_copy` read back, outputs and gradients against a dense fp32 reference on the gathered tokens; then a uniform routing for contrast. E=32, K=4, S=256, D=512, F=384 (the debug model's MoE shape).

Driver: `moonep_onbox/h200/run_all.sh` (cpu tests of the branch, self-tests np2, hot probe np2, matrix, grad probe np2, self-tests np4, hot probe np4, grad probe np4); results in `/workspace/results/` on the box, summary in `summary.txt`.

## The box

4 x H200 (143 GB), driver 595.58.03 (CUDA 13.2), every pair `NV18`, `nvidia-smi -q` Fabric State Completed / Status Success; 112 cores, 3 TB RAM, 683 GB free. Multicast probe: all four GPUs `multicast_attr=1 torch_symm_mem_multicast=True`. A plain Ubuntu 22.04 container with miniconda only: no torch, nvcc 12.6 (`/usr/local/cuda-12.6`), no NVIDIA apt repo needed.

## Environment decisions

- torch `2.15.0.dev20260906+cu126` with torchvision `0.30.0.dev20260906+cu126` (the two must share a date; the 0907 torch has no torchvision partner, which is what the first install tripped on), in a uv venv `/workspace/venv_k3` (Python 3.12; `conda create` silently produced no env, and the deps then landed in base with a PyPI torch 2.11 cu130, which is what the first MoonEP build's "detected CUDA 12.6 mismatches 13.0" meant). cu126 was chosen over installing a CUDA 13.0 toolkit because the box's nvcc is 12.6 and Hopper needs nothing newer; the tree on the 5060 box runs the 0906 nightly (cu130) too.
- Tree `/workspace/tt_moonep` = fork `k3_moonep_seam` `a706a881d` + the flavor patch (uncommitted). Requirements from `.ci/docker/requirements.txt` of that branch; Attention Gym is the same checkout the 5060 box uses (`b19162e`, upstream `meta-pytorch/attention-gym`; shipped as a tarball, installed editable with `SETUPTOOLS_SCM_PRETEND_VERSION=0.0.8` since hatch-vcs has no git metadata there) plus `apache-tvm-ffi`.
- `nvidia-cutlass-dsl==4.4.2`, MoonEP's own pin (`setup.py`); Attention Gym's `linear` extra asks for >= 4.5.0 but its KDA kernels import under 4.4.2. If a KDA kernel fails at JIT, that pin conflict is the first suspect.
- MoonEP `2bd860b` built with nvcc 12.6, `TORCH_CUDA_ARCH_LIST=9.0`, `pip install --no-build-isolation --no-deps -e .` (about 3 minutes); imports.
- Two pitfalls of the take-over: `pkill -f <pattern>` inside an ssh command whose later text contains the pattern kills the ssh shell itself (three dead sessions before the kills were moved into their own call); MoonEP's `NCCL_NVLS_ENABLE=0` and `MOONEP_MEM_HANDLE_TYPE=auto` from the earlier kit are kept.

- The K3 model refuses non-Blackwell GPUs in `KDAKernel.forward` ("Attention Gym KDA requires Blackwell SM100/SM103; got (9, 0)"); the parked local hack `matrix_scripts/local_hacks/kda_sm120_guard_lift.patch` (the same lift the A100 and H100 runs used) is applied on the box's tree, uncommitted, next to the flavor patch. The first matrix attempt (06:59) died on that guard in every cell; nothing of it is reported.
- C4 pass (user, 2026-09-17 07:10): the debug set repeats within a 100-step run, so its tables stop at step 10; the same tables rerun on packed C4 (`allenai/c4` en, streamed through the plain text loader, `ConcatThenSplitPackingConfig`, `matrix_scripts/moonep_onbox/0003-kimi_k3-c4-debug-flavors.patch`: `kimi_k3_debugmodel_c4`, `kimi_k3_debugmodel_moonep_c4`) with 100 measured steps, and those tables may report step 100. `run_c4.sh` is queued behind the debug-set sequence.
- Tables: dp2 and dp4 are different data streams and sit in separate tables, each with its own fresh-cache floor row (`dp2ep2_std_fresh`, `dp4ep4_std_fresh`: the standard EP cell alone on its own cache) and, for dp2, the EP=1 local-fallback row `dp2_moonep_ep1`. Each table carries loss and grad norm side by side.

## 07:40Z: the head, cutlass-dsl and two more dependencies

- The PR branch was not on the latest main: base `b21f7d43e` (2026-09-13), 35 commits behind `upstream/main` `a3a819c67` (2026-09-16; QB #4577 and TP #4499 both touch `kimi_k3/moe.py`). Rebased in a detached worktree (`/tmp/wt_moonep_rb`, head `9e71d1073`, five commits): `moe.py` keeps main's `_init_self_buffers` and forward signature and gains MoonEP's `parallelize`; `model.py`'s token-count loop (added by the first commit, removed by the third) is left out of both, which is the branch tip's own state. Per file, the branch-minus-base diff is identical before and after (9 files), CPU tests 17 passed. Not pushed (force-push of the filed draft branch waits for the user); the H200 tables run on this head so the numbers belong to the head the PR will carry.
- cutlass-dsl: MoonEP `setup.py` pins 4.4.2; Attention Gym `b19162e`'s KDA CuTe backend needs >= 4.5 (`cutlass.Vector`, precompile driver fails on 4.4.2); PyPI has no 4.5.x, so 4.6.0 (with `nvidia-cutlass-dsl-libs-cu12`) is the only version above 4.4.2. On 4.6.0: MoonEP `test_planning` 18 passed 1 skipped, `test_dispatch` 12, `test_combine` 14, `test_prefetch` 14, the forced-hot probe passes; `test_grad_reduce` (`test_grad_reduce_case[single_tile]` first) and `test_e2e` fail inside MoonEP's grad-reduce DSL kernel with "An MLIR function requires a Location but none was provided" (a MoonEP-vs-cutlass-4.6 incompatibility; both passed on 4.4.2, recorded above). The branch's `MoonEPTableBackendNVLink` moves slot weights and slot gradients through `create_nvl_single_owner_tensor` mappings with plain copies and adds, not through that kernel, so the K3 cells do not touch the failing path. The KDA gate on 4.6.0 (one dp1 step of the debug model, old head): step 1 loss 12.44924, grad norm 14.0625.
- New main needs `renderers==0.1.11` (`components/renderer.py`) and bumps `attn-gym[linear]` to 0.0.9; the box keeps the `b19162e` checkout (version string 0.0.8).
- The chain restarted at 07:40Z on `9e71d1073` + the three local patches; the earlier matrix attempts (guard, cutlass 4.4.2) produced no numbers.

## Results

### MoonEP self-tests, 2 ranks (06:53Z)

    test_planning     18 passed, 1 skipped
    test_dispatch     12 passed
    test_combine      14 passed
    test_e2e           1 passed
    test_grad_reduce  12 passed
    test_prefetch     14 passed

### Forced-hot probe, 2 ranks, E=32 K=4 S=256 D=512 F=384 (06:55Z)

Forced-hot (every token to experts 0..3, all home on rank 0): rank 1's slots read `[1, 0, -1, ...]` with 1024 table rows in slots; rank 0's slots stay empty. Against the dense fp32 reference: output max abs diff 0.0043 on both ranks (reference max 0.62 / 0.57), input-gradient max diff 0.017 / 0.014, expert-weight gradients w1 0.073 w2 0.020 on rank 0 (the home rank, where the slot partials are reduced in), 0.000 on rank 1 (no home experts touched). `numerics_ok_all_ranks=True`. Uniform routing: rank 0 prefetches expert 23 (128 rows in a slot) on its own, output max diff 0.0022 / 0.0025, gradients inside the same class. So the slot path runs on real GPUs and reproduces the dense math at bf16 precision; the CPU fake's contract holds on the real package.

### Debug-set dp2 table on `9e71d1073`, cutlass-dsl 4.6.0: the two rows that landed before the stop (08:05Z)

    cell            step 1 loss/grad   step 3            step 10
    dp2_std         12.52567/13.5000   7.63234/9.5000    3.31938/1.8672
    dp2_moonep_ep1  12.52567/13.5000   7.63234/9.5000    3.31938/1.8672

The EP=1 local fallback is bitwise with the standard dispatcher over ten steps on the H200 (the 5060 result, now on this box). `dp2ep2_std` and `dp2ep2_moonep` had not run when the user stopped the MoonEP work: the CPU box's audit of the implementation found problems and MoonEP changes before any more numbers are taken; the C4 tables were stopped with it (its seed step had passed: the streamed `allenai/c4` loader works on the box). The environment, the kit and the trees stay on the box for the rerun.
