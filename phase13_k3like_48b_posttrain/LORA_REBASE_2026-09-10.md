# LoRA branch rebased onto main after 4527 merged; dp/ep matrix rerun (2026-09-10)

- Upstream main moved to `ac10ca48f` ("[spmd] Fix Kimi K3 multimodal training type checking (#4527)", 2026-09-09). `lora_review1` = `93f78b5ab` (8 commits on `af9b6b195`) rebased clean onto it: `lora_review2` = `f092d37d9`, pushed to the fork. Files: `components/lora.py` +749, `optimizer.py` +13, `kimi_k3/config_registry.py` +86 (flavors `kimi_k3_debugmodel_lora`, `_qlora_mxfp4`, `_qlora_mxfp4_linear`), `tests/unit_tests/cpu/test_lora.py`, `kimi_k3/tests/test_qlora_experts.py`, `scripts/quantize_lora_dcp.py`.
- The packed-MXFP4 path imports `torchao.prototype.mx_formats.mx_tensor`; `venv_bfx9` had no torchao (the qlora cells died with `ModuleNotFoundError` on the first pass). `uv pip install --no-deps torchao==0.18.0` into `venv_bfx9` imports on the 2.15 nightly (one pytree deprecation warning). `/venv/main` has torchao 0.17.0 but its torch (2.14 0802) predates the BFX9 gate main needs on Blackwell. torchao 0.18.0 has no `torchao.dtypes.nf4tensor`, so the two NF4 tests skip; the NF4 flavor's import path needs a look before the PR opens.
- Tests on the rebased branch (torchao 0.18.0): `cpu/test_lora.py` + `kimi_k3/tests/test_qlora_experts.py` 17 passed, 2 skipped (NF4).
- Matrix (`/workspace/lora_mx`, scratch `lora_matrix.sh` + `lora_qlora_rerun.sh`; body protocol: plain torchrun, seed 42, deterministic, 10 steps, per-run triton cache, each cell twice): dp1 / dp2 / dp2 x ep2 x {lora, qlora_mxfp4}, all twelve runs rc 0, every cell's two runs bitwise. Table in `PR_BODY_LORA.md`. The previous table (on `af9b6b195`) does not reproduce to the digit because main changed under the model (4527 and the commits between); each cell is self-consistent across two runs, which is the body's floor.
- Not run: dp8 x ep8, TP cells (dp/ep only was asked), the peak-memory placeholder in the body.
- Later the same day (Windows box): `lora_review2` = `7fb4e5360`, three commits on `f092d37d9`. (1) `_get_mxfp4_experts_cls` typed `type[GroupedExperts]` in and out, `register_parameter` declared on the marker base, the packed pair asserted before init: pinned pyrefly 0.45.1 on `lora.py` 4 errors -> 0 (main: 0), the two class-line `type: ignore`s gone. (2) `kimi_k3/tests/test_qlora_experts.py` -> `tests/unit_tests/cpu/test_kimi_k3_qlora_experts.py`: the CPU workflow runs `pytest tests/unit_tests/cpu` only, so the model-dir file was never collected (4468 moved such orphans). (3) The NF4 skips explained: torchao 0.18.0 moved `NF4Tensor` to `torchao.quantization.quantize_.workflows.nf4.nf4_tensor` (pytorch/ao PR 4256) and dropped `torchao.dtypes.nf4tensor`; the four import sites moved, the merge path narrows with `isinstance` instead of an ignore (that line was also over black's width -- the pinned ufmt 2.3.0/black 22.12.0 would have rewrapped it under the comment). With torchao 0.18.0 here `test_lora.py` reads 15 passed of 16 (`test_lora_forward` is an inductor CPU compile that fails on plain main on this box); the experts test needs the K3 package, which Windows cannot import (CuTeDSL) -- rerun both files on the GPU box before the draft opens. (4) `aaa823064`: the `kimi_k3_debugmodel_qlora_mxfp4_linear` flavor removed at the user's call (unreferenced, unmeasured, one converter argument expresses it) and the two remaining flavors' docstrings cut to what the flavor is (the target-set argument and the pp8 anecdote were design text). Body header and counts updated (`lora.py` +765/-2, `config_registry.py` +58/-0).
- Same evening: `tp_sp_on_main` = `ce10692c1` (the eight TP/SP commits rebased from `d1e3979c7` onto `ac10ca48f`, clean); tp=1, tp=2 SP on, dp2 x ep2 x tp2 step 1 bitwise with the 36-cell matrix; GPU tests 3 passed 2 skipped; pushed to the fork, not to `k3_tp_sp`.
