# Integration tree onto main d398a8fb9 (2026-09-10, trial)

`k3_on_4025` (= `da7f9e348`, 48 commits on `ac10ca48f`, the PR 4527 merge of 2026-09-09) rebased in a scratch worktree (`/tmp/wt_rebase`) onto upstream main `d398a8fb9` (2026-09-10 00:22 PDT, "Merge labels into trainer batches (#4572)"). Six main commits in between: #4572 (labels merged into the batch dict: `trainer.py`, `components/loss.py`), #4544 (MTP + CP), #4451 (MTP with chunked loss), #4534 (TorchFT config overrides), #4551 (rl bmm patch removed), #4490 (DeepSeek V4 integration smoke). No conflicts; 140 CPU tests pass; the pinned pyrefly reports the same 18 pre-existing errors on the K3 folder.

Smoke, 3 steps, one seed (`.mx3_seeds_rebase`, the same seed 42 init as `.mx3_seeds_main33`, which dp1 confirms bitwise):

| cell | rebased | pre-rebase (18-cell matrix) |
| --- | --- | --- |
| dp1 | 12.40087 / 7.71281 | 12.40087 / 7.71281 (bitwise) |
| cp2 | 12.41139 / 7.39567 | 12.41139 / 7.39567 (bitwise) |
| ep2 x fsdp2 | 12.36806 / 7.45531 | 12.36806 / 7.45531 (bitwise) |
| pp2 | 12.40087 / 10.49839 / 7.73740 | 12.40087 / 10.49846 / 7.65370 |
| MTP dp1 | seeded with the MTP flavor's own seed (the plain seed lacks `mtp_layers.*`) | |

pp2 differs from step 2 (1e-5 relative, then 1% at step 3) while the three non-pipeline cells are bitwise. #4572 rewrote how the trainer hands labels to the pipeline schedule (per-micro-batch labels now travel inside the batch dict; MTP labels as a tuple). Whether the pp2 move is that change or run-to-run nondeterminism of the pipeline cell is settled by a control: the same pp2 cell on the pre-rebase snapshot with the same seed root (result below).

Control and bisect (08:27-08:45): the same pp2 cell on the pre-rebase snapshot with the same seed root reproduces 10.49846 / 7.65370, so the pipeline cell is deterministic run to run and the move is the rebase's. Rebasing the 48 commits onto each intermediate main commit and running pp2: `7e979ad74`, `f93fd4ccf`, `ffa0a390d` and `cd890fedc` all read 10.49846 / 7.65370; only the full rebase (`d398a8fb9`, #4572 "Merge labels into trainer batches") reads 10.49839 / 7.73740. With all 16 micro-batches in one schedule step (`--parallelism.num-pp-microbatches 16`, no accumulation across groups) the two trees are bitwise (10.53298 / 7.72830), so the change sits in the trainer's handling of several micro-batch groups per step after #4572, not in the per-micro-batch loss path. The pipeline branches (`k3_pp_text` 75045fed5, `k3_pp_mm` c87097ae5) are not rebased and stay where they are; the local pipeline experiments stopped here on the user's instruction (the review on PR 4312 takes priority). The integration tree's rebase is therefore not pushed.
