# 交接(2026-08-25 凌晨)

上一段会话中途换过模型,本文档把**已验证的事实**与**已知问题**分开写清,
避免下一段重新推导或误信。

## 一、必须先处理的两个结构问题(我造成的)

### 1. `PR_K3_PARALLELISM/` 里的文件内容不符合规范

新开目录本身没问题。问题是**文件内容不合规**:

* `PR_BODY_*.md` 混进了中文说明和方法学段落 —— 那是 logbook 的内容,不是 PR 正文。
  规范在 `Raising_PRs/PR_WRITING_RULES.md`,正文按顺序只答四件事然后停:
  (1) 改了什么(具体到 op / 调用点 / 标志,让 reviewer 能预判 diff);
  (2) 今天为什么是错的(一个机制,一句话);
  (3) 证据(**一张数字表或一条 repro 命令,周围不要散文**);
  (4) 什么没变(一行,针对大家会担心的默认配置)。
  设计沿革、备选方案、预先辩解、范围安慰 —— 全部不属于正文。
  该规范是 maintainer 对 PR26 回复 "couldn't really understand the PR summary which
  seems to be written by AI" 之后写的。
* `{PP,CP,EP}_RESULTS.md` / `READINESS_*.md` / `STATUS_*.md` 是**证据与判断**,
  属于 logbook(`phase13_k3like_48b_posttrain/`),不该和待发正文混在同一目录。

**下一段要做**:按 `PR_WRITING_RULES.md` 重写三份正文(纯英文、terse、无结构化标题),
把结果/判断类文件移回 logbook,`PR_K3_PARALLELISM/` 只留可直接粘贴的正文。

### 2. PP 的正文与 PP 分支不符

`PR_BODY_PP.md` 有 4 处讲 DEP(塔独占 stage、clause 2 的报错、塔 share 分解),
但 **`k3_pp_text` 分支里 DEP 文件数为 0** —— 纯文本分支明确排除了 DEP。
正文是照着含 DEP 的 `k3_pp` 分支写的,没有随分支拆分而拆分。

**下一段必须做**:PP 正文按分支拆成两份 —— 文本侧只讲块残差跨 stage 与 FQN 切分;
DEP 的内容移到多模态侧的正文。CP 同理需要核对(`k3_cp_text` 排除了 dynamic CP,
而 `PR_BODY_CP.md` 讲了 5.2.3 的视觉塔切分)。

## 二、六个分支的状态(已验证)

fork `QIU023/torchtitan`:

| 分支 | 内容 | 验证 |
|---|---|---|
| `k3_on_4025` | 集成(四并行 + DEP + dynamic CP) | 已 rebase 到 upstream/main `5fecad929` |
| `k3_pp_text` `1eabf59a4` | 纯文本 PP(**无 DEP**) | import 干净、4 unit tests、上游 28f/733p(与基线同数) |
| `k3_cp_text` `73db4b868` | 纯文本 CP(KCP+Ulysses,**无 dynamic CP**) | 5 unit tests、上游 28f/733p |
| `k3_ep` `c379ec4dd` | EP(无 MoonEP) | 上游 28f/733p |
| `k3_pp` `768a80d80` | PP + DEP(多模态) | 已推,未加 CI |
| `k3_cp` `135abcf36` | CP + dynamic CP(多模态) | 已推,未加 CI |

三个文本分支已按上游 CI 惯例改造:unit test 移到 `tests/unit_tests/`,
并各加了 integration cell(`cp2` / `pp2` + `pp2xvp2` / `ep2`),
recipe 加在 `torchtitan_recipes/tests/features.py`。

## 三、数值结果(方法学可信,全部 seed 断言通过)

方法学(五次踩坑后固化,**必须遵守**):
1. 跨格比较只在**同一脚本内、同一 seed checkpoint** 下有效(seed 本身不可复现,实测三次哈希不同);
2. **每格自带同配置预热**再计量("整个矩阵跑两遍"不等价);
3. 每个计量格断言日志出现 `Loading the checkpoint from`(harness 已内置 `ASSERT-SEED`);
4. **绝不清理正在运行的实验目录**(我曾因此删掉运行中实验的 seed,得出过错误结论)。

* **PP**:dp1 下 9 格(pp2/4/8 x vp2/4)step-1 **全 12.45788 逐位相同**;
  dp2 mesh 下 `fsdp2_pp2` = `fsdp2_pp4` = dp2 **逐位相同**;多模态 `fsdp2_pp2` = dp2 **逐位相同**。
* **CP**:dp1 下 cp2/cp4/cp8 相对 dp1 = 3.0e-4 / 3.5e-4 / 5.0e-6(老树 1.3e-3,**我们紧约 4 倍**);
  dp2 mesh 下 cp2 4.1e-3、cp4 2.75e-3(seq512);多模态 cp2 相对 1.4e-3。
* **EP**:文本 ep2/4/8 = 7.6e-4 / 1.4e-4 / 2.4e-4;多模态 = 1.4e-3 / 1.3e-3 / 1.2e-3。

全部在 bf16 相对精度(2^-8 = 3.9e-3)内。

约束(不是缺陷):FlexAttention 的 BlockMask 要求 `Q_LEN % (cp*128) == 0`
(上游后端,非我们引入),所以 cp4 需 seq>=512、cp8 需 >=1024。

## 四、代码校验与已修缺陷

全量 diff(6504 行 / 27 文件)对照老树逐文件校验:**数值零偏差**。
集合通信的 group 与可微性、折叠布局的规约轴、老树注释记录的实测硬约束、
k3 配置数值,全部一致。

已修并验证(修复前后三格逐位相同,证明不影响任何先前结果):
* `8516debcb` — KDA 整除判据 `tp*cp` 改 `cp`(KDA 是 TP-replicated,实现只按 cp 切);
  `cp_via_sharding_config` 重复定义去重;删掉引用不存在模块的不可达 wrapper 分支。
* `4351bc6dc` — AttnRes 伪查询零初始化(上游自己的构造代码里漏了;`attn_res` 契约要求)。
* `685ae1110` — CP 的两个 TP-only bug:MLA 从投影宽度推 TP-local head 数、
  k_rope 加回 `to_local_partial_grad`(老树实测丢了有 1-6% 梯度误差)。
* `b2439f4de` — LoRA 目标集按老树补齐(原来只有 3 个 MLA 专有目标,导致纯 KDA 段
  零可训练参数、pp8 报 `param_groups matched no parameters`)。
* `4ec85cb7f` — CP 拒绝折叠多文档流(causal-only mask 看不到文档边界)。

## 五、DEP run-ahead:四层断点已全修(a2da63f64 + 1dfc84f76)

| | 断点 | 修法 |
|---|---|---|
| 1 | stage 识别按 `isinstance(KimiK3ViTStage)`,该类在折叠布局下从不构造 | 按"持有 tower"识别 |
| 2 | `VisionPrefetcher` 要的 `encode_images`/`_issue_on_vision_stream`/`_join_vision_stream` 新树全无 | 从老树搬 92 行 side-stream |
| 3 | 安装块门控在 `step_inputs`,它只在 wrapper 布局创建 | 折叠布局下直接创建 |
| 4 | FSDP2 换类名 `FSDPKimiK3Model`,直接 getattr 取不到 tower | `_holds_vision_tower` 遍历 `.modules()` |

验证(4 卡,DEP 开,塔独占 stage 0):0 条 "run-ahead is OFF" 警告、
3 条来自无塔 rank 的正常 info、持塔 rank 报 `vision stage wiring: 1 stage(s)`、
rc=0 loss 12.50030。

**记一个假阳性**:修到第二层时 DEP 已能跑通且无警告 —— 但日志**零安装痕迹**,
没警告是因为函数压根没被调用。只看 rc 和"无警告"会误报修好。

## 六、待办

1. **修 `Raising_PRs/` 结构 + 拆 PP/CP 正文**(见第一节,发 PR 前必须做)。
2. 多模态两个分支(`k3_pp`/`k3_cp`)未加 CI cell。
3. DEP clause 2(塔跨多 stage)未接通:`_split_module` 够不到 `vision_encoder.layers`,
   已改为明确报错;塔的 share 分解单测逐位等价(2/3/4 shares atol=0),是将来的基础。
4. TP 暂停中(maintainer 要求),成因已记录:`routed_down(x_TD)` 是 local_map 未命名的
   第二条输入路径。恢复前必须做 tp2 x cp2 的 forward+gradient parity。

## 七、给上游的两条观察(已写,未提)

`UPSTREAM_AC_OBSERVATION.md`:
* commit `f925dad99` 把 debug flavor 的 AC 默认从 `None` 翻成 `SelectiveAC`,
  于是 debug flavor 不再覆盖 AC 关闭这条路径;且 `ac_policy.apply(model.vision_encoder)`
  让塔也被 checkpoint,任何在塔里存"每次前向状态"的特性都活不过重算。
* AttnRes 伪查询未零初始化(上游自己的构造代码)。
