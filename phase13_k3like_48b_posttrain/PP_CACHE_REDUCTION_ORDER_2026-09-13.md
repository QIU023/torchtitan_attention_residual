# PP rank cache: what each rank holds, and in which order the block gradients are summed (debug model, 2026-09-13)

Code: `k3_pp_text` = `6e1f5e41c`, `torchtitan/models/kimi_k3/pipeline_stage.py` and `layout.py`. Tables and trees below are
generated by `matrix_scripts/pp_cache_reduction_order.py` from the real split (`kimi_k3_module_fqns_per_model_part`) and
`BlockLayoutTables`, not written by hand.

## 1. Where the cache question stands

- With the total grad norm in fp32 (the pytorch PR 194033 fix, applied as the local `gn_fp32_hack.py`), the H100 c4 runs
  show the naive pipeline (cache off) identical to the single-GPU reference in loss on all 100 steps: 1024 tokens (pp2,
  pp2 x vp2, pp4 x vp4), 256 tokens (pp2, pp2 x vp2) and 2048 tokens at dp2 (dp2 x pp2 on a shared compile cache, dp2 x
  pp2 x vp2). The fp32 norm itself differs by one fp32 unit on about half the steps (the pipeline sums the squared norms
  per rank). Without the fix the bf16 norm, and with it the clip factor, depends on the partition.
- So what is left is cache on against cache off. The forward is identical (step-1 loss identical in every cell); the
  difference is only in the backward, in the order a block's gradient contributions are added.

## 2. The model's stack (debug model: 24 layers, blocks of 12)

Two stack entries: `e` (layer 0 appends the embedding output) and `x12` (layer 12, the first of block 1, appends the
output of layers 0-11). The result of block 1 goes into the head's output aggregation and never enters the stack. Every
layer reads the whole stack twice (the attention residual and the FFN residual, `_apply_attention_residual`), and the
head's aggregation reads it once, so every stage after a block's commit contributes to that block's gradient.

## 3. What the code does

Forward (`_assemble`, `_commit_and_route`): a stage builds its stack from the blocks that came by P2P (`delta_to_send`
of the previous stage) and the blocks its rank's store already holds (`cache_at_entry`), in block order, so the stack
values and their order are the same with the cache on and off. With the cache on, received and committed blocks go into
the rank's `PPRankLocalCache`, and each hop carries only the blocks the receiving rank does not hold yet.

Backward: the stage's autograd gives the gradient of every stack column: the stage's own reads (`l<s>` below) plus, for
a block it forwarded in its payload, the gradient that came back for it. Then (`backward_one_chunk`, `split_stack_grad`):
- a column that came by P2P goes back to the previous stage in the payload gradient;
- a column read from the store becomes a deposit in the rank's store, summed as `prior + grad` in the rank's backward
  order (for one micro-batch, the higher stage first);
- the stage that brought the block onto the rank collects the rank's deposits with `grad_col.add_(deposit)`: after its
  own autograd for a block it received, before it (`_retrieve_recv_grads`) for a block it committed.
Every deposit is counted against `deposits_expected`; a missing, extra or uncollected one raises.

With the cache off every hop carries every block, and each stage adds its own part to what came from the next stage.
In bf16 runs the stack gradients are bf16, so every `+` below is a bf16 rounding.

## 4. Per shape: what each rank caches, and the summation trees

`l<s>` = stage s's own contribution to the block's gradient. Parenthesisation = the order the code adds.

### pp2 x vp2: 4 stages, stage s on rank s % 2

| stage (rank) | layers | commits | cache at entry (on) | P2P out, cache on | P2P out, cache off |
| --- | --- | --- | --- | --- | --- |
| s0 (r0) | emb, 0-5 | [e] | - | [e] | [e] |
| s1 (r1) | 6-12 | [x12] | - | [x12] | [e, x12] |
| s2 (r0) | 13-18 | - | [e] | [] | [e, x12] |
| s3 (r1) | 19-23, head | - | [e, x12] | - | - |

Per rank, cache on (which stage brought the block onto the rank, which later stages there read it from the store):

- r0: `e` (commits at s0; store readers s2); `x12` (receives at s2; store readers none)
- r1: `e` (receives at s1; store readers s3); `x12` (commits at s1; store readers s3)

- block `e` (committed at s0):
  - cache off: `(l0 + (l1 + (l2 + l3)))`
  - cache on:  `(l0 + ((l1 + l3) + l2))`
- block `x12` (committed at s1):
  - cache off: `(l1 + (l2 + l3))`
  - cache on:  `(l1 + (l2 + l3))`

### pp4 x vp4: 16 stages, stage s on rank s % 4

| stage (rank) | layers | commits | cache at entry (on) | P2P out, cache on | P2P out, cache off |
| --- | --- | --- | --- | --- | --- |
| s0 (r0) | emb, 0 | [e] | - | [e] | [e] |
| s1 (r1) | 1-2 | - | - | [e] | [e] |
| s2 (r2) | 3-4 | - | - | [e] | [e] |
| s3 (r3) | 5-6 | - | - | [] | [e] |
| s4 (r0) | 7-8 | - | [e] | [] | [e] |
| s5 (r1) | 9-10 | - | [e] | [] | [e] |
| s6 (r2) | 11-12 | [x12] | [e] | [x12] | [e, x12] |
| s7 (r3) | 13-14 | - | [e] | [x12] | [e, x12] |
| s8 (r0) | 15-16 | - | [e] | [x12] | [e, x12] |
| s9 (r1) | 17-18 | - | [e] | [] | [e, x12] |
| s10 (r2) | 19 | - | [e, x12] | [] | [e, x12] |
| s11 (r3) | 20 | - | [e, x12] | [] | [e, x12] |
| s12 (r0) | 21 | - | [e, x12] | [] | [e, x12] |
| s13 (r1) | 22 | - | [e, x12] | [] | [e, x12] |
| s14 (r2) | 23 | - | [e, x12] | [] | [e, x12] |
| s15 (r3) | head | - | [e, x12] | - | - |

Per rank, cache on (which stage brought the block onto the rank, which later stages there read it from the store):

- r0: `e` (commits at s0; store readers s4, s8, s12); `x12` (receives at s8; store readers s12)
- r1: `e` (receives at s1; store readers s5, s9, s13); `x12` (receives at s9; store readers s13)
- r2: `e` (receives at s2; store readers s6, s10, s14); `x12` (commits at s6; store readers s10, s14)
- r3: `e` (receives at s3; store readers s7, s11, s15); `x12` (receives at s7; store readers s11, s15)

- block `e` (committed at s0):
  - cache off: `(l0 + (l1 + (l2 + (l3 + (l4 + (l5 + (l6 + (l7 + (l8 + (l9 + (l10 + (l11 + (l12 + (l13 + (l14 + l15)))))))))))))))`
  - cache on:  `(l0 + (((l1 + ((l2 + (l3 + ((l15 + l11) + l7))) + ((l14 + l10) + l6))) + ((l13 + l9) + l5)) + ((l12 + l8) + l4)))`
- block `x12` (committed at s6):
  - cache off: `(l6 + (l7 + (l8 + (l9 + (l10 + (l11 + (l12 + (l13 + (l14 + l15)))))))))`
  - cache on:  `(l6 + (((l7 + ((l8 + (l9 + l13)) + l12)) + (l15 + l11)) + (l14 + l10)))`

### pp8 x vp2: 16 stages, stage s on rank s % 8

| stage (rank) | layers | commits | cache at entry (on) | P2P out, cache on | P2P out, cache off |
| --- | --- | --- | --- | --- | --- |
| s0 (r0) | emb, 0 | [e] | - | [e] | [e] |
| s1 (r1) | 1-2 | - | - | [e] | [e] |
| s2 (r2) | 3-4 | - | - | [e] | [e] |
| s3 (r3) | 5-6 | - | - | [e] | [e] |
| s4 (r4) | 7-8 | - | - | [e] | [e] |
| s5 (r5) | 9-10 | - | - | [e] | [e] |
| s6 (r6) | 11-12 | [x12] | - | [e, x12] | [e, x12] |
| s7 (r7) | 13-14 | - | - | [x12] | [e, x12] |
| s8 (r0) | 15-16 | - | [e] | [x12] | [e, x12] |
| s9 (r1) | 17-18 | - | [e] | [x12] | [e, x12] |
| s10 (r2) | 19 | - | [e] | [x12] | [e, x12] |
| s11 (r3) | 20 | - | [e] | [x12] | [e, x12] |
| s12 (r4) | 21 | - | [e] | [x12] | [e, x12] |
| s13 (r5) | 22 | - | [e] | [] | [e, x12] |
| s14 (r6) | 23 | - | [e, x12] | [] | [e, x12] |
| s15 (r7) | head | - | [e, x12] | - | - |

Per rank, cache on (which stage brought the block onto the rank, which later stages there read it from the store):

- r0: `e` (commits at s0; store readers s8); `x12` (receives at s8; store readers none)
- r1: `e` (receives at s1; store readers s9); `x12` (receives at s9; store readers none)
- r2: `e` (receives at s2; store readers s10); `x12` (receives at s10; store readers none)
- r3: `e` (receives at s3; store readers s11); `x12` (receives at s11; store readers none)
- r4: `e` (receives at s4; store readers s12); `x12` (receives at s12; store readers none)
- r5: `e` (receives at s5; store readers s13); `x12` (receives at s13; store readers none)
- r6: `e` (receives at s6; store readers s14); `x12` (commits at s6; store readers s14)
- r7: `e` (receives at s7; store readers s15); `x12` (receives at s7; store readers s15)

- block `e` (committed at s0):
  - cache off: `(l0 + (l1 + (l2 + (l3 + (l4 + (l5 + (l6 + (l7 + (l8 + (l9 + (l10 + (l11 + (l12 + (l13 + (l14 + l15)))))))))))))))`
  - cache on:  `(l0 + (((l1 + ((l2 + ((l3 + ((l4 + ((l5 + ((l6 + (l7 + l15)) + l14)) + l13)) + l12)) + l11)) + l10)) + l9) + l8))`
- block `x12` (committed at s6):
  - cache off: `(l6 + (l7 + (l8 + (l9 + (l10 + (l11 + (l12 + (l13 + (l14 + l15)))))))))`
  - cache on:  `(l6 + (((l7 + (l8 + (l9 + (l10 + (l11 + (l12 + l13)))))) + l15) + l14))`

## 5. What the trees predict, against measurement

A block whose tree differs changes the gradient of everything upstream of it: `e` only feeds the embedding, `x12` is the
output of layers 0-11. So:
- pp4 x vp4 (both trees differ): layers 0-11 and `tok_embeddings`. Measured on the 5060 (step-1 gradients, cache on vs
  off): exactly those 334 of 680 parameters differ, in bf16, fp32 and fp64, layers 12-23 bitwise
  (`PP_CACHE_ORDER_PROBE_5060_2026-09-13.md`).
- pp8 x vp2 (both differ): layers 0-11 and `tok_embeddings`. Measured (bf16 step 1, 256 tokens,
  `cache_order_prediction_check.sh`): exactly those 334 parameters, max relative 7.5e-2.
- pp2 x vp2: the stage-level trees above have `x12` the same in both modes, predicting only `tok_embeddings`. Measured
  (bf16 step 1, 1024 tokens): the same 334 parameters as the other shapes, `x12` included. So the stage-level trees are
  too coarse. A stage's `l<s>` is not one term: the stage's stack column receives the incoming gradient (the block's
  payload gradient) and each of the stage's reads (two per layer) as separate contributions to one accumulator. With the
  cache off, a stage's reads are added onto the sum that came from the next stage; with the cache on, a store reader's
  reads are summed on their own and join the chain later as one deposit. At pp2 x vp2, `x12`'s naive order is
  s3's reads, then s2's reads added onto that sum, then s1's; with the cache on s2's reads are summed from zero (s2 does
  not forward `x12`, rank 1 holds it) and s3's sum is added at s1. Corrected rule, matching all three measured shapes:
  every block that takes a deposit is summed in a different order, and everything upstream of it differs.


## 6. Can the cache's order be tweaked to the naive one?

Not bitwise without moving gradients across ranks. (The finer, per-read picture of section 5 makes this stronger: a stage's first addition in the naive order starts from the next stage's sum.) The naive tree is a right fold over the stages in order,
`l_o + (l_(o+1) + (... + l_(S-1)))`, which is what one GPU computes (measured bitwise). Every inner sum
`l_s + (...)` whose two sides sit on different ranks needs the partial sum from stage s+1 to reach stage s, i.e. the
block's gradient on that hop. With the cache on that gradient does not travel: in pp2 x vp2 the naive order needs
`l2 + l3` first, `l2` is on rank 0 and `l3` on rank 1, and `e` is not in the s2 -> s3 payload, so nothing carries `e`'s
gradient from s3 to s2. No reordering of the deposits or of the collect on one rank reproduces a sum that needs a term
from another rank first.

Options:
- (A) Cache in the forward only, naive backward: the forward keeps sending only the delta; every backward hop carries the
  gradient of every block (as with the cache off), and the stores take no deposits. Bitwise naive by construction; keeps
  the forward saving, gives up the backward one. torch pipelining sizes each backward buffer from the forward output
  (`_create_grad_recv_info`, `get_bwd_recv_ops` / `get_bwd_send_ops`), so the stage needs its own backward P2P shapes:
  the same class of change as option 2 in `Raising_PRs/PR_K3_PARALLELISM/PP_EMPTY_PAYLOAD_NOTE_2026-09-13.md` (both
  sides must agree or the hop hangs).
- (B) Keep the design and state what it changes: only the association of the bf16 sums of block-gradient contributions.
  Evidence: the trees, the measured parameter sets, the difference scaling with the working precision (max relative
  7.4e-2 / 9.1e-6 / 1.7e-14 in bf16 / fp32 / fp64) and fp64 trajectories that stay at the 1e-14 level for 20 steps.
- Accumulating the deposits in fp32 makes the cache path's own sums more exact but not equal to the naive bf16 chain;
  it does not give bitwise equality either.

Nothing is changed in the code yet; (A) is a design decision.
