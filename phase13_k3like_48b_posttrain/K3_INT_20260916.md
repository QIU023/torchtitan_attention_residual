# Integration tree rebased onto main `1949c297f` (2026-09-16), after TP merged

PR 4499 (TP/SP) merged on 2026-09-16 as `3ce746c71`, so the tree drops its twelve TP commits. Worktree `/tmp/wt_int0916`: `git rebase --onto upstream/main 0cabf33cc ae0c7372c` (the 70 commits above the TP range of the 09-15 tree), head `ef3fa6762`; run worktree `/tmp/wt_int0916_rl` = the same plus the uncommitted `rl` flavor (`int0915_logs/rl_flavor_0915.patch`).

## Main since `30af8876d` (9 commits) and what conflicted

- `3ce746c71` TP (#4499): our own content, no conflict once the twelve commits were left out.
- `26db7f2ac` "Migrate LoRA to ModelConfigTransform" (#4684): `LoRAConverter` is gone from main; `LoRATransform` (a `ModelConfigTransform`) plus `LinearLoRAHandler` replace it, the LoRA linear moved to `models/common/lora.py` (`specialize_lora_linear`), `validate_converter_order` became `validate_converter_compatibility` without the LoRA ordering check. Our LoRA stack (`af051ac9d` adapter export / QLoRA NF4 + packed MXFP4 / packed TP, then the packed experts, the marker bases, the 09-15 sharding-copy fix) is built on `LoRAConverter`, and the veRL engine and the K3 LoRA flavors use `LoRALinearBase`, `merge_lora_state_dict`, `trainable_state_dict`, `LoRAConverter.Config`. Resolution on this tree: both coexist in `config/transform/lora.py`, main's `_LoRAHandler` / `LinearLoRAHandler` / `LoRATransform` inserted before our `LoRAConverter`, imports merged, the two frozen-config helpers shared (identical on both sides); `test_lora.py` keeps main's LoRATransform tests and ours. The K3 registry takes `validate_converter_compatibility`. Not done: re-expressing our QLoRA and packed experts on the new `LoRATransform` / handler seam, which the LoRA PR needs before filing (a design decision: where MXFP4 packing and the packed experts live as handlers).
- `4fe013957` + `ab1e79621` (RegionAC CP boundary test added and reverted) and #4684's test placement: `test_transforms.py` keeps main's `test_linear_lora_handler_is_exported` and 4639's `test_replaces_each_config_type_with_its_mapped_backend` in `TestContextParallelTransform`.
- QB: PR 4577 moved to `6e2fe63e2` (rebased onto main on 09-14); its content against its base is the same file set and sizes as the `01dd6ddb8` we stacked, so the stacked change stays.
- 4639 and 4680 unchanged.

Four stops in seventy commits: `test_transforms.py` (4639's K3 commit), `lora.py` + `test_lora.py` (our LoRA stack against #4684), two import unions (`typing`, `dataclasses.replace`), and the K3 registry imports (QB's hook against main's `validate_converter_compatibility`).

## Checks

- CPU (venv_bfx9): 187 passed, 1 failed across the K3, LoRA, QAT, transform, MoE, CP validation, PP, definitions and frozen-CLI tests. The failure is main's new `test_lora_runs_after_context_parallelism` (#4684), which writes `ContextParallelTransform(inner_attention=<class>)`, the pre-4639 signature; 4639's core refactor keys the replacement by config type, so the test takes the dict form on this tree (`f61bbdb83`; 4639 will have to make the same change when it rebases, it is one of its four conflicts against main tip). After it: `test_transforms.py` 16 passed.
- pyrefly on `config/transform/lora.py`: 3 errors, all from the 09-15 sharding-copy fix (`states` and `sharding` typed Optional at the packed-experts rewrite), present on the 09-15 tree too; to fix with the next edit of that file, not part of the rebase.
- GPU smokes: `int0915_logs/smoke_int0916.sh` (the 09-15 matrix, 14 cells, 3 steps, cold caches), table below.

16/16 rc 0 on `ef3fa6762` (RTX 5060 Ti box, venv_bfx9, `/workspace/.smoke_int0916/0916_021329/`; the losses are one cold cache per cell, a smoke and not a comparison):

| cell | rc | loss / grad norm, step 1 | step 3 |
| --- | --- | --- | --- |
| dp1 | 0 | 12.51390 / 14.5625 | 7.80644 / 11.3125 |
| dp1, compile | 0 | 12.54263 / 14.0000 | 7.74858 / 7.5000 |
| region AC | 0 | 12.48550 / 14.7500 | 7.55868 / 7.1562 |
| mtp | 0 | 16.16532 / 14.4375 | 9.82712 / 13.9375 |
| lora | 0 | 12.49222 / 1.1250 | 12.34447 / 1.1719 |
| qlora_mxfp4 | 0 | 12.50673 / 1.0625 | 12.26012 / 1.0391 |
| mx_qat | 0 | 12.53336 / 14.1250 | 7.36421 / 12.5000 |
| dp2 x ep2 | 0 | 12.44417 / 15.6250 | 7.32724 / 11.5625 |
| muon dp2 | 0 | 12.58015 / 13.3125 | 10.18694 / 14.3750 |
| pp2 vp2 | 0 | 12.55630 / 20.3750 | 8.25143 / 12.2500 |
| mm fsdp2 x tp2 x ep2, SP | 0 | 12.55219 / 23.5000 | 9.82179 / 19.0000 |
| mm fsdp2 x tp2 x ep2, SP off | 0 | 12.45515 / 23.3750 | 9.88010 / 19.7500 |
| mm cp2 allgather | 0 | 12.34997 / 29.1250 | 9.93399 / 19.8750 |
| mm cp2 ulysses | 0 | 12.51673 / 26.6250 | 10.04384 / 16.5000 |
| pp8 vp4 | 0 | 12.38987 / 16.5000 | 8.96693 / 12.7500 |
| pp8 vp4, ViT DEP | 0 | 12.52429 / 17.5000 | 8.53024 / 11.0625 |

`k3_on_4025` = `f61bbdb83` (force-with-lease from `ae0c7372c`), tag `k3_int_20260916`: 71 commits on main `1949c297f`. veRL runners for this tree: `matrix_scripts/verl_grpo_int0916.sh` and `verl_grpo_int0916_nd.sh` (PYTHONPATH `/tmp/wt_int0916_rl`).

## veRL on this tree

GRPO fsdp2 (`verl_grpo_int0916.sh`, verl `a3661ae3`, export `-rel`, GPUs 6,7, 3 steps): rc 0, pg loss 0.06218 / 0.02704 / 0.01277, grad norm 1.0910 / 1.1318 / 1.1243, rollout-vs-actor log-prob diff mean 0.1552 / 0.1506 / 0.1482, in line with the 09-15 tree (1.10 / 1.13 / 1.14).

LoRA cell (`kimi_k3_rl_lora`, merged sync, the engine's `LoRALinearBase` path against main's LoRA rewrite; GPUs 4,5, 3 steps): rc 0, pg loss 0.05458 / 0.02368 / 0.01685, grad norm 0.0826 / 0.0863 / 0.0866 (adapters only, as on 09-15: 0.0832 / 0.0858 / 0.0854), log-prob diff mean 0.1570 / 0.1486 / 0.1504. The coexistence of `LoRAConverter` with main's `LoRATransform` costs the engine nothing.

## Dynamic CP joins the tree (2026-09-16)

`68609b2e4` = `cpmm_review1`'s dynamic CP commit (`774e0b9b5`) cherry-picked onto `f61bbdb83`; the transform's tower exclusion was already on the tree. Eight positional hunks: the imports (no runtime `ContextParallelRouting` import, the tree keeps it under `TYPE_CHECKING`), the config field and the two attributes next to MTP's, `encode_images` (dynamic CP's body, with DEP's three stream helpers kept after it, so the pipeline's vision run-ahead now runs the partitioned encode too), `_prepare_multimodal_embeds` (DEP's `vision_embeds` path kept; the dtype cast lives inside `encode_images`), and `_build_cp_subgroups` appended after the split section. CPU: 130 passed (K3, transforms, definitions, frozen CLI, CP validation).

Seeded cut-vs-replicated cells on the tree (`int0915_logs/run_int0916_cpmm.sh`, the b200 mm CP recipes through `cpmm_probe.py`, threshold 96 for the cut, seed 42, deterministic, 2 GPUs each, 3 steps; `/workspace/.smoke_int0915/int0916_cpmm_041652/`):

| cell | rc | loss / grad norm, step 1 | step 2 | step 3 |
| --- | --- | --- | --- | --- |
| allgather-KV cp2, tower cut | 0 | 12.28012 / 27.0000 | 11.01089 / 37.0000 | 10.21265 / 20.5000 |
| allgather-KV cp2, tower replicated | 0 | 12.25950 / 27.2500 | 11.29449 / 34.7500 | 10.05819 / 20.0000 |
| Ulysses cp2, tower cut | 0 | 12.28012 / 27.0000 | 11.01089 / 37.0000 | 10.20506 / 20.3750 |
| Ulysses cp2, tower replicated | 0 | 12.25950 / 27.2500 | 11.29449 / 34.7500 | 9.98305 / 20.2500 |

Held, not located: on this tree the cut tower moves step 1 by +0.02062 (0.17% relative), against +0.00042 (3.4e-5) for the same recipes on the 4639-only branch (`REBASE_CPMM_DEP_AC_2026-09-15.md`). The tree adds the quantile-balanced router (#4577) and the rest of the stack on top of 4639, and a tower difference at the 1e-6 level can flip a routing near-tie; which of those carries the 0.17% is not measured, so this number goes in no body. The 09-10 port read the same size of move (12.39174 to 12.41139, 0.16%) on the old tree.

Tower-only probe on this tree (`int0915_logs/tower_parity_probe.py`, 2 ranks, the same weights and the debug batch's 16 x 12 image, `encode_images` cut against replicated, forward only): bf16 compute, max |cut - replicated| 4.69e-2 over a token scale of 4.34, 1.08e-2 relative, the size of bf16's own spacing (7.8e-3) and of the 09-10 bf16 row (8.5e-3 forward). The fp32 pass, which read 1e-6 on 09-10, does not run here: flex attention in fp32 wants 128 KiB of shared memory per block and the 5060 Ti has 99 KiB (`No valid triton configs`), so the fp32 parity on this tree is owed to a larger card. With the tower difference at bf16 spacing, the 0.17% at the loss is downstream amplification (the routed MoE sees a slightly different image token stream); the amount is not attributed further.

## CP composed with PP or TP: refused on this tree

`parallelize_kimi_k3` on the tree (from PR 4639's K3 commit) raises `NotImplementedError("Kimi K3 does not support context parallel with tensor parallel, context parallel with pipeline parallel")` (`parallelize.py:75`); the cp2 x tp2 and cp2 x pp2 cells (`int0915_logs/run_int0916_cpcross.sh`, tower cut, seeded) stop there, and cp2 x pp2 additionally needs the b200 recipe's SPMD type checking off (type checking refuses a pipeline). So on this tree CP composes only with FSDP and EP. The RFC's 3-of-4 matrix cells with CP (fsdp x tp x cp, fsdp x pp x cp, tp x pp x cp) therefore cannot run until that refusal is lifted and the two combinations made to work on 4639's CP; the old tree ran tp x cp with sequence parallel off (`K3 mm18`) and pp x cp (the 09-10 hang note) on our own CP, whose block-stack and head-split cp adaptations this tree still carries (`7ac90adbf`, `16d97ac53`). A maintainers' decision as much as ours: 4639 is theirs.

## Seeded composability matrix on the tree (2026-09-16)

`matrix_scripts/mx4.sh` (mx3 with a module and config per cell), tree `68609b2e4`, one seed checkpoint (`kimi_k3_debugmodel`, seed 42, built once on one GPU and loaded by every cell, asserted from the log), 4096 tokens per step in 256-token micro-batches per dp rank, deterministic, one warm pass per cell then 10 measured steps on one shared inductor cache; run `mx4_int0916_0916_043002`. The RFC's 3-of-4 cells without CP, the single-axis cells, CP with EP and with FSDP, and the max-degree EP cell; the CP x TP and CP x PP cells are refused by 4639's `parallelize_kimi_k3` on this head (below, lifted on a scratch worktree).

| cell | GPUs | seed | loss step 1 | step 3 | step 10 |
| --- | ---: | --- | ---: | ---: | ---: |
| dp1 | 1 | seed-ok | 12.52917 | 7.68933 | 3.33527 |
| fsdp2 | 2 | seed-ok | 12.52622 | 7.65715 | 3.32807 |
| tp2 | 2 | seed-ok | 12.56267 | 7.74384 | 3.37369 |
| ep2_fsdp2 | 2 | seed-ok | 12.52679 | 7.50372 | 3.37598 |
| tp2_ep2 | 2 | seed-ok | 12.54518 | 7.84336 | 3.50007 |
| pp2 | 2 | seed-ok | 12.52917 | 7.70409 | 3.34125 |
| fsdp2_pp2 | 4 | seed-ok | 12.52622 | 7.63718 | 3.35940 |
| tp2_pp2 | 4 | seed-ok | 12.56267 | 7.78131 | 3.36728 |
| fsdp2_tp2_pp2 | 8 | seed-ok | 12.55269 | 7.58042 | 3.32105 |
| fsdp2_pp2_ep2 | 4 | seed-ok | 12.52679 | 7.66609 | 3.30835 |
| fsdp2_tp2_ep2 | 4 | seed-ok | 12.53070 | 7.78067 | 3.41577 |
| cp2_ag | 2 | seed-ok | 12.53324 | 7.58167 | 3.39545 |
| cp2_ul | 2 | seed-ok | 12.53834 | 7.58215 | 3.30095 |
| cp2_ep2 | 2 | seed-ok | 12.53176 | 7.72532 | 3.39586 |
| cp2_fsdp2 | 4 | seed-ok | 12.52533 | 7.40680 | 3.31679 |
| ep8_fsdp8 | 8 | seed-ok | 12.54717 | 7.96937 | 3.27699 |

Read with the numerics rules: dp1 and dp2 and dp8 read different documents, so only cells with the same dp degree pair. Step 1 pairs that are bitwise: dp1 = pp2 (12.52917), fsdp2 = fsdp2 x pp2 (12.52622), tp2 = tp2 x pp2 (12.56267), ep2 x fsdp2 = fsdp2 x pp2 x ep2 (12.52679): the pipeline changes nothing at step 1 in all four pairings. The axes that change a reduction order move step 1 by their usual amount on this box: tp2 against dp1 +0.27%, cp2 against dp1 +0.03% (allgather) and +0.07% (Ulysses), cp2 x ep2 against dp1 +0.02%, fsdp2 x tp2 x ep2 against fsdp2 +0.04%, fsdp2 x tp2 x pp2 against fsdp2 +0.21%, cp2 x fsdp2 against fsdp2 -0.007%. Step 10 sits between 3.28 and 3.50 in every cell; not an argument either way (`k3-step10-spread` memory).

## CP x PP and CP x TP with the refusal lifted (user, 2026-09-16: "这个应该在整合树放开")

Scratch worktree `/tmp/wt_int0916_cpx` on `68609b2e4`: the `unsupported_parallelisms` block in `parallelize_kimi_k3` deleted (no test asserts it; no second guard elsewhere, the MTP-with-CP check is unrelated). First round through mx4 (`run_mx4_cpx.sh`, the matrix's seed and warm cache, tower cut at 96), both flavours:

- cp2 x pp2: the second stage raises `pixel_values were provided without a vision encoder` in 4639's `preprocess_inputs`, which every stage runs on the whole batch. Fix: the CP vision-bank block only runs on a model part that holds the tower.
- cp2 x tp2: `NotImplementedError: Operator c10d.allgather_.default does not have a sharding strategy registered` at the partitioned tower's K/V gather (`KimiK3VisionCPAttention.forward`): under TP the tower's activations are DTensors sharded on the head dim, and `torch.distributed.nn.functional.all_gather` has no DTensor path. Fix: `_gather_tokens` runs the collective on the local shard and re-wraps with the same placements (the sub-CP group's members share a tp coordinate, so each gathers its own head shard).
- cp2 alone with the tower cut, same seed and cache as the matrix: step 1 `12.53243` against the matrix's replicated `12.53324` (-0.0065%), step 10 `3.36897` against `3.39545`. The 0.17% step-1 move seen in the cold 3-step cells earlier today does not appear under the seeded 4096-token batch; the earlier cells ran the recipe's own batch shape from a fresh init.

Second round with both fixes (`run_mx4_cpx2.sh`): cp2 x pp2 passes in both flavours, allgather-KV `12.53243` / `7.73041` / `3.34532` (steps 1 / 3 / 10), bitwise at step 1 with cp2 alone with the tower cut (`12.53243`), so PP changes nothing at step 1 under CP either; Ulysses `12.54479` / `7.73200` / `3.29186` (its cut-only reference runs in the next round). cp2 x tp2 still stops, twice more, each time one layer deeper into the type checker:

- with the registered `_AllGather`, the checker's local rule refuses the input's tp type: `SpmdTypeError: No valid sharding strategy for apply on axis mesh_tp` (the tower's K/V are sharded on the head dim under TP, and a registered Function only admits R / V / I);
- inside `spmd.local()` the checker still intercepts the Function (`_AllGather.apply: autograd Function is not registered`), so a local region is not the mechanism; the gather now runs under `spmd.no_typecheck()` and `spmd.assert_type` puts the input's per-axis types and PartitionSpec back on the gathered tensor (the K2.5 tower builds its block mask the same way);
- past the gather, `_encode_images_partitioned` mixes types at `torch.cat(pieces)`: the padding rows come from `flat.new_zeros`, and a fresh tensor reads R on tp while the pixel slices are I (the declaration-traps note). The padding, the placeholder pixels and the per-item grids are now derived from the batch's own tensors (`flat[:n] * 0`, `grid_thw[img : img + 1].clone()` with the band written in) instead of fresh ones.

Fourth round (`run_mx4_cpx4.sh`): both cp2 x tp2 cells pass, the "Dynamic CP" line in both logs. All the two-axis CP cells with the tower cut, same seed, batch and warm cache as the matrix:

| cell | GPUs | loss step 1 | step 3 | step 10 | pairs with |
| --- | ---: | ---: | ---: | ---: | --- |
| cp2 allgather, tower cut | 2 | 12.53243 | 7.56621 | 3.36897 | the matrix's replicated cp2_ag 12.53324 |
| cp2 Ulysses, tower cut | 2 | 12.54478 | 7.78650 | 3.34403 | the matrix's replicated cp2_ul 12.53834 |
| cp2 x pp2 allgather | 4 | 12.53243 | 7.73041 | 3.34532 | cp2 allgather cut: bitwise at step 1 |
| cp2 x pp2 Ulysses | 4 | 12.54479 | 7.73200 | 3.29186 | cp2 Ulysses cut: 12.54478, one unit in the fifth decimal |
| cp2 x tp2 allgather | 4 | 12.55824 | 7.27533 | 3.43546 | tp2 12.56267 / cp2 cut 12.53243 (TP's reduction order) |
| cp2 x tp2 Ulysses | 4 | 12.53918 | 7.52237 | 3.35178 | tp2 / cp2 Ulysses cut 12.54478 |

The three changes are one integration commit, `8e0ae8381` on `68609b2e4` (parallelize.py: the refusal gone; model.py: the stage-aware vision bank and the typed fresh tensors; vision_encoder.py: `_gather_tokens` outside the checker with the types put back). CPU on that head: 130 passed. The three-axis cells with CP (`run_mx4_cpx5.sh`, 8 GPUs each, same seed, batch and warm cache, tower cut):

| cell | loss step 1 | step 3 | step 10 | pairs with |
| --- | ---: | ---: | ---: | --- |
| fsdp2 x cp2 x tp2 | 12.52262 | 7.48441 | 3.37414 | fsdp2 12.52622, fsdp2 x tp2 x ep2 12.53070 (reduction order) |
| fsdp2 x cp2 x pp2 | 12.52668 | 7.65896 | 3.33817 | cp2 x fsdp2 (tower replicated) 12.52533; no cut-tower dp2 reference |
| tp2 x cp2 x pp2 | 12.55824 | 7.32999 | 3.39055 | cp2 x tp2 allgather cut 12.55824: bitwise at step 1 |

Every cell of the RFC's 3-of-4 matrix now runs on the tree, with CP composing with TP and PP. PP is bitwise at step 1 against its non-PP twin in every pairing that has one (five pairings across the two matrices).

`k3_on_4025` = `8e0ae8381` (force-with-lease from `f61bbdb83`), tag `k3_int_20260916b`; `/tmp/wt_int0916` and `/tmp/wt_int0916_rl` follow it.

## AC head rebased to main tip; the two 4312 stacks smoked (2026-09-16, later)

`ac_review3` (PR 4656 repurposed) rebased on the user's word ("ac reuse head rebase到最新main先") from main `b21f7d43e` onto `810e62786` (TP #4499, quantile balancing #4577 and #4703 in between): the recompute commit `fd0a9b6c6` became `be311737f` with one import hunk (`spmd_types` / `torch_remat` lines the TP merge touched), its CPU test `ea1316606` cherry-picked to `7ec30e5a9`. `git rebase --continue` kept refusing after the hunk was resolved with no unmerged path left; the commit was made by hand (`git commit -C`) and the rebase aborted. CPU test 3 passed, ufmt and pyrefly clean, no trailers. Smoke (`int0915_logs/run_ac4656r.sh`, GPUs 4 to 6, seeded, KDA guard lifted locally): AC none / selective / full all rc 0 and identical, `12.62200` / `10.81623` / `8.22700`, peak 14.17 / 12.68 / 12.53 GiB. `ac_review3` and `k3_ac_reuse_attention` pushed to `7ec30e5a9` (force-with-lease from `ea1316606`). Body header updated (`PR_BODY_AC_REUSE.md`); the H100 table is still owed before the draft leaves DO NOT review.

PP offload stack (`pp_offload_review1` = `d49bb388b` on 4312's `de6f29514`): `int0915_logs/run_mx4_ppx.sh` with `int0915_logs/ppx_probe.py`, seed checkpoint `kimi_k3_debugmodel_9ca891244914`, one warm step then 10 measured on one cache, pp2 x vp2 with 16 micro-batches: `pp2_plain` and `pp2_offload` bitwise, `12.53584` / `6.60274` / `3.21885`, peak 10.01 / 11.08 GiB on both. PP balance stack (`pp_balance_review1`): the first pair (`/workspace/mx4_ppbal_4312_0916_081602`) failed in the balance cell at `install_pp_balance`: `NameError: name 'os' is not defined` inside `_load_transfer_engine`'s preload branch. The review branch had lost `import os` together with `import logging` when the logger was switched to titan's during the audit trim; the CPU tests never construct the engine, and pyrefly did not flag the file. Restored (`63869c502`). The preload branch itself was needed because the matrix venv runs a cu130 torch and mooncake 0.3.13 links `libcudart.so.12`; `nvidia-cuda-runtime-cu12` installed into `venv_bfx9`, after which `_load_transfer_engine` returns the class in both venvs. Rerun (`int0915_logs/run_mx4_ppbal2.sh`, `/workspace/mx4_ppbal2_4312_0916_082423`): `pp2_plain` and `pp2_balance` bitwise, `12.53584` / `6.60274` / `3.21885`; rank 0 parked 960 tensors, 960 MiB, fetched all 960 back, 9,920 below the 1 MiB floor kept local, `skip_pool` 0 against a 2048 MiB pool; peak 10.01 / 11.08 GiB on both.

The user asked whether PP balance needs the Hopper NVSwitch that MoonEP needs. It does not: it moves tensors through the Mooncake Transfer Engine, which on the standard build of 0.3.13 ignores the `protocol` string given to `initialize` and auto-discovers its transport (`TransferEngineImpl::init`: RDMA when `getHcaList()` is non-empty and `MC_FORCE_TCP` is unset, TCP otherwise, `MC_INTRANODE_NVLINK` for same-host GPU P2P; `transfer_engine_py.cpp` constructs the engine with auto discovery on). The engine's `_buffers_on_gpu` inference from `get_local_topology()` is that same rule; the `MC_FORCE_TCP` case is now mirrored (`080f44208`, the review branch's head, pushed force-with-lease). The RDMA path has not run on any box yet and is part of the H100 table. Bodies updated (`PR_BODY_PP_BALANCE.md`, `PR_BODY_PP_OFFLOAD.md`).

## Rebased onto main `810e62786` (2026-09-16, afternoon): `k3_on_4025` = `45ed0e3c1`, tag `k3_int_20260916c`

The user asked whether the tree was ready; its base `1949c297f` was five commits behind main: `497c47463` (#4736, attn-gym 0.0.9 pin), `cce35d4b7` (#4637, vocab-parallel embedding padding gradients), `f8bb599a7` (#4577, quantile balancing, merged), `e72fd863d` (#4703, dist-GEMM composed from parallel linear projections; deletes `distributed/linear.py`, nothing on the tree imports it), `810e62786` (#4635). `git rebase --onto upstream/main 1949c297f` in `/tmp/wt_int0916`, three conflicts: `kimi_k3/__init__.py` (the PP integration commit's `pipelining_fn=pipeline_kimi_k3` against #4577's `post_optimizer_build_fn=register_moe_quantile_balancing_hook`; both kept), the attn-gym dependency in `pyproject.toml` and `.ci/docker/requirements.txt` (4639's `git+...attention-gym.git@main` kept over the `==0.0.9` pin; the tree runs attn-gym main from `PYPRE` anyway), and `kimi_k3/moe.py` (#4577's `_init_self_buffers` and the MoonEP `parallelize` landed at the same spot; both kept, `_init_self_buffers` first). The stacked copy of #4577 (`ed83e6acf`, the eleven commits as one change) came through the rebase as a 12-line residue that defined `KimiLatentMoE._init_self_buffers` a second time and repeated a `score_func` kwarg in `test_moe.py` (a syntax error); dropped with `git rebase --onto ed83e6acf~1 ed83e6acf`, so the tree is main + 72 commits and its `models/common/moe.py`, `test_moe.py` and `kimi_k3/moe.py`'s quantile part are main's.

Checks, both heads in `venv_bfx9` with `PYPRE=/workspace/pylib/attn_gym_main`:

- pyrefly on `kimi_k3/` and `config/transform/lora.py`: 10 errors, the same ten lines on `45ed0e3c1` and on `8e0ae8381` (`lora.py` 3 from the sharding-copy fix; `vision_encoder.py` `spec` / `local_type` may be uninitialized from `_gather_tokens`'s typed guard; 4639's `batch_invariant` kwarg and `delta_attention` config attribute; the DEP test double's `_FakeStage` list; `n_microbatches` on `ScheduleInterleaved1F1B`; the `_DataclassT` bound; `KimiK3Model.forward`'s override signature). Nothing new from the rebase. A full-tree pyrefly in `/venv/main` reads 102 errors, all from that venv lacking `torch_checkpointing` and its torch; not the tree's number.
- CPU, the whole `tests/unit_tests/cpu` directory with `--continue-on-collection-errors`: `8e0ae8381` 19 failed / 1153 passed / 5 collection errors; `45ed0e3c1` 25 failed / 1150 passed / 5 collection errors in the same run window. The 19 shared failures are upstream tests on this box (`test_aux_loss` 2, `test_config_manager` 2, `test_cp_attention` 2, `test_model_td_layout` 3, `test_quantization` 3, `test_qwen3_5_mrope_positions` 3, `test_rope` 1, `test_varlen_attention` 3), the same set on both heads. The 6 new-only: `test_embedding.py::test_vocab_parallel_padding_forward_backward` (#4637's new test imports `experiments/transformers_modeling_backend`, which needs `transformers`, absent from the venv) and five `test_parallel_dims.py::TestParallelDimsMeshOperations` cases, which pass 29/29 when the file runs alone; they failed while the two heads' suites ran concurrently (process-group tests on one box). The 5 collection errors are `transformers` (3 files) and `torch_checkpointing` (1) missing, plus `test_validate.py` (same import), on both heads. A first run of the new head alone read 20 failed / 1155 passed (the 19 plus the embedding test).
- GPU: `int0915_logs/smoke_int0916.sh` on `45ed0e3c1`, 16/16 rc 0 (`/workspace/.smoke_int0916/0916_084831/`, one cold cache per cell, a smoke and not a comparison):

| cell | rc | step 1 loss / grad norm | step 2 | step 3 |
| --- | --- | --- | --- | --- |
| dp1_compile | 0 | 12.48581/14.5000 | 9.62366/18.1250 | 7.58130/8.7500 |
| lora | 0 | 12.50891/1.1172 | 12.43127/1.1641 | 12.30818/1.0312 |
| dp1 | 0 | 12.46716/13.6875 | 10.13902/16.8750 | 7.60793/11.0625 |
| mtp | 0 | 16.30900/14.5000 | 12.80089/17.5000 | 9.51688/8.1875 |
| dp2_ep2 | 0 | 12.49904/14.4375 | 10.31914/20.2500 | 7.87312/10.2500 |
| muon_dp2 | 0 | 12.45613/14.6250 | 11.27078/16.0000 | 9.82880/14.7500 |
| cp2_allgather | 0 | 12.50934/27.1250 | 11.72113/42.5000 | 9.94040/24.3750 |
| pp2vp2 | 0 | 12.38647/18.7500 | 10.63894/15.5625 | 7.74313/11.3750 |
| region_ac | 0 | 12.48792/14.2500 | 9.98836/16.1250 | 7.84585/9.0625 |
| qlora_mxfp4 | 0 | 12.51109/1.1328 | 12.50259/1.0391 | 12.30175/0.9883 |
| mx_qat | 0 | 12.55269/13.8125 | 9.93897/14.1875 | 7.24821/10.6250 |
| mm_tp_ep_nosp | 0 | 12.48678/23.0000 | 11.43203/26.5000 | 10.08550/21.3750 |
| mm_tp_ep | 0 | 12.47950/23.3750 | 11.04744/26.8750 | 9.30246/18.7500 |
| cp2_ulysses | 0 | 12.38704/27.0000 | 11.34622/39.0000 | 9.84055/21.6250 |
| pp8vp4 | 0 | 12.46518/16.1250 | 10.98459/14.3125 | 8.84604/10.7500 |
| pp8vp4_vit_dep | 0 | 12.53553/15.8750 | 10.78561/19.8750 | 8.88453/12.0000 |

`k3_on_4025` moved `8e0ae8381` -> `45ed0e3c1` (force-with-lease), tag `k3_int_20260916c`; `/tmp/wt_int0916` and `/tmp/wt_int0916_cpx` on it; `/tmp/wt_int0916_rl` moved with its rl flavor patch re-applied (`int0915_logs/rl_flavor_0915.patch`). The seeded matrix of the morning (`8e0ae8381`) was not rerun on this head; the five upstream commits touch the embedding padding gradient (#4637), dist-GEMM (#4703, TP cells) and quantile routing (#4577, every MoE cell), so the matrix numbers of the morning are not this head's numbers; rerun it before any table cites this head.

## Seeded matrix on `45ed0e3c1` (2026-09-16, night): the morning's 16 cells plus the five CP-composition cells

`int0915_logs/run_mx4_int0916c.sh` (`matrix_scripts/mx4.sh`, tree `/tmp/wt_int0916` at `45ed0e3c1`, seed checkpoint `kimi_k3_debugmodel_49ffe72a4588`, 4096 tokens per step in 256-token micro-batches per dp rank, deterministic, one warm pass per cell then 10 measured steps on one shared inductor cache; the CP-composition cells through `cpmm_probe` with the tower cut at threshold 96), run `mx4_int0916c_0916_094907`, 21 / 21 seed-ok. The five upstream commits between the two heads touched the embedding padding gradient (#4637), dist-GEMM (#4703) and the quantile router's merged form (#4577); none of them moved a step-1 value, and only the two TP x PP cells moved at steps 3 / 10, on a fresh warm cache (the flavor's sensitivity on this box, `k3-step10-spread`). The morning's pairings hold: dp1 = pp2, fsdp2 = fsdp2 x pp2 = fsdp2 x cp2 x pp2's dp stream partner, tp2 = tp2 x pp2, ep2 x fsdp2 = fsdp2 x pp2 x ep2, cp2 x tp2 (allgather cut) = tp2 x cp2 x pp2 at step 1.

| cell | GPUs | seed | loss step 1 | step 3 | step 10 | against the morning run on `8e0ae8381` |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| dp1 | 1 | seed-ok | 12.52917 | 7.68933 | 3.33527 | identical, three steps |
| fsdp2 | 2 | seed-ok | 12.52622 | 7.65715 | 3.32807 | identical, three steps |
| tp2 | 2 | seed-ok | 12.56267 | 7.74384 | 3.37369 | identical, three steps |
| ep2_fsdp2 | 2 | seed-ok | 12.52679 | 7.50372 | 3.37598 | identical, three steps |
| tp2_ep2 | 2 | seed-ok | 12.54518 | 7.84336 | 3.50007 | identical, three steps |
| pp2 | 2 | seed-ok | 12.52917 | 7.70409 | 3.34125 | identical, three steps |
| fsdp2_pp2 | 4 | seed-ok | 12.52622 | 7.63718 | 3.35940 | identical, three steps |
| tp2_pp2 | 4 | seed-ok | 12.56267 | 7.84468 | 3.38123 | step 1 identical; steps 3 / 10 read 7.78131 / 3.36728 in the morning (its own warm cache) |
| fsdp2_tp2_pp2 | 8 | seed-ok | 12.55269 | 7.58214 | 3.36065 | step 1 identical; steps 3 / 10 read 7.58042 / 3.32105 in the morning (its own warm cache) |
| fsdp2_pp2_ep2 | 4 | seed-ok | 12.52679 | 7.66609 | 3.30835 | identical, three steps |
| fsdp2_tp2_ep2 | 4 | seed-ok | 12.53070 | 7.78067 | 3.41577 | identical, three steps |
| cp2_ag | 2 | seed-ok | 12.53324 | 7.58167 | 3.39545 | identical, three steps |
| cp2_ul | 2 | seed-ok | 12.53834 | 7.58215 | 3.30095 | identical, three steps |
| cp2_ep2 | 2 | seed-ok | 12.53176 | 7.72532 | 3.39586 | identical, three steps |
| cp2_fsdp2 | 4 | seed-ok | 12.52533 | 7.40680 | 3.31679 | identical, three steps |
| ep8_fsdp8 | 8 | seed-ok | 12.54717 | 7.96937 | 3.27699 | identical, three steps |
| cp2_tp2_ag | 4 | seed-ok | 12.55824 | 7.27533 | 3.43546 | identical, three steps |
| cp2_tp2_ul | 4 | seed-ok | 12.53918 | 7.52237 | 3.35178 | identical, three steps |
| fsdp2_cp2_tp2 | 8 | seed-ok | 12.52262 | 7.48441 | 3.37414 | identical, three steps |
| fsdp2_cp2_pp2 | 8 | seed-ok | 12.52668 | 7.65896 | 3.33817 | identical, three steps |
| tp2_cp2_pp2 | 8 | seed-ok | 12.55824 | 7.32999 | 3.39055 | identical, three steps |

These are the tree's numbers now; the Elfie plan (`Raising_PRs/PR_K3_PARALLELISM/ELFIE_VALIDATION_PLAN_2026-09-16.md`) cites this run.

