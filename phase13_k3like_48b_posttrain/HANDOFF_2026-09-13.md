# Handoff, 2026-09-13

Snapshot for whoever picks this up next (another box, a fresh session). Everything below was checked at the time of writing; SHAs from `git ls-remote origin` on the fork `QIU023/torchtitan`.

## 1. Branches

| branch | SHA | what it is |
| --- | --- | --- |
| `k3_pp_text` (PR 4312 head) = `pp_review4` | `dbc425403` | text-decoder PP; untouched today |
| `k3_tp_sp` (PR 4499 head) = `tp_sp_on_main` | `22eeec412` | TP/SP; the two now agree |
| `k3_on_4025` = `k3_int_20260912_cp` | `dddade302` | integration tree: main `56a721b64` + new TP + PP + all old k3_on_4025 features + CP (#4500 + packed MLA kernels) + the three type-checking fixes. Record: `K3_INT_20260912.md` |
| `k3_int_20260912` | `f201b9ac0` | the same without CP |
| `k3_ac_reuse_attention` | `6ef880995` | ac_reuse_attention + the AttnRes checkpoint wrapper + typing; the wrapper (`51b9c1999`) is the piece worth opening first (it is Kimi's "AttnRes wrapped in checkpointing") |
| `pp_fp64_probe` | `57f562aba` | probe only: `pp_review4` + the c4 probe commit + WIP float64 config (section 3) |

pytorch PR 194033: local branch `get-total-norm-dtype` = `1ad4f1623f` (rebased onto main `b8c2b362c4`, Jane's review addressed), fork still at `64ec4af36b`. **Force-push waits for the user's approval.** Backup ref `get-total-norm-dtype-pre-rebase-0913`; details and replies in `Raising_PRs/PR_pytorch_194033_grad_norm_fp32/`.

## 2. PP numerics on c4 (H100, running)

**Why c4.** The debug flavor's `cc12m-test` is 32 samples and is memorised by step ~20. The probe flavor `kimi_k3_debugmodel_c4` (in `pp4h_probe_c4.patch` / the first commit of `pp_fp64_probe`) feeds `c4_test` through the multimodal loader as text-only rows, each doc's first 256 tokens: 2000 rows, ~0.5M tokens; 100 steps read 21 % (1024 tokens/step) or 43 % (2048). No images, so `pixel_values` is None and the vision tower does not run (fine for PR 4312, a text-decoder PR).

**Box.** `ssh -p 33503 root@117.18.102.50` (4 x H100 PCIe, same model as the 09-12 runs). venv `/workspace/venv_pp`: torch `2.15.0.dev20260906+cu130`, torchvision `0.30.0.dev20260906`, spmd_types 0.2.5, torch_remat 0.2.0, Attention Gym `499404b` (the 09-12 versions). Tree `/workspace/tt_pp` = `dbc425403` + `pp4h_probe_c4.patch` (uncommitted). Kit `/workspace/kit` (= `matrix_scripts/tp_h100_v2/`). Launched as `TT=/workspace/tt_pp OUT=/workspace/out_c4 bash run_pp_c4.sh > /workspace/run_pp_c4.log`; it prints two tables and `RUN-PP-C4-DONE` at the end. **Stop the box when done** (billed).

**1024-token stream, done** (4 x 256 micro-batches, one seed checkpoint, 100 steps; loss / grad norm):

| cell | step 1 | step 10 | step 20 | step 50 | step 100 |
| --- | --- | --- | --- | --- | --- |
| dp1 | 12.60998 / 16.8750 | 3.42196 / 5.0625 | 3.02271 / 2.1406 | 2.66869 / 1.4609 | 2.53660 / 1.5703 |
| dp1, accumulation reversed (noise floor, no PP) | 12.60998 / 16.8750 | 3.38945 / 4.6875 | 3.06651 / 2.9531 | 2.71110 / 1.4141 | 2.56357 / 1.6797 |
| pp2 | 12.60998 / 16.8750 | 3.33437 / 3.2969 | 2.98862 / 2.2188 | 2.71135 / 1.6094 | 2.56287 / 1.5312 |
| pp2 x vp2, cache on | 12.60998 / 16.8750 | 3.31003 / 3.2812 | 3.03207 / 2.9375 | 2.69393 / 1.6328 | 2.56581 / 1.5781 |
| pp2 x vp2, cache off | 12.60998 / 16.8750 | 3.33143 / 3.3125 | 2.99671 / 2.4062 | 2.71330 / 1.7578 | 2.57117 / 1.5547 |

dp1's trajectory: 1:12.61 5:5.96 10:3.42 20:3.02 30:3.03 40:2.77 50:2.67 60:2.63 70:2.76 80:2.64 90:2.48 100:2.54 -- no collapse toward zero (cc12m-test reached 0.19). Small rises at 30 / 70 / 100 are step noise, not memorisation; whether steps 50 / 100 go into the PR body is the user's call under the numerics-table rule. At step 100 every pipeline cell sits +1.0 .. +1.4 % from dp1, the order-only noise floor +1.06 %: same class.

**Still running:** the 2048-token dp2 stream (dp2, dp2 x ep2, dp1 no-sync, dp2 x pp2, dp2 x pp2 x vp2 cache on / off). Next: read `/workspace/run_pp_c4.log` tables, copy logs into `phase13_k3like_48b_posttrain/pp_h100x4_c4_logs_2026-09-13/`, then update `Raising_PRs/PR_K3_PARALLELISM/PR_BODY_PP.md` (raw value, change against the reference beneath it; separate dp2 table with its token count) and the numerics replies.

## 3. float64 probe (local 5060, in progress)

User's instruction (2026-09-13): run the float64 probe on this box; branch off `pp_review4`, keep the diff as a patch. Branch `pp_fp64_probe`, worktree `/tmp/wt_fp64`; patches in `matrix_scripts/tp_h100_v2/fp64_probe/` (re-export after each commit). Done so far: the c4 probe commit, float64 allowed in `training.dtype` / `mixed_precision_param` / `mixed_precision_reduce` and `TORCH_DTYPE_MAP`.

To do (plan, env switch `TT_FP64_PROBE=1`, installed at the top of `train.py` `main()`):
1. Cast interception: ~50 hard-coded casts sit on the path (loss, RoPE, router `RouterGateLinear`, MoE, KDA, AttnRes `.float()`); patch `Tensor.float` / `.bfloat16` / `.half` and `Tensor.to(<float dtype>)` to float64 while the probe is on, instead of editing each site.
2. KDA: `l2norm` -> `attn_gym...naive.l2norm_fwd_ref` (same eps 1e-6); the recurrence -> `naive_recurrent_kda(..., cu_seqlens=...)` (restarts per document, computes in float64) or `chunk_kda(..., impl="reference")` -- the latter is **not verified yet**. **Trap found:** `bound_gate(impl="reference")` validates `A_log` as float32 and rejects float64 -- write the gate in PyTorch from the formula instead.
3. Short conv: `causal_conv1d` has no reference impl; write a per-document depthwise causal conv (`F.conv1d` with left pad W-1, groups=C, silu). **Tap order not verified yet** -- compare against the kernel in fp32 (stored vs flipped taps) before use.
4. flex attention: `FlexAttention._compiled_flex_attn = flex_attention` (eager works in float64; compiled fails in Triton).
5. MoE: `GroupedExperts._grouped_mm` -> per-expert matmul loop over `offs` (`torch._grouped_mm` rejects float64; K3 does not override the seam).
6. Chunked loss: `GradAccumulator(..., dtype=torch.float32)` -> float64.
7. Trainer: log loss and grad norm with full precision (`repr`) next to `metrics_processor.log`; the 4-decimal line cannot show float64 differences.
Run plan: one float64 seed; dp1, dp1 reversed, pp2, pp2 x vp2 cache on / off at 1024 tokens, 20 steps, `--training.dtype float64 --training.mixed_precision_param float64 --training.mixed_precision_reduce float64`, venv `/workspace/venv_bfx9`, JIT/tmp dirs under `/tmp/jitwarm/<run>`. Expectation if there is no bug: step-1 loss / grad norm equal to ~1e-15 relative and trajectories that stay together far longer than in bf16.

## 4. PR 4312 drafts waiting for the user to post

- `REPLY_4312_ACT_MEMORY_2026-09-12.md` (reply to 3976976576): per-stage forward and backward tables for 2 x 4 pp2 x vp2 and 4 x 4 pp4 x vp4, generated from `BlockLayoutTables` at `dbc425403` (deposits == collections: 3 and 27).
- `REPLY_4312_NUMERICS_SHORT_2026-09-12.md`; `PP_EMPTY_PAYLOAD_NOTE_2026-09-13.md` (an empty payload still posts a 0-byte forward send; dropping it is not worth the hang risk now).

## 5. CP on the integration tree (done, open items)

CP runs need Attention Gym main: `PYTHONPATH=/workspace/pylib/attn_gym_main` (the venvs keep 0.0.8 so PP / TP numerics do not move). Type checking passes for all eight CP / TP / fsdp cells (`K3_INT_20260912.md`). Open: CP x TP and CP x PP are refused, not measured; #4500's own KDA GPU tests need SM90/SM100 (H100).

## 6. Rules added today (CLAUDE.md / memory)

Chat replies in Chinese; drafts live in md files, never pasted into the chat; drafts speak as the author (no we / our / us); never report steps past the reference's memorisation. Local box: the disk watchdog prunes top-level `/tmp/triton_*`, `torchinductor_*`, `torchelastic_*` below 40 GB free -- put JIT and TMPDIR under `/tmp/jitwarm/<run>`.
