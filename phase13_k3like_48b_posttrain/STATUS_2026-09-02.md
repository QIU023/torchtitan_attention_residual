# Status 2026-09-02 (end of the overnight push)

## torchtitan PR line

| PR / branch | state | evidence |
|---|---|---|
| EP #4314 `k3_ep` = `69f84292d`; `ep_review1` = `cd8856107` | approved; GitHub shows dirty against main 8792c3a24 (21 commits); `ep_review1` carries one merge commit resolving the only conflict (`kimi_k3/__init__.py`: upstream's `seq_len` on model_registry and the `(builder, max_context_len)` registry tuple, kept together with `moe_comm_backend`) plus tianyu-l's rename `set_expert_parallel_sharding_config` -> `set_kimi_k3_sharding_config`; CPU: 37 passed; pushed to `k3_ep` (fast-forward) on the user's word, GitHub now reports mergeable with CI running | before/after dp1/dp2/ep2 from one seed (69f84292d vs the merge): bitwise on all three (dp1 12.52977 / 7.27107 / 2.98077, dp2 12.53137 / 7.31248 / 3.15823, ep2 12.53146 / 7.20212 / 3.10296); two upstream MoE commits in the range (grouped-mm seam takes the weight untransposed, shape suffixes) are the reason to check |
| TP/SP `tp_review1` (4 commits on `k3_ep`) | rewritten on main's attn-gym KDA: head-parallel MLA and KDA behind a `local_map` on `inner_kda`, SP overlay, grad-norm mesh grouping, latent-MoE norm under SP | dp1 bitwise with main; tp2 12.55057 / 7.52677 / 3.00361; tp2+SP 12.54164 / 7.44412 / 3.08160 |
| AC `ac_review1` (2 commits on main) | residual math under checkpoint; `ac_reuse_attention` | both bitwise with main; 12.68 / 12.81 GiB |
| PP `pp_review1` = `1c0c1416c` | merge + format + cache offload + stage-aware HF adapter + pp_balance; review branch only, `k3_pp` untouched | offload pp2 x vp2 bitwise; pp_balance transport exact, allocated peak unchanged at debug width (why: `PR_RESULTS_2026-09-02.md`) |
| spmd_types on `k3_on_4025` = `9f87e0891` | backend branch, dense declarations under spmd_types, invariant-vs-replicated weight types, stream conversions, tower invariant with a cp axis; `--debug.spmd_typechecking` passes for tp2 | dp1/dp2 bitwise with partial_dtensor (seeded 10 steps); seeded 10 steps: tp2 spmd 12.47125 / 7.22017 / 3.10949 vs partial 12.47604 / 7.27227 / 3.06511 (dp1 12.46442 / 7.27823 / 3.07888); cp2 spmd 12.44640 / 7.37571 / 3.10927 vs clean imperative 12.44640 / 7.39556 / 3.09412 |
| spmd_types on the attn-gym base `spmd_review1` = `4b88ada6b` (on `tp_review1`) | the same declaration fixes ported; dp1/dp2 bitwise with partial_dtensor over 10 seeded steps, tp2 12.55332 / 7.38015 / 3.00522 vs 12.55057 / 7.52677 / 3.00361 | landing base per the user; fla base gets no more work |
| CP `cp_review2` = `84fd3ee22` (attn-gym port + spmd + declarative Ulysses); `cp_review1` still the fla-KDA branch, the force-push that would replace it was blocked and is left to the user | attention-gym PR 453's `context_parallel_kda` recipe in the `inner_kda` body (unmerged upstream); Ulysses is now the MLA inner attention's declaration, so CP requires spmd_types and the `_validate_cp_backend` override is gone; `kimi_k3_cp2` flavor selects spmd_types | cp2 12.53972 / 7.18344 / 2.93330 vs dp1 12.52977 / 7.27107 / 2.98077; dp2 x cp2 and tp2 x cp2 (no SP) run with the seed; spmd_types probed on `spmd_probe`: dp1/dp2/ep2/tp2 train (dp1/dp2/ep2 bitwise with partial_dtensor), cp2 reaches the MLA kernel where the imperative Ulysses collides with the declared all-gather-KV; KCP's local_map identity pair already holds; 34 tps at 256-token micro-batches is FSDP2 folded into the cp axis plus rank skew, the recipe itself is 15% at 2048 tokens per rank (cp2 509 vs dp2 601) |
| QB `k3_qb` | fileable, independent of EP | dp1/dp2 with sign-step control rows |
| LoRA `k3_lora_extras` | fileable; leaked QAT flavor removed | lora / qlora dp1, dp2 |
| QAT `k3_qat` | on `QuantizationConverter`, exported; deferred (`QAT_UPSTREAM_AUDIT_2026-09-02.md`) | draft body with the relation section |
| MTP `k3_mtp` | merged main, one import resolution | 2 CPU tests |
| CP `cp_review1` | Ulysses half reviewable; KCP waits on attention-gym promoting `context_parallel_kda` (kernels are portable, the orchestration lives in `examples/`, which the wheel does not ship) | -- |
| torchtitan #4374, attention-gym #421 | premise removed by attention-gym #442; to be closed | dp1/dp2 on main with the whitelist lifted |

Bodies: `Raising_PRs/PR_K3_PARALLELISM/`, every landed row pasted in. Ledger: `PR_RESULTS_2026-09-02.md`. Map: `OVERNIGHT_2026-09-02.md`.

## veRL (fork `kimi_k3_integration` = `dbc08fd7`)

Pipeline parallelism in the torchtitan engine is new: one pipeline microbatch per stage, verl micro-batches fed in chunks with zero-loss padding and padded to `VERL_PP_TOKEN_BUDGET` ahead of the CP split, verl's loss reaching the last stage through a bridge on the schedule, a tolerant data-parallel mesh lookup. `rollout_probs_diff_max` per cell, three steps each, MoE debug model, vLLM 6dc76a9ad:

| cell | max gap |
|---|---|
| fsdp2 / x ep2 / x cp2 (4 GPUs) / pp2 | 6.7e-4 / 7.1e-4 / 5.3e-4 / 2.6e-4 |
| QAT x dp2 / x ep2 / x cp2 / x pp2 | 7.1e-4 / 7.2e-4 / 5.3e-4 / 3.3e-4 |
| QAT x ep2 x cp2 x pp2 (8 GPUs) | 3.3e-4 |

Closed on the verl side: offload under PP was a masked OOM (fix `261594b7`: reshard on exit, `reset_iter_state` on an exception); pp2 with offload passes at the 1024-token budget (2.57e-4 / 2.40e-4 / 1.77e-4); TP is in the ladder (tp2 3.66e-4 / 4.44e-4 / 5.34e-4 at micro-batch 2; micro-batch 4 ran out of memory in the entropy pass over the gathered logits); dataloader workers are killed at teardown (cosmetic).

## pytorch PR-194033

Rebased onto main `a6fc3c2959` (710 commits, no conflicts); the two reviewer-requested regression tests added and run on nightly 2.15.0.dev20260902 (8 + 2 passed). Not pushed; the branch is backed up as patches in `Raising_PRs/PR_pytorch_194033_grad_norm_fp32/`.

## Decisions waiting on the user

1. sync `k3_pp` from `pp_review1` (carries pp_balance)
2. force-push the pytorch branch
3. QAT: fold into PR-3889 or file the draft
4. close #421 / #4374
5. attention-gym: `context_parallel_kda` + `PackedShard` promotion (asked)

## Environment fixes made today

Seed caches renamed `seed_ckpt` and excluded from the disk pruner (it had swept them); the verl runner prints `rc=` last so chained cells can wait on it; `VERL_VLLM_VERSION` bypasses verl's version gate for the source-built vLLM; `venv_verl`'s torch DCP and `ray/data/checkpoint` are deleted by uninstalling the old vLLM dev package (its RECORD lists them) -- exact reinstalls repair it.


## 2026-09-04 morning (overnight results)

| branch | head | note |
|---|---|---|
| `cp_review2` (fork) | `adc012ce4` | kernel-owned Ulysses/KCP, SP guard, all-gather KV kernel; gradients sha1-identical to the declarative tree on a shared cache |
| `pp_review2` (fork) | `ca5f34ea8` | one-line review fixes, transport switch on the pipelining entry, pure split function, uneven splits |
| PR branches `k3_cp_text` / `k3_pp_text` | unchanged | nothing pushed to the PRs |

Answers and design notes: `REVIEW_ANSWERS_PP_CP_2026-09-04.md`.

| `pp_review2` (fork), 2026-09-03 late | `3af70c9ee` | plus the cat-at-the-start refactor, per-micro-batch release, the irregular 30-layer debug model, and design B: the adapter and its wrappers replaced by the `AttnResPipelineStage` subclass (`pipeline_llm(stage_class=...)`); matrix rerun on the 30-layer model |

## 2026-09-04 (local): the stack rebased onto post-EP main

EP PR 4314 merged (`9b5f60c40`). New fork branches, all on that commit, PR branches untouched: `tp_review2` `80d7e0951` (TP/SP, 4 commits) -> `spmd_review2` `8955c0778` (+2 declaration commits) -> `cp_review3` `e63f54d6b` (+1 commit: the whole CP content of `cp_review2`); `pp_review3` `af8bd2cf5` (PR head squashed as `a4d68655c` + the 9 review commits, design B on top); `qb_release` `0902c7a24` (clean). CPU tests: 31 pass on `cp_review3`, 12 on `pp_review3`, 14 on `qb_release`. Conflict resolutions and what was dropped: `REVIEW_ANSWERS_PP_CP_2026-09-04.md` section 5. PR bodies rewritten for the new heads: `Raising_PRs/PR_K3_PARALLELISM/PR_BODY_PP.md` (design B) and `PR_BODY_QB.md` (release title). GPU sanity on the rebased trees: `matrix_scripts/rebase_main_{pp,cp,qb}.sh` (run worktrees carry the uncommitted SM120 guard lift; results below when they finish).

PP on the rebased tree (step 1 / 3 / 10): dp1 12.51030 / 7.39629 / 3.49625; pp2 x vp4 12.51030 / 7.45319 / 3.38121; pp4 x vp4 12.51030 / 7.44880 / 3.57213; pp8 x vp4 12.51030 / 7.40443 / 3.47327; pp8 x vp4 whole-stack 12.51030 / 7.45462 / 3.45668. QB: see `PR_BODY_QB.md`. CP on `cp_review4`: cp2 packed / generic Ulysses 12.53972 / 7.18344 / 2.93330 vs 12.53972 / 7.18619 / 3.00631; packed / generic all-gather 12.53972 / 7.22178 / 3.09487 vs 12.53972 / 7.21651 / 3.08671; tp2 x cp2 12.55243 / 7.19853 / 3.06006 (`rebase_main_cp4.sh`).

`cp_review4` `f6ca064be` (2026-09-04): the CP branch on fegin's CP stack (scratch base `cp_base_stack` `a8a6f331f`), packed MLA kernels restored on 4450's interface, the KDA transform in `torchtitan_recipes/kimi_k3.py`; `REVIEW_ANSWERS` section 5.1, PR body `PR_BODY_CP.md`, reply drafts `REPLY_4313_2026-09-04.md`. GPU: packed-vs-generic cells queued (`matrix_scripts/rebase_main_cp4.sh`).
