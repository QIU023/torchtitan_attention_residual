# Integration tree rebuilt on PP round 3 (2026-09-22, overnight)

Goal (the user's, 2026-09-21 evening): replace the tree's PP (4312's old head `de6f29514`, one squash at the bottom of `k3_on_4025`) with 4312 round 3 (`k3_pp_text` = `498dc5731`, 42 commits on main `63c4e9fef`), re-stack the 81 commits above it (transport, DEP, pp_balance, the rl flavors, CP x PP, LoRA), CPU tests and the 21 cell mx4 matrix against the 09-17 step-1 baseline; then rebase onto `63c4e9fef` (#4808's fused FFN layout) and run the matrix again; then the veRL engine cells on the new tree and the split.

## Step 1: the PP swap on base `a3a819c67`

Worktree `/tmp/wt_int0922`, detached at `a3a819c67`. The round 3 change applied as one diff (`git diff 63c4e9fef 498dc5731`, 3-way) as the bottom commit, then the 81 commits cherry-picked in order; commits the round already carries (`7dedac883` has_backward, `43b49efe4` payload without trainable upstream) are dropped.

API map for the ports: `kimi_k3/pipeline_stage.py` -> `kimi_k3/pipeline_parallel/stage.py`, `kimi_k3/layout.py` -> `pipeline_parallel/layout.py`, the entry `pipeline_kimi_k3` from `parallelize.py` -> `pipeline_parallel/__init__.py`; `_routing()` -> `layout()` / `store()`; `infer_block_layout_tables_from_stages(stages, ...)` -> `infer_block_layout_tables(...)`; core `pipeline_with_first_stage_modules` -> `pipeline_with_first_last_stage_modules` (`return_split=True` hands the split back; `llm_split_with_stage_modules` gone); config `module_fqns_per_model_part` -> `pipeline_parallel_module_fqns_per_model_part`.

### Outcome of the cherry-pick (2026-09-21 ~10:15 UTC)

The round 3 diff applied onto `a3a819c67` without conflict (bottom commit "integration: text-decoder pipeline parallel (k3_pp_text 498dc5731, as one change)"). Of the 81 commits above the old squash, 75 landed and 6 were skipped as already carried or obsolete: the observability logger and the typing-import fixups of the old entry, the two stage fixes round 3 has (`has_backward`, the payload with nothing trainable upstream), the typed transport stage class, the unused-import drop. Recurring conflict: every commit that had touched the old entry brought the whole entry back into `parallelize.py`; resolved by keeping HEAD and only a commit's new top-level definitions (`matrix_scripts`-side helper `resolve_par.py` in the scratchpad, kept with the kit). Ports into `kimi_k3/pipeline_parallel/__init__.py`: the neighbor transport composed into the rebuilt stage class, `attn_res_cache_offload` on the store, `pp_balance`, and DEP (`vit_dep_split` through core's `_get_pipeline_metadata` / `_generate_llm_fqn_per_model_part`, the runtime install moved into the package; the cell is `kimi_k3_pp4_vp4_vit_dep`, 16 stages with the tower and the embedding alone on the first). The flavor keeps `kimi_k3_full_attention_layers` and the `num_layers` / `num_mtp_layers` knobs from the MTP port at the 17 layer, dim 256 defaults; the 33 layer entry is gone. Three functions the original tree had already removed came back through the resolver and were removed again; `parallelize.py`'s remaining functions are identical to the original tree's (AST compare). Tests: the remat and recompile-limit tests read the flavor's shape (dim 256; 10 layer variants for 17 layers). CPU: the K3 / CP / transform / LoRA / MoonEP / pipeline subset 197 passed, the only failure the box's missing `flash_attn`. Tag `k3_int_20260922a` = `9dc5757ea`; the previous head is `k3_int_20260918_pre_pp3` = `42691735c`. The matrix `run_mx4_int0922.sh` (pp2 x vp2 cells as CLI flags on the shared flavor, since the recipe is gone) runs with a fresh seed cache: the flavor changed shape, so the 09-17 step-1 numbers are not comparable; the comparison is within the run (PP cells against dp1 / fsdp2 / tp2 at step 1).

### Step 1 matrix (2026-09-21 20:50 UTC): 21 / 21 on `k3_int_20260922a`

`logs_int0922/mx4_int0922a_results.txt` (8 x 5060 Ti, `/workspace/venv_bfx9`, seed cache `.mx4_seeds_0922`, the shared 17 layer flavor at 4096 tokens per step, 256 per microbatch). Every cell completes 10 steps. Step 1 is read against the cell that sees the same data stream on the same reduction: `pp2` = `dp1` (8.05722), `fsdp2_pp2` = `fsdp2` (8.06545), `tp2_pp2` = `tp2` (8.05712), `fsdp2_pp2_ep2` = `ep2_fsdp2` (8.06545), `tp2_cp2_pp2` = `cp2_tp2_ag` (8.05713), all equal at the printed five decimals; `fsdp2_tp2_pp2` (8.06520) and `fsdp2_cp2_pp2` (8.06541) have no non-PP twin in the matrix (their fsdp2 x tp2 / fsdp2 x cp2 neighbours read 8.06529 / 8.06549, the flavor's own step-1 spread across reduction orders). `ep8_fsdp8` is dp8 data, its own row. The 09-17 numbers are not compared (other flavor shape). `k3_on_4025` moves to `9dc5757ea` (tag `k3_int_20260922a`) on this result.

## Step 2: rebase onto `63c4e9fef` (#4794 trainer refactor, #4808 stacked w13)

Worktree `/tmp/wt_int0922b`, 79 commits over `63c4e9fef` after the rebase (conflicts: the CP refactor against main's `prepare_context_parallel_input`, the flux trainer, the attn-gym pin (one pin, main's 0.0.10), the feed_forward hooks (main's, the split moved into the adapter), the compile refusal). Tag `k3_int_20260922b` on the commit "integration: the LoRA transform on main's stacked w13, and two rebase slips".

What #4808 broke and how it is met (2026-09-21 21:00 UTC): main stores `w13` as `[2, F, D]` (`Linear(num_linears=2)`) and drops the split hooks; the packed MXFP4 base read `out_f, in_f = w.shape` and the LoRA linear built a 2-D `lora_b`, so the QLoRA recipe failed at build and the plain LoRA recipe's `w13` forward failed at the add. The LoRA linear now builds its adapters through main's `_LoRALinearMixin` (stacked `lora_b`, the mixin's adapter sharding, the R/I branch our tree had already added there), the packed pair keeps the weight's leading shape, the packed forwards flatten the stack for the matmul as `Linear` does, FSDP shards every matrix-shaped parameter of a stacked Linear along F (the packed pair included; main's rule read `child.weight`, which a packed module no longer has). The `w1` / `w3` split of packed keys that item 8 of the veRL plan relied on is gone with the hooks; the merged dict carries `w13.weight` `[2, F, D]` and main's adapter splits it on `to_hf`. Two rebase slips: the batch-invariant import in `kimi_k3/model.py`, and `compile_config.enable` where main made the config optional (the cell crashed in `parallelize_kimi_k3` until guarded like main). CPU on the whole `tests/unit_tests/cpu` tree (venv_bfx9): 1170 passed, 19 failed, 6 collection errors; the failures are the box's upstream baseline (config_manager, cp_attention, embedding, model_td_layout, quantization, qwen3_5_mrope, rope, varlen_attention; the same on the step-1 tree) plus one stale test of ours fixed in the commit (adapter stages test at dim 1024); the collection errors are files importing `transformers` / flux.

Matrix mechanics on this base: `--checkpoint.*` is gone from the CLI (the checkpointer is a component, `create_seed_checkpoint` suppressed), so `mx4b.sh` launches every cell through the scratch wrapper module `mx4ckpt` (`--module mx4ckpt --config cell|seed`, the base flavor named by `MX4_BASE_MODULE` / `MX4_BASE_CONFIG`), which attaches `CheckpointManager.Config(interval=100000)` to a cell and `CheckpointManager.Config()` + `create_seed_checkpoint` to the seed; the seed-load assertion is unchanged. A stale `sitecustomize.py` probe (2026-08-21, a read-only print of norm placements) sat on the scratch path the matrices put first on PYTHONPATH; it was harmless on the step-1 tree (it only reads `Trainer.model_parts`) and crashed every cell here; moved aside. Smoke on the seed: `dp1` step 1 = 8.15339 (the step-1 tree read 8.05722; #4808 changes the fused weight's init draw, so the two bases are not compared). The 21 cells run as part A (16 cells of at most 4 GPUs on GPUs 0-3, `run_mx4_int0922b_partA.sh`) and part B (the five 8-GPU cells, after part A and the 4-GPU veRL cells), so the veRL cells run on GPUs 4-7 meanwhile.

veRL on this tree: the rl flavor export `/root/models/kimi-k3-debug-nt-0922b` (12 layers, dim 1024, from `export_rl_newtree.py` on `/tmp/wt_int0922b`; 1428 tensors, `A_log` in the released `[1, 1, H, 1]` layout straight from the adapter, so no `-rel` step; the other files symlinked from `kimi-k3-debug-nt`); runners `verl_grpo_int0922b.sh` (text) and `verl_grpo_k3_image_int0922b.sh` (image) point at the tree and that export.

### #4794 on the tp cells (2026-09-21 21:30 UTC)

Part A's `tp2` cell died at trainer construction: `MoE models require expert_parallel_degree (1) to be greater than or equal to tensor_parallel_degree (2)` (`Decoder.Config.update_from_config`, main's `610bb6f6b` "[MoE] Deprecate pure TP on routed experts", #4794, 2026-09-20; `a3a819c67` still ran pure TP on the experts). Every tp cell on this base therefore carries `--parallelism.expert_parallel_degree 2`: part B's three 8-GPU tp cells were edited before launch, part A's three 4-GPU tp cells (`tp2_pp2`, `cp2_tp2_ag`, `cp2_tp2_ul`) run again as part C (`run_mx4_int0922b_partC.sh`), and the pure `tp2` row is the same cell as `tp2_ep2` and is not run twice. Their numbers are not compared with tp cells measured on earlier bases (the experts changed sharding). Memory: `moe-tp-needs-ep-after-4794`.

veRL engine on this base (21:10 UTC): the first cell died in the worker at `CheckpointManager.Config.__init__() got an unexpected keyword argument 'enable'`. The engine (branch `kimi_k3_integration_rebased`, `/tmp/wt_verl_new`) now builds the checkpointer without `enable` when the component has no such field and passes it as `checkpointer` when `Trainer.Config` names it so, builds `CompileConfig()` or `None` when the tree's compile config has no `enable`, and reads `model_parts` / `optimizers` / `lr_schedulers` / `pp_schedule` / `checkpointer` / `parallel_dims` / `train_context` off `trainer.engine` when the trainer has one (#4794's `TrainingEngine`), the trainer itself otherwise. The engine's own unit tests: 63 passed against the tree.

### Step 2 matrix, part A (2026-09-21 21:10 to 22:05 UTC): 12 / 16, the four tp cells refused by #4794

`logs_int0922/mx4_int0922b_partA_results.txt` (GPUs 0-3, seed cache `.mx4_seeds_0922b`, `mx4b.sh`). Step 1 against the same-data twin: `pp2` = `dp1` (8.15339); `fsdp2_pp2` = `fsdp2_pp2_ep2` = `ep2_fsdp2` = `fsdp2` (8.11933); `cp2_ep2` = `cp2_ag` (8.15329); `cp2_fsdp2` (8.11948), `fsdp2_tp2_ep2` (8.11963), `tp2_ep2` (8.15325), `cp2_ul` (8.15326) have no twin and read within the flavor's own step-1 spread of the reduction order, as on the step-1 tree. `tp2`, `tp2_pp2`, `cp2_tp2_ag`, `cp2_tp2_ul`: no step, `ASSERT-SEED-FAIL` from `Decoder.Config.update_from_config` (#4794, see above); the last three run again as part C with `ep2`, `tp2` alone is the `tp2_ep2` row. Every step-10 value is within 0.2% of its twin.

### Step 2 matrix, part C (22:00 to 22:40 UTC): the three 4-GPU tp cells with ep2, 3 / 3

`logs_int0922/mx4_int0922b_partC_results.txt`. `tp2_pp2` = `cp2_tp2_ag` = `tp2_ep2` at step 1 (8.15325: the same data stream and reductions, the pipeline and the all-gather CP change nothing at step 1); `cp2_tp2_ul` reads 8.15306, its own row (Ulysses changes the reduction). Part B (the five 8-GPU cells, tp ones with ep2) starts when the veRL chain frees GPUs 4-7.

### veRL engine cells on the rebased tree (21:17 to 23:13 UTC): 6 / 6

`logs_int0922/verl_cells_int0922b_results.txt`, runner `verl_cells_int0922b.sh` (GPUs 4-7, `/tmp/wt_verl_new` at the engine port plus two test guards, torchtitan `/tmp/wt_int0922b`, export `kimi-k3-debug-nt-0922b`, three GRPO steps, synthetic reward). Rollout-vs-actor log-prob diff mean at steps 1 / 2 / 3: `fsdp2 x pp2` 0.159 / 0.116 / 0.111; `pp2 x cp2` 0.157 / 0.116 / 0.118; `fsdp2 x pp2 x ep2` 0.158 / 0.118 / 0.116; the save / resume pair (fsdp2): run A (`save_freq=2`, two steps) 0.159 / 0.150 and `global_step_2` on disk, run B (`resume_mode=auto`, four steps) logs `Resuming from .../global_step_2, setting global step to 2` and reads 0.144 / 0.146 at steps 3 / 4; the image cell `fsdp2 x pp2 x ep2` on the 8-row image set 0.166 / 0.101 / 0.095 with `prompt_length/mean` 111 (the expanded media block, so the images were carried). All `rc=0`; the only tracebacks are the DataLoader's exit-time noise. The first attempt died before step 1 on `CheckpointManager.Config(enable=...)` (the port above); everything else ran unchanged. Left on GPUs: item 8 (QLoRA packed w13 sync, `FSDP_SIZE=8`) after part B releases the eight cards.

### Item 8 on this base, prepared (2026-09-21 23:55 UTC)

The 09-19 failure ("KimiK3StateDictAdapter found TorchTitan keys without a mapping: w1.base_qdata ...") cannot recur here: #4808 removed the split hooks, so a packed model's state dict carries `w13.base_qdata` `[2, F, D/2]` and `merge_lora_state_dict` hands back `w13.weight` `[2, F, D]`, which main's adapter splits into `gate_proj` / `up_proj` on `to_hf`. Checked on CPU under venv_verl's torch: the packed DCP loads strictly into `kimi_k3_rl_qlora_mxfp4` (623 keys, 94 packed pairs), the merge yields 407 dense keys with no packed or adapter key left, `to_hf` gives the export's 1428 keys with nothing unmapped. The packed DCP `/workspace/.qlora_rl_packed_0922b` comes from `make_qlora_src_0922b.py` (the rl LoRA flavor at its seed-42 init, model-only flat keys, written by the engine venv's torch 2.14 so no cross-version rewrite) through `scripts/quantize_lora_dcp.py`, which now packs each pair in the packed model's own shape (tree commit "integration: quantize_lora_dcp packs each pair in the packed model's own shape"; it used to flatten every weight to `[rows, cols/2]`, which no longer loads into a stacked base). The GRPO cell (`verl_item8_int0922b.sh`: `FSDP_SIZE=8`, `initial_load_path` on actor and ref, `model.lora.merge=True`, `RAY_CPUS=48`) runs once part B releases the eight cards.

### Step 2 matrix, part B (23:13 to 23:32 UTC): the five 8-GPU cells, 5 / 5; `k3_on_4025` moves

`logs_int0922/mx4_int0922b_partB_results.txt`. `fsdp2_tp2_pp2` (with ep2) = `fsdp2_tp2_ep2` at step 1 (8.11963); `tp2_cp2_pp2` (with ep2) = `tp2_ep2` = `cp2_tp2_ag` (8.15325); `ep8_fsdp8` (8.05535, dp8 data), `fsdp2_cp2_tp2` (8.11975) and `fsdp2_cp2_pp2` (8.11959) are their own rows. `fsdp2_cp2_pp2` is not `cp2_fsdp2`'s twin, on this base or any earlier one (8.06541 vs 8.06549 on the step-1 tree, 12.52668 vs 12.52533 on 09-16 / 09-17): the pp cell runs the `_min96` recipe, which lowers the dynamic-CP threshold so the 192-patch debug image is cut over the cp ranks, while `cp2_fsdp2` keeps the default threshold and the tower replicated. The matrix on this base is therefore complete: 20 distinct cells (the pure `tp2` row is `tp2_ep2` under #4794), every cell reaching step 10, every PP cell equal to its same-data twin at step 1.

`k3_on_4025` = tag `k3_int_20260922c` = `28cc50565` (the `k3_int_20260922b` tree plus "integration: quantize_lora_dcp packs each pair in the packed model's own shape"), pushed; the worktree `/workspace/tt_4025/torchtitan` sits on it.

### Item 8 on the stacked base (2026-09-22 00:22 UTC): passes

`verl_item8_int0922b.sh` (eight cards, `FSDP_SIZE=8`, `kimi_k3_rl_qlora_mxfp4`, `initial_load_path` = the packed DCP above, `model.lora.merge=True`, `RAY_CPUS=48`): three GRPO steps, `rc=0`, rollout-vs-actor log-prob diff mean 0.156 / 0.145 / 0.145, pg loss 0.0634 / 0.0217 / 0.0158, grad norm 0.0811 / 0.0833 / 0.0832 (adapters only, the fused projection's adapter in the set; the 09-16 run on the old layout read 0.0866 / 0.0920 / 0.0919 and a diff of 0.147 / 0.150 / 0.151). The merged sync's witness is the `SYNC-CHECKSUM` stderr line (the engine logger's warnings never reach Ray's logs): every sync digests `layers.0.feed_forward.w13.weight`, a key only the merge produces, and the digest changes from sync to sync (60a89fe0, 327d2e84, 01668228), so the merged, moving weights reached the rollout. On CPU under venv_verl the engine recognises the packed LoRA linear (`_is_lora_wrapper` True, the class now carries main's `_LoRALinearMixin`), `_merged_state_dict_if_lora` returns 407 keys with 94 moved and no packed key, and `to_hf` on the raw packed state dict still raises the 09-19 error, which is what the merge avoids. So the item that failed on 09-19 with unmapped `w1.base_qdata` keys passes on this base without an adapter change: the split hooks are gone with #4808, the merge yields the stacked dense `w13.weight`, main's adapter splits it. Only the DataLoader's exit-time traceback, as in every cell.

## Where the night ends (2026-09-22 00:30 UTC)

Done: steps 1 and 2 of the plan with their tags (`k3_int_20260922a`, `k3_int_20260922b`, `k3_int_20260922c`), `k3_on_4025` on `28cc50565` (main `63c4e9fef`), both matrices complete (21 / 21 on the step-1 tree, 20 distinct cells on the rebased one, PP cells equal to their same-data twins at step 1 everywhere), the veRL engine ported to main's API with seven cells passing (the six planned ones and item 8), the split kit regenerated on the veRL head `e3f2ccfb` with remap tooling and a two-tree verification. Left for the user: item 6 (the design decision), items 11 / 12 (a card). Left as findings, not fixed tonight: the tree's CP API diverges from main (`prepare_context_parallel_batch` vs main's `prepare_context_parallel_input` export, the per-type `ContextParallelTransform`, the balancer dataclass), which is what upstream verl's base engine and the CP PR line depend on from opposite sides; `kimi_k3/model.py` carries an unused `dense_activation_placement` import (pyflakes); `test_torchtitan_engine_cp_config.py`'s stage-04 variant is flex-only now, so PR 4 of the veRL split no longer asserts patch 5's KDA mapping. Worktrees: `/tmp/wt_int0922` (step 1), `/tmp/wt_int0922b` (step 2, detached at `28cc50565`), `/tmp/wt_verl_new` (`e3f2ccfb`), `/tmp/tt_main_63c4` (a plain extract of upstream main for the split's stage tests, not a worktree). The eight GPUs are free.
