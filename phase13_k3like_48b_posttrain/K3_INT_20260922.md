# Integration tree rebuilt on PP round 3 (2026-09-22, overnight)

Goal (the user's, 2026-09-21 evening): replace the tree's PP (4312's old head `de6f29514`, one squash at the bottom of `k3_on_4025`) with 4312 round 3 (`k3_pp_text` = `498dc5731`, 42 commits on main `63c4e9fef`), re-stack the 81 commits above it (transport, DEP, pp_balance, the rl flavors, CP x PP, LoRA), CPU tests and the 21 cell mx4 matrix against the 09-17 step-1 baseline; then rebase onto `63c4e9fef` (#4808's fused FFN layout) and run the matrix again; then the veRL engine cells on the new tree and the split.

## Step 1: the PP swap on base `a3a819c67`

Worktree `/tmp/wt_int0922`, detached at `a3a819c67`. The round 3 change applied as one diff (`git diff 63c4e9fef 498dc5731`, 3-way) as the bottom commit, then the 81 commits cherry-picked in order; commits the round already carries (`7dedac883` has_backward, `43b49efe4` payload without trainable upstream) are dropped.

API map for the ports: `kimi_k3/pipeline_stage.py` -> `kimi_k3/pipeline_parallel/stage.py`, `kimi_k3/layout.py` -> `pipeline_parallel/layout.py`, the entry `pipeline_kimi_k3` from `parallelize.py` -> `pipeline_parallel/__init__.py`; `_routing()` -> `layout()` / `store()`; `infer_block_layout_tables_from_stages(stages, ...)` -> `infer_block_layout_tables(...)`; core `pipeline_with_first_stage_modules` -> `pipeline_with_first_last_stage_modules` (`return_split=True` hands the split back; `llm_split_with_stage_modules` gone); config `module_fqns_per_model_part` -> `pipeline_parallel_module_fqns_per_model_part`.

