# Integration tree rebuilt on PP round 3 (2026-09-22, overnight)

Goal (the user's, 2026-09-21 evening): replace the tree's PP (4312's old head `de6f29514`, one squash at the bottom of `k3_on_4025`) with 4312 round 3 (`k3_pp_text` = `498dc5731`, 42 commits on main `63c4e9fef`), re-stack the 81 commits above it (transport, DEP, pp_balance, the rl flavors, CP x PP, LoRA), CPU tests and the 21 cell mx4 matrix against the 09-17 step-1 baseline; then rebase onto `63c4e9fef` (#4808's fused FFN layout) and run the matrix again; then the veRL engine cells on the new tree and the split.

## Step 1: the PP swap on base `a3a819c67`

Worktree `/tmp/wt_int0922`, detached at `a3a819c67`. The round 3 change applied as one diff (`git diff 63c4e9fef 498dc5731`, 3-way) as the bottom commit, then the 81 commits cherry-picked in order; commits the round already carries (`7dedac883` has_backward, `43b49efe4` payload without trainable upstream) are dropped.

API map for the ports: `kimi_k3/pipeline_stage.py` -> `kimi_k3/pipeline_parallel/stage.py`, `kimi_k3/layout.py` -> `pipeline_parallel/layout.py`, the entry `pipeline_kimi_k3` from `parallelize.py` -> `pipeline_parallel/__init__.py`; `_routing()` -> `layout()` / `store()`; `infer_block_layout_tables_from_stages(stages, ...)` -> `infer_block_layout_tables(...)`; core `pipeline_with_first_stage_modules` -> `pipeline_with_first_last_stage_modules` (`return_split=True` hands the split back; `llm_split_with_stage_modules` gone); config `module_fqns_per_model_part` -> `pipeline_parallel_module_fqns_per_model_part`.

### Outcome of the cherry-pick (2026-09-21 ~10:15 UTC)

The round 3 diff applied onto `a3a819c67` without conflict (bottom commit "integration: text-decoder pipeline parallel (k3_pp_text 498dc5731, as one change)"). Of the 81 commits above the old squash, 75 landed and 6 were skipped as already carried or obsolete: the observability logger and the typing-import fixups of the old entry, the two stage fixes round 3 has (`has_backward`, the payload with nothing trainable upstream), the typed transport stage class, the unused-import drop. Recurring conflict: every commit that had touched the old entry brought the whole entry back into `parallelize.py`; resolved by keeping HEAD and only a commit's new top-level definitions (`matrix_scripts`-side helper `resolve_par.py` in the scratchpad, kept with the kit). Ports into `kimi_k3/pipeline_parallel/__init__.py`: the neighbor transport composed into the rebuilt stage class, `attn_res_cache_offload` on the store, `pp_balance`, and DEP (`vit_dep_split` through core's `_get_pipeline_metadata` / `_generate_llm_fqn_per_model_part`, the runtime install moved into the package; the cell is `kimi_k3_pp4_vp4_vit_dep`, 16 stages with the tower and the embedding alone on the first). The flavor keeps `kimi_k3_full_attention_layers` and the `num_layers` / `num_mtp_layers` knobs from the MTP port at the 17 layer, dim 256 defaults; the 33 layer entry is gone. Three functions the original tree had already removed came back through the resolver and were removed again; `parallelize.py`'s remaining functions are identical to the original tree's (AST compare). Tests: the remat and recompile-limit tests read the flavor's shape (dim 256; 10 layer variants for 17 layers). CPU: the K3 / CP / transform / LoRA / MoonEP / pipeline subset 197 passed, the only failure the box's missing `flash_attn`. Tag `k3_int_20260922a` = `9dc5757ea`; the previous head is `k3_int_20260918_pre_pp3` = `42691735c`. The matrix `run_mx4_int0922.sh` (pp2 x vp2 cells as CLI flags on the shared flavor, since the recipe is gone) runs with a fresh seed cache: the flavor changed shape, so the 09-17 step-1 numbers are not comparable; the comparison is within the run (PP cells against dp1 / fsdp2 / tp2 at step 1).

### Step 1 matrix (2026-09-21 20:50 UTC): 21 / 21 on `k3_int_20260922a`

`logs_int0922/mx4_int0922a_results.txt` (8 x 5060 Ti, `/workspace/venv_bfx9`, seed cache `.mx4_seeds_0922`, the shared 17 layer flavor at 4096 tokens per step, 256 per microbatch). Every cell completes 10 steps. Step 1 is read against the cell that sees the same data stream on the same reduction: `pp2` = `dp1` (8.05722), `fsdp2_pp2` = `fsdp2` (8.06545), `tp2_pp2` = `tp2` (8.05712), `fsdp2_pp2_ep2` = `ep2_fsdp2` (8.06545), `tp2_cp2_pp2` = `cp2_tp2_ag` (8.05713), all equal at the printed five decimals; `fsdp2_tp2_pp2` (8.06520) and `fsdp2_cp2_pp2` (8.06541) have no non-PP twin in the matrix (their fsdp2 x tp2 / fsdp2 x cp2 neighbours read 8.06529 / 8.06549, the flavor's own step-1 spread across reduction orders). `ep8_fsdp8` is dp8 data, its own row. The 09-17 numbers are not compared (other flavor shape). `k3_on_4025` moves to `9dc5757ea` (tag `k3_int_20260922a`) on this result.

## Step 2: rebase onto `63c4e9fef` (#4794 trainer refactor, #4808 stacked w13)

Worktree `/tmp/wt_int0922b`, 79 commits over `63c4e9fef` after the rebase (conflicts: the CP refactor against main's `prepare_context_parallel_input`, the flux trainer, the attn-gym pin (one pin, main's 0.0.10), the feed_forward hooks (main's, the split moved into the adapter), the compile refusal). Tag `k3_int_20260922b` on the commit "integration: the LoRA transform on main's stacked w13, and two rebase slips".

What #4808 broke and how it is met (2026-09-21 21:00 UTC): main stores `w13` as `[2, F, D]` (`Linear(num_linears=2)`) and drops the split hooks; the packed MXFP4 base read `out_f, in_f = w.shape` and the LoRA linear built a 2-D `lora_b`, so the QLoRA recipe failed at build and the plain LoRA recipe's `w13` forward failed at the add. The LoRA linear now builds its adapters through main's `_LoRALinearMixin` (stacked `lora_b`, the mixin's adapter sharding, the R/I branch our tree had already added there), the packed pair keeps the weight's leading shape, the packed forwards flatten the stack for the matmul as `Linear` does, FSDP shards every matrix-shaped parameter of a stacked Linear along F (the packed pair included; main's rule read `child.weight`, which a packed module no longer has). The `w1` / `w3` split of packed keys that item 8 of the veRL plan relied on is gone with the hooks; the merged dict carries `w13.weight` `[2, F, D]` and main's adapter splits it on `to_hf`. Two rebase slips: the batch-invariant import in `kimi_k3/model.py`, and `compile_config.enable` where main made the config optional (the cell crashed in `parallelize_kimi_k3` until guarded like main). CPU on the whole `tests/unit_tests/cpu` tree (venv_bfx9): 1170 passed, 19 failed, 6 collection errors; the failures are the box's upstream baseline (config_manager, cp_attention, embedding, model_td_layout, quantization, qwen3_5_mrope, rope, varlen_attention; the same on the step-1 tree) plus one stale test of ours fixed in the commit (adapter stages test at dim 1024); the collection errors are files importing `transformers` / flux.

Matrix mechanics on this base: `--checkpoint.*` is gone from the CLI (the checkpointer is a component, `create_seed_checkpoint` suppressed), so `mx4b.sh` launches every cell through the scratch wrapper module `mx4ckpt` (`--module mx4ckpt --config cell|seed`, the base flavor named by `MX4_BASE_MODULE` / `MX4_BASE_CONFIG`), which attaches `CheckpointManager.Config(interval=100000)` to a cell and `CheckpointManager.Config()` + `create_seed_checkpoint` to the seed; the seed-load assertion is unchanged. A stale `sitecustomize.py` probe (2026-08-21, a read-only print of norm placements) sat on the scratch path the matrices put first on PYTHONPATH; it was harmless on the step-1 tree (it only reads `Trainer.model_parts`) and crashed every cell here; moved aside. Smoke on the seed: `dp1` step 1 = 8.15339 (the step-1 tree read 8.05722; #4808 changes the fused weight's init draw, so the two bases are not compared). The 21 cells run as part A (16 cells of at most 4 GPUs on GPUs 0-3, `run_mx4_int0922b_partA.sh`) and part B (the five 8-GPU cells, after part A and the 4-GPU veRL cells), so the veRL cells run on GPUs 4-7 meanwhile.

veRL on this tree: the rl flavor export `/root/models/kimi-k3-debug-nt-0922b` (12 layers, dim 1024, from `export_rl_newtree.py` on `/tmp/wt_int0922b`; 1428 tensors, `A_log` in the released `[1, 1, H, 1]` layout straight from the adapter, so no `-rel` step; the other files symlinked from `kimi-k3-debug-nt`); runners `verl_grpo_int0922b.sh` (text) and `verl_grpo_k3_image_int0922b.sh` (image) point at the tree and that export.
