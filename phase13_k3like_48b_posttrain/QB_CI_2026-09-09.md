# QB PR 4412: the CI run of 2026-09-08 and the fix (2026-09-09)

A maintainer ran CI on the PR head `3c9cef31a` on 2026-09-08 23:37 UTC. That head is the 2026-09-02 commit on a main of the same day; the tip-rebased head (`qb_review3`, then `qb_review4` after this note) was never synced to `k3_qb`. Every job failed; the raw logs are in the PyTorch CI bucket (`https://ossci-raw-job-status.s3.amazonaws.com/log/pytorch/torchtitan/<job id>`, gzip), the GitHub API refuses log downloads without admin rights.

| job | failure | ours? |
|---|---|---|
| lint (3.11) | pyrefly: 8 errors. 6 in `torchtitan/components/quantile_balance.py` (274, 277: `moe.expert_bias_E` typed Optional; 308, 363, 365: `bias` typed `Module | Tensor`, so `.detach()` / `.to()` / `copy_` do not type), 1 in `torchtitan/models/kimi_k3/config_registry.py:110` (`config.model_spec` Optional), 1 in `torchtitan/distributed/cudagraph.py:17` (`torch.cuda._annotate_cuda_graph_trace` not importable, main's file) | 7 yes, 1 main |
| build-test (CPU unit tests) | collection errors in 5 test modules: `ModuleNotFoundError: No module named 'torch.cuda._annotate_cuda_graph_trace'` (imported by `torchtitan/distributed/cudagraph.py`, reached through `torchtitan.trainer`); the QB test module itself collected | main |
| 1 GPU unit tests | runner infrastructure: `Credentials could not be loaded`, `Executing the custom container implementation failed` | infra |
| multi-GPU unit tests | the same `_annotate_cuda_graph_trace` import at collection (`tests/unit_tests/gpu/test_fused_swiglu_override.py`) | main |
| 1 GPU integration (fake PG) | the same import, at the integration runner's first import; no test ran | main |
| 8 GPU integration, features / models (required subset) | the same import; no test ran. K3 has no test in those suites (its only integration test is `tests/integration_tests/b200.py::kimi_k3_mm_fsdp`) | main |

The import comes from main commits `eae4563ad` (Enable CudaGraph Annotations, #4359) and `d2e8d9f59` (#4447); the CI torch of that night did not have the module (the lint job installs the cu130 nightly fresh and hit it too). Main's tip `65ba8a697` no longer imports it (`966befce6`, #4493) and main's own CI is green there, so the rebase removes every non-lint failure.

## What `qb_review4` carries

`qb_review4` = `qb_review3` (`db65fc9f2`, three commits) rebased onto main `65ba8a697` (clean; the five main commits since `f6b9152e9` touch nothing under the model). The second commit, `52b19b16b` "narrow the bias and score types for the checker", is the fix for the seven pyrefly errors: the bias is fetched once as a `Tensor` with an `assert` on the Optional, and `post_optimizer_build_fn` is set on a `model_spec` the config function asserts non-None.

Local checks on `qb_review4` (`/tmp/wt_qb_r4`, torch nightly 0906 cu130, attention-gym main):

| check | result |
|---|---|
| `pre-commit run --files` on the three files | every hook passes except pyrefly, whose 58 reported errors are all in files the PR does not touch (missing torch-internal imports on this box); none in `quantile_balance.py`, `config_registry.py` or the test |
| `pytest tests/unit_tests/cpu/test_quantile_balance.py` | 14 passed |
| `pytest tests/unit_tests/gpu/test_kimi_k3.py` (4 GPUs) | 2 passed, 1 skipped |

The same CPU test passes on the CI merge commit too (14 passed, `/tmp/wt_qb_oldmerge`), which is the direct check that the QB test was not what failed.

Action: sync `k3_qb` to `qb_review4` (`git push origin qb_review4:k3_qb --force-with-lease`, the user's step) and ask for a CI re-run; drop the "[DO NOT review, pending EP PR merging]" title prefix, the EP PR is merged. Note for the reply to the maintainer: one sentence that the failures were the stale head (seven type errors, fixed) plus the `_annotate_cuda_graph_trace` import that broke main's CI that night.
