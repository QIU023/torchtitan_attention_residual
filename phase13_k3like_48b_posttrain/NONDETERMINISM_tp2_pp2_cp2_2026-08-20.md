# `mm_full/tp2_pp2_cp2` 的反向不可复现(2026-08-20)

> **已修复(同日)。** `_vision_stream()` 在 `torch.is_grad_enabled()` 时返回 `None`:
> 侧流不再承载 autograd 图。该格四跑单一轨迹,且与"仅 DEP"、与关侧流探针三者逐位相同 ——
> 三条独立路径落在同一条轨迹上,说明修复给出的是本来就该有的结果,不是第四种新数值。
> 其余 20 格逐位不变。本文保留全过程,因为归因错了两次,那两次比结论更值得读。

## 结论先说

**`mm_full/tp2_pp2_cp2` 在同一棵树、同一配置、带 `--debug.seed 42 --debug.deterministic`
的条件下不能自我复现。** 前向是确定的,不确定的是反向的梯度归约。

因此:**这一格不能作为任何前后对照的证据**。任何用它判断"某个改动是否改变了数值"的结论,
无论正负,都是噪声。

## 证据

同树(`k3_full_tree_draft` @ `568b52133`)同配置跑三遍,step 1:

| 跑次 | loss | grad_norm |
|---|---|---|
| before | 12.05472 | 245.8773 |
| repro1 | 12.05472 | **245.8816** |
| repro2 | 12.05472 | 245.8773 |

**loss 相同而 grad_norm 不同**,这把随机源钉在反向:前向若有不确定性,step 1 的 loss 就会先动。
step 2 之后 loss 才开始分叉,那是权重已不同的后果,不是新的随机源。

对照格 `mm_full/fsdp2_pp2_cp2` 三跑逐位相同(12.04848 / 13.0949),所以 **harness 本身是确定的**,
`--debug.deterministic` 兑现了 —— 这不是环境问题,是这个配置组合特有的。

## 触发条件很窄

在 21 格 CP 基线里,只有这一格偏离。具体地:

* `text/tp2_pp2_cp2`、`mm_lora/tp2_pp2_cp2` 都与 8-19 逐位一致 -> 不是 TP+PP+CP 本身;
* `mm_full/fsdp2_tp2_cp2`、`mm_full/ep2_fsdp2_tp2_cp2`(有 TP 无 PP)一致 -> 不是 TP 单独;
* `mm_full/fsdp2_pp2_cp2`、`mm_full/ep2_fsdp2_pp2_cp2`(有 PP 无 TP)一致 -> 不是 PP 单独。

**触发条件是全参多模态 + TP + PP 同时在场。** mm_lora 不触发这点值得注意:LoRA 冻结基座,
反向路径窄得多。

## 定位:是 PREFETCH,不是 DEP(2026-08-20,四配置分离)

第一次定位只关了 `KIMI_VIT_DEP` 和 `KIMI_VIT_PREFETCH` 两个开关一起,得出"在 DEP 路径上",
**那个结论是错的**。分离之后:

| 配置 | 跑次 | 不同轨迹 | 两两不同 | step1 loss |
|---|---|---|---|---|
| A DEP + prefetch | 7 | **7 种** | **21/21 对** | 12.05472 |
| B 仅 DEP | 6 | 1 种 | 0/15 对 | 12.05472 |
| C 仅 prefetch | 1 | 与 D 逐位相同 | - | 12.03366 |
| D 都关 | 6 | 1 种 | 0/15 对 | 12.03366 |

证据 `gate_logs/dep_nondeterminism_2026-08-20.txt`。三条结论:

1. **不确定性由 `KIMI_VIT_PREFETCH` 引入。** 仅开 DEP 六跑单一轨迹;两个都开则七跑七种。
2. **前向差异由 `KIMI_VIT_DEP` 引入**(B 与 A 同,C 与 D 同)。DEP 不是数值惰性的调度优化。
3. **prefetch 无 DEP 时完全惰性** —— `pf.take(mb)` 依赖 DEP patch 设的 `_dep_current_mb`,
   所以 C 与 D 逐位相同。它不是一个独立配置。

A 组七跑七种轨迹说明这**不是间歇性的**。先前以为"三跑里只异常一次",是因为只比了 step 1;
比完整十步则每跑都不同。**只比首步会把每跑必不同的现象读成偶发。**

## 机制:侧流上的梯度累加(2026-08-20,第三轮)

再关一个变量 —— 让视觉编码回到当前流(`_vision_stream()` 返回 None 的临时补丁),
prefetch 照常开:

| 配置 | 跑次 | 不同轨迹 | step1 |
|---|---|---|---|
| DEP + prefetch,侧流开 | 7 | **7 种** | 12.05472 / 245.8773 |
| DEP + prefetch,**侧流关** | 3 | **1 种** | 12.05472 / 245.8773 |

**侧流关掉即确定,且首步数值与侧流开时逐位相同。** 所以侧流不改变算什么,只改变归约顺序 ——
与"loss 全同、grad_norm 先分叉"的形态完全吻合。

机制:编码的 autograd 图录制在 side stream 上,反向也在侧流跑。prefetch 把 mb+1 的编码挪进
mb 的前向,于是多个 micro-batch 对**同一批 tower 参数**的梯度累加跨流并发,顺序由真实设备
时序决定。prefetch 是必要条件,不是原因本身。

### 一个被证伪的假设

本文前一版把 **DEP 气泡的延迟反向重放顺序**列为首要嫌疑。这个格子(pp2)的日志里
`DEP bubble backward` **一行都没有** —— 气泡重放根本没参与;而且它的顺序本来就是确定的
(`run_next` 取 `min(pending)`,`drain` 用 `sorted`)。那是读代码猜的,没验证。**留着它会让
下一个人往错方向修。**

### 修复

`_vision_stream()` 在 `torch.is_grad_enabled()` 时返回 `None`。原则:**侧流在不录制
autograd 图时安全,录制时不安全。**

支撑这个选择的测量是:关掉侧流后,DEP+prefetch 的十步轨迹与"仅 DEP"**逐位相同** ——
侧流对数值的唯一贡献就是那个无序累加,它不在算别的东西。

没选另外两条路的理由:

* **关掉 prefetch** 会丢掉报告 5.2.3 的并发,而这是唯一实现;
* **给反向补跨流定序边**看似更完备,但今天**没有任何东西可保**:两个调用点都立即 join,
  所以 grad 开启时侧流重叠不了任何东西(代码自己的注释即如此写)。为当前收益为零的路径
  加同步复杂度,是把成本花在错的地方。

侧流机制保留给将来真正的 deferred 设计。那个设计需要跨流集合通信定序(尚未建立),而且
**在它能承载梯度之前还得先解决有序累加** —— 这一点写进了代码注释,免得下一个人以为侧流
已经可以带梯度。

`test_k3_multimodal.py::TestVisionSideStreamGating` 钉住这条性质,防止被"优化"回去。

## 一条被推翻的树内注释

`run_postmerge_gate.sh` 写着 prefetch "proven numerically inert (loss identical to
prefetch-off step for step on four pp8xvp4 pairs)",据此认为 carry 它零成本。

期望值上它确实惰性(D -> C 逐位相同)。但在 DEP 开启时它**让配置失去可复现性**。那四对
pp8xvp4 的证据要么取自 prefetch 未真正生效的配置,要么比的步数不足。

**代价不是零:开着它,这个配置没有可复现的数值。** 注释需要改。

## 这条比它本身更值得记的地方

**8-19 那次 58/58 没有能力发现它。** gate 的通过判据是"这一格是否跑满 10 步",不比数值。
所以一个反向不确定的格子可以年复一年地"通过",而它同时是每一次前后对照里的噪声源。

这与 `consistency/README.md` 里那条 flaky 是同一类问题的两个实例:**验证基线本身不可信,
而没有任何东西会因此变红**。那次是测试读了未初始化的权重,这次是 gate 的判据看不见数值。

纪律照旧,并补一条:

* 拿任何格子做前后对照之前,先确认它能自我复现 —— 至少同配置跑两遍;
* **判据只数步数的矩阵,不能用来支撑"数值未变"的结论**。它能证明"没崩",仅此而已。

## 待办

1. ~~关 DEP 复跑该格~~ **已做**,见上:不确定性在 DEP 路径上;
2. **分离 `KIMI_VIT_DEP` 与 `KIMI_VIT_PREFETCH`**,确定是哪一个带来不确定性,以及
   哪一个改了 step 1 的前向;
3. 从"在 DEP 路径上"再定位到具体位置(延迟反向的 grad queue 是首要嫌疑,未证);
4. 在 gate 里给该格数值比对以外的判据 —— 现状是它每次都"通过",而数字每次都不同;
5. 定位确认后修。这是我们自己的代码,不是可以接受的既有行为。
