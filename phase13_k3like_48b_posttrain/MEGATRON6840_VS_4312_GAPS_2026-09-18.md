# What MLM PR 6840 has that PR 4312's head does not (2026-09-18)

Read from the source on both sides: 6840 at `upstream/pr6840` (`megatron/core/transformer/attention_residual.py`, 770 lines) and the current `pp_review5`, which is now the PR 4312 head `de6f29514` replayed onto main `a3a819c67` (`ab8ea5f61`, 37 commits, 16 files). Every line number below is from that head.

This revision replaces the 2026-09-18 morning version, which concluded three times and reversed twice without measuring. Everything numeric here is measured on the box; the harness is in the session scratchpad (`agg_bench.py`, `loop_bench.py`, `real_shape.py`). Where an earlier claim of mine is contradicted, the contradiction is stated rather than quietly dropped.

Nothing here is a cache item. The cache comparison stands where it was, in sections 3c and 3d of `MEGATRON_ATTNRES_VPP_MIGRATION_2026-09-14.md`.

## The three candidates, and what measurement did to them

    candidate                                        verdict after measurement
    1. block stack as a list of references           real, and it is the same change as 2
    2. aggregation that saves per token statistics   real, and it is the largest win by far
    3. received payload unpacked as views            still does not port, for a reason in the schedule

Items 1 and 2 are not two changes. 6840's `_AttnResAggregation` takes the values as a sequence, so keeping the sources as references and saving only statistics are the same edit. Treating them as separable is what produced the earlier "narrow form removes almost nothing" reversal: measured alone, dropping the concatenation really does save little, because the concatenation is not what autograd retains.

## What the aggregation actually retains

`_apply_attention_residual` (`model.py:153`) builds `values_TND` by concatenation and then upcasts:

    values_float = values_TND.float()                  fp32 [T, N+1, D]
    keys_TND     = values_float * torch.rsqrt(...)     fp32 [T, N+1, D]

Both survive to backward. At the PR's own shape (2048 tokens per micro batch, `D` 7168, bf16, one slice 28.0 MiB) a stack of 8 plus the partial retains 1008.2 MiB for one call, which is exactly the two fp32 tensors and nothing else. The bf16 concatenation itself is not among the retained storages: its cost is a transient allocation and one copy, so removing only the concatenation removes only peak and bandwidth, not residency. That is the point the earlier version got backwards in both directions.

The aggregation runs twice per layer, at `model.py:263` and `model.py:280`, and once more at the model level (`model.py:523`). The growing concatenation at `model.py:251` runs once per block. So residency is dominated by the per layer aggregations, by a factor of roughly twice the block size.

## Measured: one aggregation at the PR's own shape

T=2048, D=7168, bf16, RTX 5060 Ti. `kept` counts storages autograd holds that are not already live as inputs. `dev` is the maximum absolute difference from the current form, relative to the current form's own maximum. Timings are the median of 7 runs after 10 warmups; an earlier version of this table used 3 warmups and its timing columns were wrong, since a first call at these shapes can cost 280 ms against a warm 0.7 ms. Memory comes from a separate counted run.

    N  variant                 kept MiB   fwd peak   bwd peak   fwd ms   bwd ms      dev
    1  today                      224.1     420.1M     447.9M     3.98     9.20        .
    1  6840 list + Function         0.0     224.1M     364.1M     3.39     6.64    0.32%
    1  today + AC reuse            28.0     420.0M     672.0M     4.11    12.42        .
    4  today                      560.2    1092.2M    1119.9M     9.00    21.93        .
    4  6840 list + Function         0.1     280.1M     448.2M     8.70    16.14    0.35%
    4  today + AC reuse           112.0    1092.0M    1680.0M     9.36    30.07        .
    8  today                     1008.2    1988.2M    2015.8M    15.89    39.11        .
    8  6840 list + Function         0.2     280.2M     560.3M    15.74    29.05    0.47%
    8  today + AC reuse           224.0    1988.1M    3024.0M    16.02    53.62        .

On memory the reference form is better on every column at this shape. Residency falls to nothing because its saved tensors are per token statistics plus references to sources that already exist, and forward peak stops growing with `N` once the stack is not materialised in fp32.

On time the result is narrower than an earlier version of this document claimed. Backward is consistently faster, 1.38x at N=1, 1.36x at N=4 and 1.35x at N=8, because it recomputes one fp32 upcast per source instead of replaying the whole region. Forward is parity, 1.17x at N=1 falling to 1.01x at N=8, not the win that the badly warmed table reported. AC reuse pays for its recompute in backward, at 0.74x, 0.73x and 0.73x of the current form.

AC reuse keeps exactly the stack (224.0 MiB at N=8, which is 8 slices) because `torch.stack` sits outside the checkpointed region, and it has the worst backward peak of the three, since the recompute rebuilds both fp32 tensors on top of the backward's own working set.

## Measured: the whole block loop, and why the shape matters

32 layers as 8 blocks of 4, 63 aggregations, T=512, D=1024, with the attention and feed forward replaced by cheap stand ins so the columns describe the residual machinery alone.

    variant                 kept MiB   fwd peak   bwd peak   fwd ms   bwd ms
    today                     1565.2    1620.3M      82.0M     36.9     52.4
    6840 list + Function       256.1     288.2M      54.0M     68.9    104.5
    today + AC reuse           283.1     366.2M     116.0M     48.2     88.2

Memory ranks the same way. The timing columns of this loop table are withdrawn: they were taken with 2 warmups and the rerun above showed that is not enough to measure anything at these sizes. What replaced them is the debug flavor's own cell, which is worth naming precisely because it is what every smoke and CI run exercises and therefore the first number a reviewer meets. `DEFAULT_DEBUG_MODEL_SEQ_LEN` is 2048 and `_debugmodel` sets `dim` 1024 with `attn_res_block_size` 12 over 24 layers, so the stack reaches 2 and roughly half the aggregations run against a one block stack and half against a two block one. Each row below was measured twice, 41 runs after 15 warmups, and both passes agreed:

    N   today fwd   Function fwd   ratio     today bwd      Function bwd   ratio
    1       0.325          0.407   0.80x     1.21 to 1.44   1.28 to 1.31   a wash
    2       0.639          0.551   1.16x             1.98           1.13   1.75x

Forward at N=1 is the one reproducible loss: 0.80x in both passes, about 0.08 ms in absolute terms. Backward at N=1 is not a loss, and an earlier pass reporting 0.73x there was wrong. That figure came from the current form's own instability, whose median moved from 1.44 to 1.21 ms between passes and was bimodal within one of them, while the Function held 1.28 to 1.31. At N=2 the Function wins both. So the debug flavor nets out close to neutral on time while keeping the memory win, which is the honest thing to tell a reviewer who runs it.

Larger shapes at the same `dim`, one pass each: T 8192 at N=2 reads 1.10x forward and 1.43x backward, and T 16384 at N=2 reads 1.11x and 1.38x.

The mechanism is kernel launch overhead: the reference form replaces a few large ops on `[T, N+1, D]` with `N+1` smaller ones, so it is launch bound when the slices are small, which is why the loss appears at the narrowest stack rather than the smallest T. This is consistent with 6840 carrying three paths behind one interface (`_get_fla_fused_attnres`, `_get_compiled_attn_res`, and the eager Function) rather than the eager Function alone.

## Correctness of 6840's backward, checked rather than assumed

`_attn_res_bwd_math` is hand derived, so it was worth checking rather than trusting. Transcribed and run under `torch.autograd.gradcheck` in float64 it passes at two shapes, as does autograd on the same forward:

    T=5 N=3 D=4   explicit backward True    autograd on the same forward True
    T=3 N=5 D=6   explicit backward True    autograd on the same forward True

An earlier run of this check failed, and the failure was mine: the transcription left one `.float()` in the output accumulation, so a float64 probe was truncated to fp32 exactly where the values enter the output. The signature of that bug is instructive. The gradients of `pseudo_query` and `key_norm_weight`, which reach the output only through the float64 softmax path, matched to 1e-8, while the value gradients were wrong by 5 percent. Any explanation that did not account for the two passing inputs would have been wrong.

## The result is not bitwise, and by how much

The reference form reorders the same arithmetic: it takes the dot product first and scales by the inverse RMS, where the current form normalises the values first and then contracts. The difference is input conditioned, because the depth softmax amplifies it when the logits are extreme.

    pseudo query scale     |logit| max   mean max prob    out dev   grad dev
    N(0,1)                      131.50           0.966      3.70%      3.45%
    N(0,1) / sqrt(D)              4.41           0.336      0.42%      0.35%

At a realistic logit scale the deviation is about one bf16 ulp (2^-8 is 0.39%). At an unrealistic scale, where the softmax is nearly one hot, reordering decides near ties and the deviation is an order of magnitude larger. This matters for how the change is justified: it cannot be claimed under the usual bitwise step 1 bar against the pre change baseline, because it is a different summation order. The honest bar is a step 1 gradient comparison against the current form with the deviation sized next to it, on one warm cache, plus the float64 gradcheck above as the correctness argument.

## How this sits with PR 4656's AC reuse, re judged

The earlier version judged this against a baseline of plain `today`, which was the wrong baseline: AC reuse is not tied to the PP cache and it is on by default. Read from its own base (`origin/k3_on_4025`), the branch is now three commits and four files (`fd0a9b6c6`, `615908fcd`, `362b6cc70`), not the two the earlier version described. `KimiK3TransformerBlock.checkpoint_residual` is initialised to `True`, `_attention_residual` routes to `remat.region` under RegionAC, to `_checkpointed_attention_residual` otherwise, and `parallelize_kimi_k3` clears the flag only when a Selective, Full or Region policy already checkpoints the whole block. So the default configuration checkpoints the residual.

With that as the baseline, the table above answers the question directly. At N=8 the reference form retains 0.2 MiB against AC reuse's 224.0, peaks at 280.2 MiB in forward against 1988.1, peaks at 560.3 MiB in backward against 3024.0, and its backward costs 27.87 ms against 53.48. The reference form dominates on all four.

The two are alternatives, not complements. Wrapping the reference form in a checkpoint would discard statistics it deliberately kept and recompute them, spending the recompute twice to save the 0.2 MiB it still holds. So the globally optimal arrangement is the reference form with `checkpoint_residual` defaulting to `False`, and AC reuse retained only for the regions it covers outside the residual, which is most of its diff (the q, kv, inner attention, gate and wo regions in `KimiMLAAttention`, and the KDA regions). That is a recommendation for 4656, which is already filed, and therefore the user's call rather than mine.

## Item 3, why it does not port

What 6840 does: `unpack_attn_res_payload` returns `torch.chunk` slices of the received tensor, which share its storage, so gradients accumulate into the one tensor handed back to the schedule. `assemble_stack` instead builds the stack with `torch.stack` and then `detach().requires_grad_(True)`.

Why the view form cannot be taken. 6840 reads the accumulated gradient off `leaf.grad`. Under `torch.distributed.pipelining` that slot does not survive: `stage_backward` appends `val.grad` to its outputs and then assigns `val.grad = None`. The fresh leaf is what lets `backward_one_chunk` take `(grad_hidden, grad_stack)` out of `self.bwd_cache` after the base class has run and hand the columns out itself through `split_stack_grad`. The `detach` is load bearing.

What it costs not to have it, at the PR's own pp8 x vp4 recipe, 93 layers at block size 12, so 8 blocks and one slice of 28.00 MiB:

    side     what it pays per micro batch
    ours     one stack copy on each receiving stage, k slices for a stage holding k blocks
             k=1: 28.0 MiB   k=4: 112.0 MiB   k=8: 224.0 MiB, and only the last stages hold 8
    6840     12 extra slices on every hop, from padding to the uniform width: 336.0 MiB
             pp8 vp2: 16 slices, 448.0 MiB; pp4 vp4: 8 slices, 224.0 MiB

Both copy once on the send side as well. So not implementing item 3 leaves no deficit: the copy is bounded by the blocks a stage actually holds, while 6840's padding is paid on the wire on every hop and exceeds the heaviest stage here.

## What is not worth taking

- `_AttnResGradTap` (line 346), for the `stage_backward` reason above.
- The uniform payload width, measured at between +12.0% and +27.1% of payload slices depending on the shape.
- `pack_attn_res_payload` returning "a fresh, viewless tensor", which Megatron's `deallocate_output_tensor` requires. That constraint is Megatron's, not torchtitan's.

## The change, and why it is smaller than the earlier version claimed

The earlier version proposed a narrow form that changes the helper's signature and its three call sites, and a wide form that changes the cross stage contract and both SPMD declarations. Neither is needed for the measured win.

`block_residual_TND` appears in `model.py` (18 references) and `sharding.py` (3). The three in `sharding.py` are `_block_residual_placement` plus the layer's `in_src` and `in_dst` entries, all keyed on the layer argument name and its rank. `pipeline_stage.py`, `mtp.py`, `cp_kda.py` and `layout.py` have none.

The whole win is inside `_apply_attention_residual`. Replacing the concatenation with `unbind(dim=1)`, which returns views and copies nothing, gives the sequence the Function wants, and the helper keeps its exact signature. That leaves untouched: all three call sites, both SPMD declarations, the layer to layer contract, the stage code, and the review diff 4312 is being read on. The growing concatenation at `model.py:251` stays, and is second order next to what the aggregations retain.

Precedent for the mechanism is in core, not only in Megatron: `torch.autograd.Function` appears in `components/loss.py`, `models/common/linear.py`, `models/common/dist_gemm.py`, `models/common/aux_loss.py`, `models/gpt_oss/moe.py` and the quantization modules. `RMSNorm.eps` is a plain float, so it crosses the Function boundary as a non tensor argument. The residual norm and projection weights are declared TP unsharded in `sharding.py`, so the backward's reductions are over replicated weights and need no collective. K3's `parallelize.py` calls no `torch.compile`, so there is no graph break to manage today.

Test surface before the change: no test under `tests/` referenced `attention_residual` or `block_residual`. `test_kimi_k3_pp_exact_block_grads.py` builds its own synthetic `_ExactStage` with `torch.arange` weights, so it covers the transport and not the aggregation math. A change confined to the helper was therefore uncovered, which is why one was added with it.

## What `pp_review5` carries that the PR branch does not

Now nothing but the rebase. `pp_review5` was reset on 2026-09-17 to `ab8ea5f61`, which is the PR head `de6f29514` replayed commit by commit onto main `a3a819c67`: 37 commits, 16 files, +1525/-34, verified at 0 conflict markers, 0 parse failures, 0 unresolvable imports and 0 trailers. The earlier version of this document compared against the previous `pp_review5` (`291f3844a`, 23 commits), which is the stale line and is preserved as the tag `pp_review5_replay_broken_20260917`.

The real increments over the PR branch remain the two stacked drafts, each one commit and three files over `de6f29514`:

    pp_offload_review1   attn_res_cache_offload, the rank store on pinned host memory   +58/-7
    pp_balance_review1   pp_balance, activations parked on a peer via Mooncake          +427/-1


## What landed, and how it was verified

`pp_review5` is `3f6f35127` on the fork, two commits over the replayed PR head `ab8ea5f61`. The old remote tip is preserved as the pushed tag `pp_review5_origin_before_replay_20260918`.

    1ad999623   kimi_k3: aggregate the block residual without retaining FP32 copies
    3f6f35127   kimi_k3: test the attention residual against plain autograd

The change is confined to `model.py`. `_AttentionResidualAggregation` is a `torch.autograd.Function` whose forward unbinds the stack, builds the depth softmax from per source scores and inverse RMS, and saves only the score weight, the norm weight, the softmax weights, the scores, the inverse RMS and references to the two inputs. `_apply_attention_residual` keeps its exact signature, so no call site, no SPMD declaration and no stage code moved. Its dangling docstring, the defect commit 13 introduced, is gone with it.

Measured on the form that shipped, T=2048, D=7168, N=8:

    variant                     kept MiB   fwd peak   bwd peak   fwd ms   bwd ms      dev
    today                         1008.2    1764.2M    2015.8M    15.89    39.11        .
    Function, unbind inside          0.2     280.2M     560.3M    15.74    29.05    0.40%

Forward is parity and backward is 1.35x faster; the memory columns are where the change earns its place.

Correctness, in the order the checks were run:

- In fp32 the new form reproduces the old one exactly, forward and every gradient, with and without the partial block: 0.000% on both, at T=64, N=5, D=32. In bf16 the difference is one ulp, 0.457% and 0.376% on the output and 0.289% and 0.131% on the gradients.
- Against autograd through the old formulation the shipped gradients differ by 3.6e-7 with float64 inputs and 1.8e-7 with float32, against a gradient maximum of 2.85, which is 0.0000% relative.
- `torch.autograd.gradcheck` does not apply to the shipped op, and the False it returns is not a defect. The residual is FP32 by contract, so a float64 input is truncated inside the op: a 1e-6 probe step moves the output by 1.192e-07, exactly one fp32 ulp, which leaves the numerical Jacobian degenerate. The float64 gradcheck reported earlier was run on a transcription that follows the input dtype, which is what makes it meaningful there and inapplicable here.
- The four existing K3 CPU tests pass unchanged, 16 passed, and the new `test_kimi_k3_attention_residual.py` passes, 2 tests and 4 subtests.
- The whole CPU suite reads 1049 passed, 19 failed, 11 skipped and 5 collection errors, against this tree's recorded baseline of 1047 passed and 19 failed. The difference is exactly the two new tests and their four subtests, and the new file is confirmed present in the collection rather than inferred from the counts. None of the 19 failures is a Kimi K3 test: they sit in varlen attention, qwen3_5 mrope, quantization, model td layout, config manager, aux loss, rope, embedding and cp attention. The 5 collection errors are two missing packages in this venv, `torch_checkpointing` and `transformers`, in files that reference no Kimi K3 code.

The empty stack needs no guard. `partial_block_TD` is None exactly when `first_layer_in_block`, and that branch performs the concatenation at `model.py:251` before the aggregation, so the stack is at least one column wide whenever the partial is absent, and when the stack is zero wide the partial is present. The new test covers the zero wide stack with a partial, which is the half that is reachable.

Still open on this branch: a step 1 gradient comparison on the H100 against the current form, since the change is not bitwise. Nothing here has been proposed to 4312, which is untouched and still waits for review.

## Windows-side audit of the shipped form (2026-09-18)

Read from the fork, which now carries everything this document cites:
`pp_review5` = `3f6f35127`, 39 commits on `a3a819c67` and three behind
upstream/main, two commits over `ab8ea5f61`; both `ab8ea5f61` and `291f3844a`
and both tags fetch. The two commits touch `model.py` (+147/-16) and add the
test (+92), nothing outside the model folder, no trailers and no cherry-pick
lines. `_NeighborP2PTransportMixin`, `_PipelineTransportGroups`,
`_neighbor_p2p_stage_class` and `_warmup_pp_edge_communicators` are absent from
both `ab8ea5f61` and `pp_review5`, so Elfie's transport port is out of this line
as intended.

The backward was re-derived here rather than trusted. All five paths match the
code: the softmax Jacobian (`p * (pairing - sum p pairing)`), the score path
(`grad_logits * inverse_rms`, contracted with the query), the variance path
(`grad_logits * scores * -0.5 * inverse_rms^3`, times `2/D` times the value),
the query accumulation as a per-source GEMV, and the product rule splitting
`grad_query` onto the two weights. The five return values line up with the five
forward arguments, `eps` correctly returns None, and the empty-stack branch in
backward is guarded.

Three things to add to what is written above.

- The weight product changed precision, and part of the reported bf16 deviation
  is avoidable. The current form computes `norm.weight.float() *
  projection.weight.squeeze(0).float()`, two upcasts then an fp32 multiply; the
  shipped form computes `(score_weight_D * norm_weight_D).float()`, which under
  FSDP mixed precision multiplies two bf16 tensors and rounds before upcasting.
  Measured at D 7168 with bf16 weights: `query_D` is off by 3.86e-3 at most and
  1.40e-3 on average, and propagated through the depth softmax at a realistic
  logit scale that alone moves the output by 0.169%. The table above reports
  0.457% and 0.376% in bf16 and attributes it to the reordering; this part is not
  reordering and goes away by upcasting both factors before the multiply, in the
  forward and in the backward's copy of the same line.
- The verification list has no run of the cell that type checks. The change sits
  in `model.py`, so it is on every path, not only the pipeline: `kimi_k3_mm`
  (`kimi_k3_debugmodel_mm`, fsdp 2 x tp 2 with SP x ep 2) runs with SPMD type
  checking on, and `spmd_types.register_local_autograd_function`'s own docstring
  says that "unregistered autograd functions that reach the type checker leave
  their outputs untyped or raise in strict mode". Precedent cuts both ways: of
  the five core modules cited above, only `models/common/linear.py` registers,
  while `components/loss.py`, `dist_gemm.py`, `aux_loss.py` and `gpt_oss/moe.py`
  do not. So this is a question to settle by running that cell on this branch,
  not a defect on paper; if it does need registering, the Function qualifies as
  local (the N axis is never sharded and the contraction is over a TP-replicated
  D).
- `@once_differentiable` on the backward removes double backward, which the
  plain-autograd form had. Nothing in the tree needs it today; it belongs in the
  Limitations line of whatever body carries this.

Not verifiable here: the new test does not run on this box, since importing
`kimi_k3.model` reaches attn-gym's CuTeDSL kernels (`No module named cutlass`),
so its 2 tests and 4 subtests stand as the box's result; and the 6840 side has
no source in this workspace to read.

## Order

1. Done, as `1ad999623` and `3f6f35127` above.
2. A step 1 gradient comparison on the H100 against the current form, on one warm cache, sized against the deviation table above. Until that lands the change is measured but not justified to the numerics bar.
3. The `checkpoint_residual` default in 4656, once 1 and 2 are in. That is a decision about an already filed PR.
4. The wide form, a list across layers, only after 4312 merges, since it moves the cross stage contract and both SPMD declarations.
