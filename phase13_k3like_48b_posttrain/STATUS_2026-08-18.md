# 主任务状态,2026-08-18

取代 `OVERNIGHT_PLAN_2026-08-19.md` 的"路线"一节。下面每条都是本轮查过的,不是回忆。

## 0. 一句话

四个轴的实现是完整的、矩阵是绿的;卡点全在别处 —— 三个等 4025 合并,三个等大显存机器,一个等设计
对话。本轮真正的新东西是**发现 PR 切分漏了六个上游核心文件**,以及**修掉一个会进上游正文的错数**。

## 1. 代码在哪

| 分支 | 位置 | 说明 |
| --- | --- | --- |
| `dep_exp_impl` | `f489aca86` | 工作分支,一切都在这 |
| `attention_residual_dev` | `f489aca86` | **本轮 ff 到位**,此前落后 75 个提交 |
| fork `main` | 落后 458 | 未动,等 DEP 去留定了再说 |
| pytorch fork `get-total-norm-dtype` | `b198c32e5e` | 两个提交:DTensor dispatch 修复 + `get_total_norm` 的 dtype |

**可删的远端分支(已是 `attention_residual_dev` 的祖先)**:`align_4025`、`experiments_rl_unmerged`、
`k3_models_move`、`k3_pr_{a,b,c,d}_*` 四个、`migrate_{carrier_tensor,step3_cp,stream_dtensor,tp_attnres_tail}`、
`phase11_kimi_linear_447m_aligned`、`vlm-sglang-overlay` —— 13 个。没删,删分支是不可逆的。

**仍有独立内容的**:`grad-norm-fp32`(7)、`perceiver_resampler_projector`(7)、`pixelshuffle_projector`(7)、
`qwen25_vl_video_vla`(7)、`step_decay_lr_scheduler`(6)、`k3_refactor`(6)、`tp_declarative_refactor`(5)、
`core_revert_probe`(2)、`kimi_k3_upstream_pr`(2)、`k3_config_tree`(1)、
`fix_moe_ep_off_in_grad_placements`(1)。

## 2. 验证状态,以及一个诚实的缺口

**58/58 全过** —— 三个 arm(text 18 / mm_full 22 / mm_lora 18),`accounting` 自己报的
"logs found: 58 of 58 expected; passed: 58"。每个 CP 格都从 `apply_cp_kimi_k3` 内部打出
`Applied CP … kda_cp_mode=kcp`,包括 PP 下各 stage 层数不同的那几格 —— 改动的分支确实被走到了。

**缺口:那次 gate 跑的树是 `ae08727a2`。** 当前 HEAD `f489aca86` 多了两个提交(`Module` 重命名、
`router_input_BLD` 的 sharding 条目),它们只过了 420 单测 + `pre-commit`,**没进过全矩阵**。

要不要为这两个提交再跑一轮 58 格,取决于怎么看它们:重命名是纯符号替换(编译过、无遮蔽、单测数
不变);sharding 条目会改变 TP/SP 下 latent MoE 的重分布路径,**这个是有可能动数值的**,只是我们
的模型本来就用 `dataclasses.replace` 覆盖了同样两个键,所以实际路径应当不变。判断是"应当",不是
"测过"。

`pre-commit` 还有一项没跑:`pyrefly` 未安装,类型检查这一关一直是空的。

## 3. PR 切分漏了六个上游核心文件(本轮发现)

按 merge-base(upstream `65fa556be`,8-11)算,我们**新增 96 个文件、修改 8 个既有上游文件**。
`Raising_PRs/diffs/` 那五个 diff 只覆盖 `models/kimi_k3/` 下的新增文件,**这 8 个一个都没包含**。

| 文件 | 改动 | 归属 | 不带上会怎样 |
| --- | --- | --- | --- |
| `components/lr_scheduler.py` | +19 -3 | PR23 | 只持冻结权重的 stage 没有 lr 可调度 -> 抛错 |
| `components/optimizer.py` | +28 -4 | PR23 | param_groups 空匹配被当成错误 -> 抛错 |
| `distributed/fsdp.py` | +29 | PR23 / DEP | 条件执行的 FSDP 模块掉出 autograd 图 -> **死锁,不是报错** |
| `models/utils.py` | +7 | PR21 或独立 | 非 Shard placement 读 `.dim` -> 抛错 |
| `distributed/utils.py` | +73 -5 | PR26(已 filed 4135) | — |
| `common/moe.py` | +47 -14 | PR27 + PR28 | — |
| `common/moe_sharding.py` | +9 -2 | PR28 | — |
| `models/__init__.py` | +4 | 注册行 | — |

**结论:PR23 按现在的切法提出去跑不起来。** 前三项都是 LoRA×PP / DEP 路径上的必需改动,而
`fsdp.py` 那条的失败形态是死锁,比抛错更难查。

`models/utils.py` 那 7 行值得单独看:非 Shard placement 读 `.dim` 会抛,而专家权重在 cp 轴上就是
Replicate。这在未改动的上游模型上应该也能触发,更像独立 bugfix 而不是 K3 的一部分 —— 没验证过,
要验就写一个不含我们任何代码的 repro。

**未决**:并进各自的轴 PR,还是先提成几个独立小 PR。这会改变四个 PR 的边界,等人定。

## 4. PR 清单

**已 filed**:PR19(torchtitan 4054)、PR26 torchtitan 侧(4135)。

**写好待点头,不依赖 4025**
- **PR27** `GroupedExperts.gate_up_combine` —— 只碰 `models/common`。证据齐:stock
  `deepseek_v3_debugmodel` 同树跑两遍、只反向那一个 hunk,全精度读 TensorBoard,
  `grad_norm`/两个 loss/`lr`/`n_tokens_seen` 共 50 个点**逐位相同**(`RESULTS.md`)。
- **PR28** `MoE.forward(router_input_BLD=)` + `moe_sharding.py` 条目 —— 与 PR27 互相独立,
  两个 patch 任意顺序都能打上,两个都打等于当前树(验证过)。
- **PR26 pytorch 侧** —— fork 上两个提交,PASTE 待点头。正文里那段 CPU repro 本轮实跑过:
  确实复现(spread 1.95e-03,k=1 snap 到 256.000000),stock torch 确实拒绝 `dtype`。

**等 4025 合并** —— 四个轴 PR(PR21 EP / PR22 TP / PR23 PP / PR24 CP)。4025 头仍是
`c3a80b01e`,与上次比较时相同,仍未并入 upstream/main。四个 PASTE 正文本轮补齐(此前一个都没有,
全是 5–7 级标题、10–22 处粗体的 kit 文档形态 —— 正是 maintainer 那条"seems to be written by AI"
点名的形状)。

**索引/命名对齐**:查证后确认**不需要** —— 他们改的是函数内部变量,传给模型 config 的键仍是
1-based 的 HF 值,与我们一致。

## 5. 阻塞矩阵

**等大显存机器**(三件卡在同一个显存约束上)
- DEP 气泡隐藏 —— **前提搞错,结论已撤回**,见 `DEP_MEASUREMENT_RETRACTION_2026-08-19.md`;
  下面这条原记录留着作对照,但 `mb >> pp` 的方向是错的(placed 与 mb 无关)。原文:需 `mb >> pp` @ seq 4096。目前实测是**负的**:cost ratio 0.493 时 -0.95%,
  2.0 时 -2.08%。约束是报告自己的 upfront 前缀而非 cost ratio;placed share ≈ `(mb-pp)/mb`,
  gate 里 pp8×vp4 那格打出 `DEP bubble runtime: 0/0 planned`,与理论一致。
- delta 偏置是否随 shape 变化 —— 48B carrier 在 8×15.5 GiB 上 seq 1024/512/256 全 OOM。
- MoonEP 实现与测量 —— 要 8×NVLink。draft 本轮用 `inspect` 实测过:四个方法参数名与 kind 与
  `BaseEPTokenDispatcher` 逐字一致,且覆盖 `init_buffer`、继承 `wire_meshes`,与
  `DeepEPTokenDispatcher` 同一模式。

**等设计对话** —— `ShardingConfig` 能否表达 CP/PP。本轮发现上游 `apply_cp_to_forward` 自己的
docstring 就写着它是临时方案、CP 最终该走 ShardingConfig 声明式,所以问题应该问得更窄:声明式目标
是否覆盖"不是重分布"的 CP。Ulysses 是一次 all-to-all,能用 `Shard(seq) -> Shard(head)` 表达;KCP
序列全程不聚合、靠前缀扫描重算递推,没有任何 placement 说得出来。草稿在
`Raising_PRs/RFC_QUESTION_sharding_config_cp_pp.md`,未发。

**等人定** —— DEP 代码去留(机制正确、无加速),以及第 3 节那个切分问题。

## 6. 本地技术债

1. **没有 `kimi_k3/sharding.py`** —— 这是我们 `parallelize.py` 1768 行对 deepseek_v3
   `sharding.py` 192 + `parallelize.py` 100 的最大单项来源。上游五个模型无一按轴拆文件,
   全是这两个文件;之前计划里"按轴拆成三个文件"方向是错的,已撤销。
   config 化本身比记录的靠前:22 个 `Kimi*` 类里 7 个已直接继承 config-based `Module`、2 个间接,
   还在裸 `nn.Module` 上的只剩 `KimiK3MTPLayer`、`KimiK3MultimodalModel`(带 `KimiK3ViTStage`)、
   `KimiLoRALinear`。
2. **`pyrefly` 未安装** —— `pre-commit` 的类型检查这一关从未真正跑过。

## 7. 本轮抓到的两个自己的错(留着,因为都是可复发的形态)

- **PR21 正文的 `12.0594 -> 11.8446`,终点取的是第 3 步的值**,正确是 10.2364。暴露方式是 gate
  跑同一格,首步一模一样、末步不同。读一列数字时取错行,单看那份日志是发现不了的。
- **"按轴拆 `parallelize.py`"**是我自己写进计划的,查了上游才知道方向相反。计划条目和 PR22 的
  Scope 段都改了。

顺带一条边界:gate 的 mm_full arm 设 `KIMI_VIT_PREFETCH=1`,`run_cells.sh` 不设。两次运行有两个
共同格子,**全 10 步、7 位有效数字完全相同**,所以 prefetch 在 EP 布局上也是数值惰性的(此前只在
pp8×vp4 上验过)—— 这条是"因为比过所以能跨着读",不是"因为文档说惰性所以能读"。
