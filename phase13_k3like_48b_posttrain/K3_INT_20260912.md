# Integration branch rebuilt on main `56a721b64`: `k3_int_20260912` (2026-09-12)

Fork branch `k3_int_20260912` = `f201b9ac0`, 50 commits on upstream/main `56a721b64` (#4414). Worktree `/tmp/wt_int0912`. `k3_on_4025` is untouched (still `acf7c2ae6` on ac10ca48f) until the CP decision below.

## How it was built

1. The TP stack of `tp_sp_on_main` = `22eeec412` (4 commits), then the KDA capability guard at >= 8.0.
2. PR 4312 as one change: `k3_pp_text` = `pp_review4` = `dbc425403`.
3. The previous integration `k3_on_4025` (`acf7c2ae6`) replayed commit by commit on top (`replay_map.txt` in the session scratchpad has old -> new for every pick).

## Conflicts resolved during the replay (the non-obvious ones)

| old commit | what it is | how it landed |
| --- | --- | --- |
| b2809220f + fix | neighbor P2P transport | stage class chosen in `_pipeline_module_split`; K3's `_as_attn_res_stage` keeps the transport subclass (cast for the checker, `f201b9ac0`) |
| a5a9f373f | attention-residual checkpoint wrapper | the pick left a second, stale `prefix_sum_TD` definition that shadowed the real one; folded out of the commit (its old-tree fix e345abe6c became empty and was skipped) |
| d613126f1 | cache offload to pinned host memory | onto `PPRankLocalCache` (the old `RankStore`) |
| dd9db729e | pp_balance (Mooncake) | knob and install onto the new `pipeline_kimi_k3` |
| 058e3c708 | MTP | mirror layer declared through the new TP API (`_set_kimi_k3_layer_sharding`, `_stream_weight_config`); SP/CP guard after `set_kimi_k3_sharding_config` |
| 2e2147186 | KDA document offsets | main already builds the `"kda"` mask entry with `create_varlen_metadata_for_document` under flex too; what remains is the explicit `cu_seqlens` argument for callers without masks (verl) |
| 10327c38a | inner-KDA declarations name cu_seqlens | skipped: the new TP's `local_spmd` check reads positional arguments only, and `cu_seqlens` is keyword-only |
| 785d4d77b | DEP | split ported onto core's `llm_split_with_pinned_modules`: `_kimi_k3_vit_dep_split` (tower + embedding alone on stage 0, text over the rest; a spelled-out split must start with the tower stage); the pp8 x vp4 vit_dep cell moved to the b200 suite next to pp8 x vp4; the cp2 and old pp8 x vp4 feature entries not carried |
| 599077a00 | MoonEP | static per-rank token loop before the MTP guard |
| b517bc374 | released format | registry keeps main's 24-layer `debugmodel` and `debugmodel_33_layers`, adds `report_arch` / `k3mini` |
| 892fb1697 | per-block compile | main's compile refusal removed; the CP entry stays in the unsupported list |
| 751d760ce | quantile balancing (#4577) | both sides of `common/moe.py` (main's `MicrobatchWiseLoadBalanceLoss` + the balancer); `d0f7fb82a` restores the `DTensor` import the balancer needs |

## Not carried

- CP, pending the decision below: `38b348e96` (it included scratch copies of fegin's #4322/#4449/#4450), `4c2cd4d4c`, `edc4cd71b`, `61a73ca6c`, and what depends on them: `65f2dc536` (dynamic vision CP), `ccf493e08`, `00874d256`.
- `dbc60701d` (old declarations; the new TP covers them).

## Checks (2026-09-12/13, 5060 box)

- CPU (`tests/unit_tests/cpu` + `models/kimi_k3/tests`, K3/LoRA/PP/transport/config/compile/MoE/optimizer): 528 passed, 7 failed on the first run; rerun with watchdog-proof caches, 4 fail on both this branch and clean main `56a721b64` (`test_aux_loss` CPU-backend x2, `test_quantization` mxfp8 x2: environment), the other 3 were the watchdog pruning `/tmp/torchinductor_root`.
- Two-GPU quantile-balancing test: passes after `d0f7fb82a`.
- GPU smoke, 3 steps, venv_bfx9: dp1, dp1 + model compile, MTP, pp2 x vp2, tp2, DistMuon at dp2 -- all rc=0 with a step-3 loss.
- pyrefly: main 47 errors; this branch 89. The difference is the transport (35, of which 32 are `_NeighborP2PTransportMixin` attributes; all on the old integration head too), DEP's `dep_bubble_plan.py` (2), MTP's flops loop and the forward override (2, both on the old head), and one `flatten_args` assignment in `pipeline_stage.py` that PR 4312's own head `dbc425403` also carries in a full check.
- ufmt clean on every changed file.

## Open

- CP: port our CP commits to the merged API names (FlexInnerAttention / CPInnerAttention / KVAllGatherCPFlexInnerAttention / UlyssesCPFlexInnerAttention, transforms in config/transform), or build on upstream #4500; then move `k3_on_4025`.
- PR 4312: the `flatten_args` pyrefly error on `pipeline_stage.py` (full check only).

## Commits

```
4dc404ec4 kimi_k2_7: the vision tables keep the tower's tensor-parallel declaration under type checking
9da8028d2 kimi_k2_7: the MoonViT tensor-parallel plan is public, the projector norm named by the caller
52070b60e kimi_k3: tensor parallelism with sequence parallel on the declarations
11810d93b kimi_k3: a tensor-parallel integration cell in the b200 suite
d43f1d44d kimi_k3: accept any CUDA capability of 8.0 or newer for the KDA kernels
22e828d14 integration: text-decoder pipeline parallel (k3_pp_text dbc425403, as one change)
6531c8bfc distributed: create pipeline-edge NCCL communicators eagerly, before step one
2d456e354 distributed: opt-in pipeline transport on two-rank edge groups, CPU metadata and a collective vote
32ceb4359 integration: the neighbor transport composes with the stage class core builds and with K3's AttnRes rebuild
c9a57c64f integration: the typing import sits with the other imports
51b42b5b1 lora: adapter export, QLoRA (NF4 + packed MXFP4) and packed TP
410d87694 [kimi k3] LoRA, QLoRA-MXFP4 and QLoRA-linear debug flavors, with the experts tests
136eae454 lora: packed experts dequantize their LOCAL shard, the merge gathers explicitly
29e1e607e lora: the packed-experts dequant view derives its leading dim from the shard
e6713ace0 optimizer: a fully frozen model part gets no optimizer
7eb62eb0b lora: type the marker bases so the checker sees the built subclass
37c3aecd2 kimi k3: the mx_qat flavor moves to the QAT change
e423025dd lora: the packed experts carry the declared entry through to the packed pair
a7153235e lora: adapters of a base the declarations keep invariant on TP take its placement
918faf93c kimi_k3 pipeline: a payload whose blocks have nothing trainable upstream gets no gradient, and its deposits are discarded
44e4edf45 kimi_k3: ac_reuse_attention -- selective AC wraps only the MoE/feed-forward, attention activations are reused
c9b4a5c79 kimi_k3: the attention-residual computation is wrapped with checkpointing -- per-layer saved activations match the standard residual architecture
3f791249d kimi_k3: type the ac_reuse_attention hook for the checker
74d8618e7 kimi_k3: attn_res_cache_offload -- the rank store parks its blocks on pinned host memory
8d96ef2f4 kimi_k3: pp_balance -- PP ranks park saved activations on a peer through the Mooncake Transfer Engine
afd70c620 [kimi k3] MXFP4-weight / MXFP8-activation fake-quant QAT on the routed experts
a4b8b703c kimi_k3: port MTP to the integration tree
9be890876 kimi_k3: to_hf places the layer-0 attn-res placeholders on the stage that holds layer 0
5b1ac4f4e kimi_k3: the pipeline stage's backward honours has_backward
551b9da43 kimi_k3: to_hf leaves LoRA adapters out instead of raising
cd45e7b0f kimi_k3: the KDA and its short convolution take the packed stream's document offsets under flex attention
19f92eac9 kimi_k3: DEP -- the vision tower and the embedding on a pipeline stage of their own, with the encodes placed around the schedule's actions
1392f88bd kimi_k3 DEP: the docstrings say what the code does; the measurements behind the choices live outside the source
88658b87d kimi_k3: the prefetch timing spans are typed as CUDA events
5e092d63d kimi_k3: MoonEP as a moe_comm_backend, on the standard dispatcher seam
eca8f66f3 optimizer: a model part with nothing to train gets an empty optimizer and scheduler
42dacf93b kimi_k3: the released checkpoint format, checked against a released-layout artifact
0da59fd52 kimi_k3: compile each transformer block, with the KDA kernel wrapper carved out
840e52066 kimi_k3: a DistMuon recipe with per-head MLA and KDA layouts
af1b8156d integration: quantile balancing from #4577 (shuhuayu, head ebf6cc451), the eight commits as one change
73bc013ad tests: the quantile-balancing GPU test reads this tree's router signature
8235bbb56 lora: type the packed-experts factory so pyrefly sees the built subclass
b08c36698 lora: the packed-experts test lives in the CPU suite CI collects
38452c3a8 lora: NF4 imports follow torchao 0.18, where nf4_tensor moved
4b594ceef kimi_k3: the qlora_mxfp4_linear flavor goes; the LoRA flavor docstrings say what the flavor is
fd83da189 lora: drop the Replicate import the packed TP matmul no longer uses
a93e1574d integration: the local reorder reshapes the router outputs
d0f7fb82a integration: the quantile balancer imports DTensor
be4f5504d integration: ufmt on the files the replay touched
f201b9ac0 integration: type the transport-wrapped stage class for the checker
```

## CP on top: `k3_int_20260912_cp` (2026-09-13)

The user's call (2026-09-13): CP comes from upstream #4500 (acisseJZhong, head `35f2a6601` of 2026-09-11, one commit on main `15a45326f` where fegin's #4322/#4449/#4450 landed); PR 4313 is not carried whole, only its packed MLA kernels are ported onto it. Fork branch `k3_int_20260912_cp` = `k3_int_20260912` + 4 commits, worktree `/tmp/wt_intcp`.

| commit | what |
| --- | --- |
| `b6cb75270` | #4500 cherry-picked. Adapted: sharding stays the TP stack's (its KDA/MLA boundaries already carry the cp axis through `local_spmd`; #4500's `LocalMapConfig` declarations dropped); block and model forwards keep the PP/MTP/DEP arguments next to `kda_cp_routing` / `vision_bank_indices_T`; SPMD input annotation unconditional (no `spmd_backend`); attn_gym context-parallel imports lazy (non-CP runs work on attn_gym 0.0.8; CP needs attn_gym@main, #4500 bumps the pin); CP with TP or PP refused until measured; capability guard stays >= 8.0 |
| `103fbc12c` | the vision tower declared on the cp axis too (`set_moonvit_sharding_config(include_cp_axis=...)`, K2.5 default unchanged): without it FSDP found plain tower parameters under CP |
| `7b72c8498` + the contiguity commit | packed MLA kernels (`kimi_k3/cp_mla.py`) on main's `KVAllGatherCPFlexInnerAttention` / `UlyssesCPFlexInnerAttention`, `MLAContextParallelTransform` (per-layer rope width), two b200 cells, a CPU test |

Runs (5060, venv_bfx9, attn_gym@main on PYTHONPATH from `/workspace/pylib/attn_gym_main`, one seed checkpoint, 3 steps, type checking off -- see Open):

| cell | step 1 loss / grad norm | step 2 | step 3 |
| --- | --- | --- | --- |
| dp1 | 12.64048 / 25.3750 | 11.64012 / 44.2500 | 9.91138 / 21.0000 |
| #4500 all-gather K/V cp2 (headtail) | 12.64048 / 25.3750 | 11.57963 / 43.0000 | 9.95032 / 20.2500 |
| packed all-gather K/V cp2 | 12.64048 / 25.3750 | 11.57963 / 43.0000 | 9.95032 / 20.2500 |
| #4500 Ulysses cp2 (twice: identical) | 12.64048 / 25.3750 | 11.59816 / 43.2500 | 9.92561 / 20.2500 |
| packed Ulysses cp2 (before the contiguity commit) | 12.64048 / 25.3750 | 11.78811 / 43.5000 | 10.12928 / 21.5000 |

Kernel-level check (`matrix_scripts/tp_h100_v2/cp_mla_gradcheck.py`, two ranks, real collectives, full mask, packed vs main's generic kernel):

| kernel | dtype | out | dq | dk nope | dk rope (summed over heads) | dv |
| --- | --- | --- | --- | --- | --- | --- |
| Ulysses | fp32 | bitwise | bitwise | bitwise | 6.7e-8 | bitwise |
| all-gather | fp32 | bitwise | bitwise | bitwise | 6.7e-8 | bitwise |
| Ulysses | bf16 | bitwise | bitwise | bitwise | 4.4e-3 (1 ulp) | bitwise |
| all-gather | bf16 | bitwise | bitwise | bitwise | 4.4e-3 (1 ulp) | bitwise |

The rope gradient is the one place the packed kernel sums in a different order (each rank sums its heads, the reduce-scatter adds the ranks); Adam's first step (about lr x sign(g)) turns such ulp differences into the step-2 gap above, as between dp1 and CP. Before the contiguity commit the split views made the compiled flex attention pick a different kernel (1 ulp in the bf16 output too).

### Open

- With SPMD type checking on, every CP recipe stops in KDA's gated output norm (`kda.py` KimiRMSNormGated): an operand carries Varying on the cp axis (named `default_pg` at cp = world) with no PartitionSpec entry. Not the KDA inner boundary (`attention_activation_placement` carries (DP, CP) in its spec); needs a probe.
- #4500's own KDA GPU tests are gated to SM90/SM100/SM103: run them on H100.
- CP with TP / PP: refused; not measured.
