# 交接(2026-08-26 凌晨)

上一份是 `HANDOFF_CLEANUP_2026-08-25.md`(Windows 侧)与 `HANDOFF_2026-08-25.md`。
这一份覆盖 GPU 侧从"重切三支"到"PP adapter 移植完成"这一段。

## 一、这段时间最重要的一件事

**PP PR 的核心代码从未被执行过,现已修复并首次测出。**

cross-stage cache adapter 与 `layout.py` 共约 900 行,是 PP PR 最有技术含量的部分。
在 2026-08-26 之前,**我们的每一个 PP 格、以及老树的 58 格,走的都是朴素传输**(整个
块栈随 P2P 逐跳传递),adapter 一行没跑过。

判据不是推断。代码里有一行专为此存在的日志:

    # Say so on success, not only on fallback: the adapter is numerically
    # neutral by design, so without this line "wrapped" and "silently fell
    # back" are indistinguishable from the outside.
    logger.info("cross-stage cache adapter wrapped %d stage(s): %s", ...)

它在此前全部 26 个 measure 日志里出现 **0 次**。

老树同样从未进 delta 模式(`CODE_REVIEW_20_VERDICTS_2026-08-16.md:10-18`),但老树另有
独立的 delta 对照实验;4025 这棵树上一次都没有。

## 二、七处不匹配,同一根源:adapter 从未适配折叠布局

| # | 假设 | 4025 实际 | 修复 commit |
|---|---|---|---|
| 1 | `attn_res_cache` 可从 config 设 | 从未声明为字段;退休 env 兜底后彻底不可达 | `982a9e037` |
| 2 | 默认关 | PP 下 delta 才该是默认,整栈是回退 | `ddc05c911` |
| 3 | `submod.num_blocks`/`layers_per_block`/`config.num_hidden_layers` | 三个都不存在(老树 wrapper 模型的属性) | `16b1c99c8` |
| 4 | 载体走 `blocks=` 关键字 | 第二位置参数 `block_residual_TND` | `7606e3cc7` |
| 5 | token 数 = `shape[0]*shape[1]` | 折叠成 `[T,D]`,乘出来 = T×D = 262144 | `7606e3cc7` |
| 6 | 可把 adapter 塞进 `model_parts` | 破坏 `_skip_lm_head`;并给 checkpoint key 加前缀 | `7606e3cc7` |
| 7 | 模型返回"本段新增的块" | 返回**累积载体**,新增块是尾部差值 | `7606e3cc7` |

**逐层剥**:第 3 层通了第 4 层才暴露,第 4 层通了第 5 层才暴露。每层都靠**探针打印
边界处的实际形状**定位 —— traceback 指向消费者,缺陷在生产者。探针代码已全部拆除。

第 6 条的 checkpoint 前缀此前从未暴露:adapter 一旦生效,存出的 key 与不开时对不上。

用户提供的历史背景:JZ 提 PR 时本地就说过 PP 这个接口要改,只是没在他 PR 下留 comment,
一直在推老树。`CODE_REVIEW_20_VERDICTS_2026-08-16.md:429-448` 记的是**相邻**的另一个坑
(shape inference 产出的载体秩不对),结论写着 "Fixed",读起来像整块都清了。

## 三、分支与树的当前状态

已推(fork `QIU023/torchtitan`),**三个 PR head 都不带 4135**:

| 分支 | origin |
|---|---|
| `k3_pp_text` | `e5177bf0e` |
| `k3_cp_text` | `3557bbe7d` |
| `k3_ep` | `86708027f` |
| `k3_on_4025` | `7606e3cc7` |

本地 worktree 各多一个 4135 cherry-pick(第二表用),**不要推**:
`tt_pptext` `4765eda53` / `tt_cptext` `5c75b170b` / `tt_ep` `d30649390`。

`k3_on_4025` 本地另有一条 rebase 到 `30eb5e502` 的线(`1ebd9e8a6` + 后续),**未推** ——
用户在未 rebase 的线上继续推提交,推不推由用户定。备份分支 `k3_on_4025_pre_rebase_0825`。

logbook `7dacab1`。

## 四、证据状态

| PR | 主表(无 4135) | 第二表(有 4135) | 正文 |
|---|---|---|---|
| **CP** | 7 格 ✅ | 7 格 ✅ | 已写 `de1f81f` |
| **EP** | 7 格 ✅ | 7 格 ✅(ep4 因端口冲突重跑过) | 已写 `6aac6e0` |
| **PP** | 10+7 格 ✅ | 10+7 格,回退组还剩 4 格 | 第一张已写 `931238b`,第二张待补 |

PP 主表核心结论:**九个流水线格 step-1 与 dp1 逐位相同**(12.48548),4–32 段、2–8 卡,
六格由 adapter 驱动。开 cache 的 vp 格 step-2 相对 dp1 是 5.1e-5 ~ 7.0e-4,而
**不开 vp 的 pp2 相对 dp1 就是 3.5e-4** —— 同量级。内存:`pp8×vp4` 峰值 8.50 → 6.60 GiB
(−22%),极差 5.84 → 3.98。

正文格式(用户定):第一张表标注无 4135,第二张标注有 4135,**关于 4135 只留一句**。

## 五、待办

1. **PP 第二表**收尾(回退组 4 格)→ 补进正文
2. **多模态**:动态 CP(`kimi_k3_debugmodel` vs `_dyncp`,各 dp1/cp2/cp4)与 **DEP**
   (`vit_dep` 现已可达)。**先单格烟测确认代码路径真的走到再铺矩阵** —— PP 的教训
3. `k3_on_4025` rebase 推不推
4. 多模态两个分支 `k3_pp`/`k3_cp` 尚未 rebase、单测仍在顶层收不到

## 六、待查(不进正文)

* **`pp8` 在 4135 下的表现随修复而变**:`gn2`(修复前)pp2=pp4=pp8=7.87346;
  `gn3`(修复后)pp2=pp4=7.87346 但 **pp8=7.91590**。两轮 dp1 完全相同(同 seed,
  对比合法),pp8 走回退路径且不带 4135 时修复前后逐位相同。未解释。
  区分方法:pp8 单格重跑看是否复现(8 rank 规约可能不确定)。
* **与老树的 delta 差异**:老树记 12 格 delta 与 naive **逐位相等含反向**,
  我们测到反向 s2 = 1.5e-4 ~ 6.4e-4。未验证,不作任一侧结论。

## 七、这段时间固化的纪律

* **中间信号 ≠ 结果**。看到 wrap 日志就报"adapter 跑通了",而 `results.txt` 里
  s1/s3/s10 三个空格子当时就在提示它崩了。同类错误当晚犯了三次(另两次:把过期的
  监控回放当新数据;`grep -c 'wrapped'` 数出 8 就说回退组也在 wrap,核到确切短语才
  发现是别的词)。**以 `results.txt` 为准,核到确切来源。**
* **force-push 前必 fetch 比对远端 head**。曾覆盖用户两个 commit(`d7c884b3c`、
  `55f82b30a`),已恢复;此后 push guard 拦下过一次。
* **一张表 = 一次 run**。跨 run 不相减 —— cp8 假越界、`fsdp2_cp2` 假异常都是这么来的。
* **每张表自带同 dp 度的纯数据并行行**。不开任何轴,step-2 就有 1.2e-2 ~ 1.6e-2。
* 判据只用 **step-2**;step-3/step-10 被混沌主导,同一格能在两步间差一个数量级。

## 八、2026-08-26 03:20 追加:多模态起步

`k3_on_4025` 现在 `0e85d2b09`(origin)。新增 `kimi_k3_debugmodel_dep` flavor,
形状抄 `kimi_k3_debugmodel_dyncp`。

**DEP clause 1 已验证生效**,而且是按 PP 那次的教训先做单格烟测、确认代码路径真的
走到才铺矩阵:

| | stage 0 | stage 1 |
|---|---|---|
| 无 DEP | `tok_embeddings` + layers.0-11 | layers.12-23 + head |
| 有 DEP | `tok_embeddings` + **`vision_encoder`**(零层) | **layers.0-23** + head |

塔独占一段、不带任何解码层,正是 report 5.2.3 clause 1。两组都 exit 0、跑满 2 步。

日志里的 `DEP vision stage wiring: 1 stage(s) on this rank, roles []` 中 `roles` 为空
是**预期**:角色表是 clause 2(塔跨多段)用的,clause 1 下没有角色要分配。不要把它
读成"没生效" —— 判据是 stage 划分,不是这行。

**PP 第二表已完成并写入正文**(`49d9772`)。结果值得记:有 4135 时,
**传输关闭的六个 vp 格塌成同一个值 7.87346**,传输开启的六格保持分散
(7.90505 … 7.95831)。补丁去掉的是"总范数依赖参数怎么分组";每跳送多少 delta 取决于
怎么切,以不同顺序求和是补丁碰不到的算术。这条同时收口了"vp 格彼此不同是不是缺陷"
那个疑问 —— 关掉传输它们完全一致。

三份正文现在各有主表(无 4135)+ 第二表(有 4135);PP 另有六对 delta/naive 配对表和
内存表。

**下一步**:动态 CP 矩阵(`kimi_k3_debugmodel` 阈值 256 不触发 vs `_dyncp` 阈值 64 触发,
各 dp1/cp2/cp4)与 DEP 矩阵(`kimi_k3_debugmodel` vs `_dep`)。两者都还没跑。
