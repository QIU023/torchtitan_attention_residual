# PR 4312's head rebased onto torchtitan main a3a819c67 (2026-09-18)

## What was rebased, and the correction that got there

The first attempt rebased `pp_review5`, on the understanding that it was the PP PR's head. It is not. `de6f29514` is the head of PR 4312, confirmed by fetching `refs/pull/4312/head` fresh and getting the same object; it is also the tip of `origin/k3_pp_text` and `origin/pp_review4`. `pp_review5` branched off the 2026-09-04 main three days earlier and is 101 commits behind that head, and every identifier its 23 commits carry is already on the head at equal or greater spread (`PPRankLocalCache`, `layer_to_stage_from_split`, `self.first_layer_in_block`, `first_stage_modules`, the config exclusivity message, `AttnResPipelineStage`). `pp_review6` sits on the PR's own line, twelve commits behind it. So the first replay was done on a superseded sibling and is discarded.

The real job replays 37 commits from merge base `d9ca9e55a` onto `a3a819c67`, with 62 upstream commits in between. `git merge-tree` predicted five conflicting files, exactly the set the 4312 follow-up note recorded. Result: `ab8ea5f61`, 37 commits, 16 files, +1525/-34.

## How each conflict was decided

Every resolution was taken from the commit's own diff rather than from the shape of the conflict, because the conflicts are mostly diff alignment: git pairs a line the commit deletes against an unrelated neighbour. Five conflicts turned out to carry old-base text that upstream has since dropped, and taking "our" side wholesale would have reinstated it:

    ("tensor parallel", tp_enabled)      upstream supports K3 TP now; the commit only deletes the pipeline entry
    TODO: Add TP Support                 zero occurrences on upstream/main and on the integration tree
    from .moe import KimiFeedForward...  upstream narrowed that import to KimiLatentMoE
    spmd_backend validation              the field is gone from ParallelismConfig; it appeared three times
    from torchtitan.tools.logging        the module moved to torchtitan.observability.logging

Each was checked against upstream, against the integration tree, and against the commit's own added lines before being dropped. Two resolutions needed a positive fix rather than a choice: `parallelize.py` gets `import logging` plus `logger = logging.getLogger(__name__)` in place of the moved module, and the `__all__` in `pipeline_parallel.py` keeps upstream's logger assignment while taking the commit's widening.

## The gate that made this checkable

Every replayed commit was compared against the original commit's own per-file numstat. Thirty-three files matched exactly. Four differed and each was traced line by line before being accepted:

    commit 6   model.py +34/-21 against +32/-20   upstream's one-line docstring reopens as multi-line
    commit 13  model.py +0/-4 against +0/-5       a blank line inside the docstring, see below
    commit 19  pipeline_parallel.py +35/-2        an import line upstream already had, merged away
    commit 32  parallelize.py +92/-11             one blank line between import groups

A harness refuses to stage anything unless the resolver exits zero, no file keeps a real conflict marker, and every modified file parses. That gate exists because an earlier run committed three conflict-marker lines into five commits: the resolver raised, the shell carried on, and `git add` staged the unresolved file. The audit also resolves every `from torchtitan...` import against the commit's own tree, which `ast.parse` cannot do; a package directory with no `__init__.py` counts as resolvable, since `torchtitan.tools` is a namespace package.

## Verification of the finished head

    37 commits on a3a819c67, no rebase in progress
    conflict markers in any commit            0
    python files that fail to parse           0
    unresolvable torchtitan imports           0
    Co-Authored-By / Claude-Session trailers  0
    CPU tests                                 90 passed, 2 failed

The two failures are `test_flux_config_via_cli` and `test_suppressed_model_spec_is_opaque_to_tyro`, both raising `ImportError: Cannot import config_registry for module 'flux'`. They fail identically on a control tree that sits on the same main without this replay, so they are this box's missing `transformers`, not the rebase. At the head no file imports `torchtitan.tools.logging` or `torchtitan.tools.profiler`, `RankStore` and `kimi_k3_mm_fsdp` are gone, and the only `spmd_backend` left is in upstream's own `tests/unit_tests/gpu/test_dist_gemm.py`, which this PR does not touch. `layout.py` is byte identical to the integration tree's copy.

## One defect still standing

Commit 13 leaves `_apply_attention_residual`'s docstring as a summary line, a blank line, then the closing quotes. The commit removes a paragraph and the blank that separated it; my resolution removed the paragraph only. A checker written for that exact shape finds this one instance in the replay and none on upstream/main, and nothing after commit 13 rewrites the region, so it reaches the head. Fixing it properly means replaying commits 13 to 37 with the corrected resolver, which is mechanical since each of those resolutions is scripted and gated. Left for the user to call.

## The smoke

The PR ships its own two pipeline configurations in `torchtitan_recipes/tests/b200.py`: `kimi_k3_debugmodel_pp2_vp2` (the shared 24-layer model, 26 units, pp 2, Interleaved1F1B, 4 micro-batches) and `kimi_k3_debugmodel_pp8_vp4` (its own 33-layer model, 35 units, pp 8, 8 micro-batches, with the split spelled out because no `layers_per_stage` reaches 32 stages for 35 units). Those are what a smoke runs, rather than the older `rebase_main_pp33_matrix.sh`, whose cells were sized for a 33-layer flavor that no trainer config on this head selects. `--config` reaches them through a fully qualified module path, and neither enables SPMD typechecking, which is unsupported under pipeline parallelism anyway. The KDA guard lift for this box is parked at `matrix_scripts/pp4312_local_hacks_2026-09-18.patch`.

## The smoke, and what it covers

The PR's whole Kimi K3 pipeline surface on this head is two integration cells, both in `tests/integration_tests/b200.py`. The ten pipeline configurations still in `torchtitan_recipes/tests/features.py` are llama3's; `kimi_k3` appears zero times in that file, because commit 28 moved the K3 pipeline cells into the b200 suite. So two cells is the whole matrix here, not a subset of the older ten-cell script, whose cells were sized for a 33-layer flavor that no trainer config on this head selects.

Both ran on the rebased worktree with the KDA guard lifted for this box, three steps, cold caches, rc and step count as the verdict:

    cell         gpus  rc  steps  loss 1 / 2 / 3
    pp2 x vp2    2     0   3      12.34963 / 10.93338 / 8.04338
    pp8 x vp4    8     0   3      12.59161 / 11.15744 / 8.35423

Every rank built its own stage, rank 0 taking the vision tower, and both runs ended on "Training completed" with the process groups destroyed. The pp8 cell took about four and a half minutes. Nothing in either log is a failure: the recurring lines are `lspci` missing, peak flops undefined for an RTX 5060 Ti, and NCCL's async-error-handling override.

## CPU tests, with a control

The full `tests/unit_tests/cpu` directory on the rebased head reads 1047 passed, 19 failed, 11 skipped, 83 subtests passed, and 5 files that cannot be collected. Every one of the 19 was compared by name against the same files on a tree that sits on the same main without this replay:

    failing only on the rebased head    0
    failing on both                     19
    failing only on the control          1

So none of them is caused by the rebase. They are this box: the five uncollectable files need `transformers`, and the rest need `flash_attn` or a torchao carrying the 32x32 swizzle kernels. The PR touches none of the files those tests live in, nor the sources they exercise; its sixteen files are the pipeline, the K3 model folder and the config.

The box is left clean: eight GPUs at idle, no process holding GPU memory, 65 GB free.

## The reading that would have saved the first attempt

`PR_BODY_PP.md` already recorded which branch backs the PR, in a line dated 2026-09-11: the sync onto `k3_pp_text` took `pp_review4` = `66601a7fb`, "not `pp_review5`". Reading the PR's own body before touching a branch would have settled the premise in one step, instead of a full replay on a superseded sibling followed by a fetch of `refs/pull/4312/head` to prove it. The order to follow next time is: read the body for the branch it names, confirm with the PR ref, then rebase.

The four `REPLY_4312_*` drafts are posted material, answered by 09-13, and are left untouched. Only the body is live, and it now carries a dated note listing the rows that need an H100 rerun against the new base, with today's two cells marked smoke only.

