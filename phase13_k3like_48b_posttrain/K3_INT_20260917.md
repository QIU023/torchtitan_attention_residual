# Integration tree on main `a3a819c67` (2026-09-17)

## Rebase

`k3_on_4025` = `cf7418637` (81 commits on main `810e62786`) rebased onto `upstream/main` `a3a819c67`, eight commits newer (#4577 quantile balancing and #4499 TP were already in the old base; the new ones include #4759 typing in traverse and the `renderers` dependency). Rebased head `31a1b39fa` in the detached worktree `/tmp/wt_int0917`.

One conflict, in `torchtitan/config/transform/context_parallel.py`: main's #4759 dropped the `cast(BaseAttention.Config, traversed)` in the single-type loop while the tree's `support CP for KimiK3` (`45f57dadb`) had replaced that loop with one over a `{config_type: replacement}` mapping that walks parents. Resolved to the tree's loop with main's typing (no `cast`, and the now unused `from typing import cast` import removed), which is what both changes wanted. Every other commit applied unchanged.

## Environment

New main needs `renderers==0.1.11`; `expecttest` is needed by any `DTensorTestBase` test. Both installed into `venv_bfx9`, which the matrix uses.

## CPU tests on the rebased tree

`pytest tests/unit_tests/cpu -k "kimi_k3 or context_parallel or transform or lora or moon_ep or token_dispatcher"`: **153 passed, 1 skipped, 32 subtests passed, 1 failed**.

- The failure is `test_varlen_attention.py::TestPackedVarlenInnerAttention::test_out_transform_receives_th_lse`, `ModuleNotFoundError: No module named 'flash_attn'`. The same test fails the same way on a pristine `upstream/main` worktree, so it is this box's missing package, not the rebase.
- Five files cannot be collected at all on this box and were excluded: `test_dataset_flux.py`, `test_debug_config_defaults.py`, `test_flux_sharding.py`, `test_torch_checkpointing.py`, `test_validate.py` (import errors from packages the box does not carry, `torch_checkpointing` among them; the same on main).

## Matrix

Running as `mx4_int0917_0917_121812` (`matrix_scripts/run_mx4_int0917.sh`, tree `/tmp/wt_int0917`, the same seed checkpoint and batch as the 09-16 run so step 1 is comparable). Comparison tool: `matrix_scripts/mx4_compare.py`, which aligns two results files by cell and reports step 1 first; it was self-checked against the baseline, 21 of 21 identical.

At 13:55Z, 13 of 21 cells are done and **step 1 is identical to `mx4_int0916c` in every one of them**.

Two cells move at steps 3 and 10 while step 1 holds: `fsdp2_tp2_pp2` (7.58214 to 7.60699, 3.36065 to 3.34876) and `tp2_pp2` (7.84468 to 7.83963, 3.38123 to 3.36004). That is these two cells' own behaviour, not something the rebase introduced: the 09-16 record already carries the same pair moving between two runs of the **same** tree, `tp2_pp2` reading 7.78131 / 3.36728 in the morning against 7.84468 / 3.38123 in the matrix, and `fsdp2_tp2_pp2` reading 7.58042 / 3.32105 against 7.58214 / 3.36065, both annotated "step 1 identical". Today's spread is the same size. Step 1 is the correctness bar and it did not move, so the locate-before-writing rule is not triggered; no mechanism is claimed for the later steps.


Launcher ready: `matrix_scripts/run_mx4_int0917.sh` (the 09-16 cell list against `/tmp/wt_int0917`, tag `int0917`). Every flavor and recipe it names exists on the rebased tree (`kimi_k3_debugmodel`, the b200 recipes for pipeline and the two CP backends, the three `min96` probe flavors in `cpmm_probe.py`). It waits for the GPUs, which are running the veRL image cells; step-1 of every cell is compared against `mx4_int0916c`.

## Push preconditions, checked at 14:00Z while the matrix runs

Everything the push needs is in place, so a green matrix can be followed straight by the push and the tag:

- The rebased worktree `/tmp/wt_int0917` is clean at `31a1b39fa`, 81 commits on `upstream/main`.
- The remote `k3_on_4025` is still `cf7418637`, which is not an ancestor of the rebased head, so the push is a `--force-with-lease` against that value, as any rebase requires.
- The tag name `k3_int_20260917` is free both locally and on the remote.
- 62 trailer lines appear in those 81 commit messages, and they stay. The no-trailer rule covers the branches that reach upstream, the PR and review branches such as `k3_moonep_seam` and `k3_ac_reuse_attention`; the integration tree and the logbook keep theirs. None of the 62 is new tonight either: the rebase carried the original messages across unchanged.
