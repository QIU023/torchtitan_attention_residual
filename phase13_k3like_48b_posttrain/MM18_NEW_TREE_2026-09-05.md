# The 18-cell multimodal matrix on the new tree (2026-09-05)

The question: with TP/SP on the declarations, can the new tree (main `390e2985b`, spmd_types
default) run the 18 cells of `MATRIX_18_CORRECTED_2026-08-09.md`? Not from any one branch:
CP stacks on the declarations, not on TP/SP, and PP sits on a rebase nobody has combined with
either. So one scratch integration branch, `mm18_int` (fork, `40bb5bbb7`):

    tpsp_review3 c0ab32d07 (declarations + clip grouping + TP/SP + tower typing)
      + cherry-picks of cp_review5's three commits (stack copy, CP layer, gym port)
      + merge of pp_rebased_local bf1d06dff (the PP line on main 60d0f7a25)

## What the merge decided

- `model.py` `update_from_config`: TP/SP's `enable_sp` stays; CP's KDA-kernel check stays; the
  declarations are issued at `tp > 1 or spmd_types` as TP/SP wrote it.
- `model.py` vision splice: the CP path comes first (the rank's slice of the tower output,
  masked-scattered into its sequence shard); the SP path (gather the shard, splice whole,
  cut back in `forward`) second. CP together with SP is refused (`NotImplementedError`, the
  CP branch's own stance for the DTensor case): the splice under SP needs the whole sequence,
  which a CP shard does not have. So the three TP x CP cells run with
  `--parallelism.no-enable-sequence-parallel`.
- `model.py` `forward`: PP's incoming block stack (`block_residual_in`) plus the declarations'
  `multimodal_context` prep, SP re-shard and type asserts; the stack starts from the received one
  on a later stage, else from the empty cut of the stream (the layout carrier).
- `parallelize.py`: no parallelism is listed unsupported any more (TP/SP's list said PP, PP's
  said TP and CP); the CP hook and the PP split / `pipeline_kimi_k3` both stay.
- The two `features.py` lists keep both the cp2 and the pp8 x vp4 cells.
- 33 CPU tests (PP layout, stage, FQN injection; CP attention) pass on the merged tree.

## The run

`matrix_scripts/mm18_matrix.sh`: `kimi_k3_debugmodel` (33 layers on this tree), seed 42,
`--debug.deterministic`, 4096 tokens per step in 256-token micro-batches (the PP tables' batch,
seed checkpoint `.mx3_seeds_main33/..._49ffe72a4588` shared with them), 8 pipeline
micro-batches, spmd_types on every cell, Attention Gym `b19162e` on `PYTHONPATH`, per-cell warm
pass then a 10-step measure pass. The CP cells use the `kimi_k3_debugmodel_cp2` recipe flavor
with the degree overridden where needed; `--config` finds it through the run worktree's local
registry alias (the same uncommitted hack the CP matrices' worktree carries, `matrix_scripts/`
does not add flavors to the branch). The first cp4 pass ran before the alias was copied in and
failed at config lookup; it was rerun (`mm18_cp4_rerun.sh`). Three streams: 2-GPU cells on GPUs 0-1 and 4-GPU
cells on GPUs 2-5 concurrently, the nine 8-GPU cells serial afterwards.

## Results

18/18 ran (2026-09-05 23:43 to 2026-09-06 03:10, paused between 11 and 12 for the review round;
`mm18_matrix.sh`, `mm18_cp4_rerun.sh`, `mm18_resume.sh`; run dirs `/workspace/mx3_mm18_*`). Every
cell under `spmd_types`, none needed `partial_dtensor`. Rows are grouped by data stream (the loader
shards documents by dp rank); the cells ran in several invocations with their own compile caches,
so step 1 is the comparison and step 3 / 10 carry the flavor's own floor.

dp = 1 (one data stream; PP cells bitwise dp1 at step 1, as a pipeline reorders nothing there):

| cell | world | step 1 | step 3 | step 10 |
| --- | --- | --- | --- | --- |
| dp1 | 1 | 12.41967 | 7.49054 | 3.46681 |
| pp2 | 2 | 12.41967 | 7.57102 | 3.37227 |
| pp4 | 4 | 12.41967 | 7.61773 | 3.36642 |
| pp8 | 8 | 12.41967 | 7.54141 | 3.42905 |
| tp2 | 2 | 12.40921 | 7.11185 | 3.43252 |
| tp4 | 4 | 12.40089 | 7.52885 | 3.32423 |
| cp2 | 2 | 12.39522 | 7.26284 | 3.47249 |
| cp4 | 4 | 12.38998 | 7.33331 | 3.29343 |
| tp2 x pp2 x cp2 (SP off) | 8 | 12.39219 | 7.54896 | 3.49866 |

dp = 2:

| cell | world | step 1 | step 3 | step 10 |
| --- | --- | --- | --- | --- |
| fsdp2 | 2 | 12.40417 | 7.36395 | 3.32191 |
| ep2 x fsdp2 | 2 | 12.40257 | 7.39607 | 3.33510 |
| fsdp2 x tp2 x pp2 | 8 | 12.39824 | 7.28017 | 3.45290 |
| ep2 x fsdp2 x tp2 x pp2 | 8 | 12.40022 | 7.56348 | 3.45623 |
| fsdp2 x pp2 x cp2 | 8 | 12.38701 | 7.61203 | 3.29409 |
| ep2 x fsdp2 x pp2 x cp2 | 8 | 12.39003 | 7.59716 | 3.40360 |
| fsdp2 x tp2 x cp2 (SP off) | 8 | 12.38612 | 7.53708 | 3.30439 |
| ep2 x fsdp2 x tp2 x cp2 (SP off) | 8 | 12.36745 | 7.27280 | 3.39085 |

dp = 8:

| cell | world | step 1 | step 3 | step 10 |
| --- | --- | --- | --- | --- |
| ep8 x fsdp8 | 8 | 12.39792 | 8.02794 | 3.26490 |

Reading: the TP cells start 1e-2 below dp1 (sequence parallel gathers and shards the stream, the
KDA head split and the tower run on other shapes, and the random-init router flips on the rounding,
the mechanism of `CP_STEP1_2026-09-06.md`); the CP cells 2e-2 below (the first KDA layer under KCP,
same note). New ground on this tree, all first runs: TP x PP under `spmd_types` (two cells), PP x CP
(two), TP x CP with SP off (three), and PP itself on `spmd_types` (the PP line ran `partial_dtensor`
before the declarations merged in). Nothing here is a correctness proof beyond step 1: the same-data
pairs with and without each change are the PRs' own tables.

