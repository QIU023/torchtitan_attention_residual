# Handoff: ViT DEP, veRL, ViT CP, LoRA TP

State as of 2026-08-06. Repos: fork `QIU023/torchtitan` `attention_residual_dev`
at `ad7813328`, `QIU023/verl` `kimi_k3_integration` at `412e5c31`, logbook `main`
pushed. `kimi_k3` suite 266 passed.

---

## ViT DEP -- design only, and the objective changed

Not implemented. Design in `phase13_k3like_48b_posttrain/VIT_DEP_DESIGN_2026-08-06.md`.

**The task was posed as "put the ViT compute into the PP bubbles", and that is the
wrong thing to optimise.** Bubbles are a fixed O(P) cost -- warmup plus cooldown,
independent of the microbatch count. The vision encode is proportional to M, since
every microbatch carries images. So as M grows the bubble fraction shrinks while
the tower's share does not. The real win is that today the whole tower runs on the
stage owning `embed_tokens` while P-1 stages wait, and at real scale MoonViT-V2 is
447.4M against k3mini's 80.9M text side. The bubble is just where the spare
capacity happens to sit.

**Why the naive version cannot work.** Stage 0 needs microbatch 0's features at the
START of its first forward; stages 1..P-1 are idle at that moment precisely because
they are waiting for stage 0. Formally stage k is idle for t in [0,k) and stage 0
needs microbatch j at t~=j, so stage k can only serve j >= k and must finish inside
k slots. For small j that is one or two slots and the tower's forward does not fit.

**Design A -- parallel prologue (recommended).** Before the action list starts,
every PP stage encodes a disjoint slice of the batch's images; one collective on
its own communicator; schedule untouched. Tower time `T_vit -> T_vit/P + gather`.
It does not overlap the bubble -- it shortens the prologue, which is the deliberate
trade for keeping the property the AttnRes adapter is built around, that PP owns
all NCCL.

**Design A is the ViT CP mechanism with the group swapped**, and that code is
landed and verified (below). The real prerequisite is tower weights on every PP
stage -- today only stage 0 has them, and `pipeline_adapter` states "nothing
vision-side crosses a stage". Replicating 447.4M across 8 stages does not scale;
FSDP-sharding the tower along the PP axis does, and is a new mesh interaction.

**Design B -- true interleaving.** Three reasons not to reach for it first: benefit
bounded by (M-J0)/M and vanishing for short pipelines; delivery runs AGAINST the
pipeline direction (stage k -> stage 0) and no such channel exists; and it requires
sends interleaved with the schedule's action list, which is exactly how PP
deadlocks and gives up the adapter's deadlock-free property.

**Not in the way:** the AttnRes cross-stage adapter is orthogonal. Verified
bit-identical on and off, multimodal PP8xVP4, seq 1024 through 8192.

**Sequence to implement A:** (1) shard the tower across the PP axis; (2) make the
CP image-sharding helper take a group argument so one implementation serves both
axes; (3) A/B as CP was verified -- exact equality is the expected result, since
distributing images changes no arithmetic.

---

## veRL -- text GRPO runs; QAT multimodal SFT runs; multimodal GRPO does not

### Done

* **Text GRPO, one step end to end.** `actor/entropy 7.506`, `reward 3.11e-05`,
  `rewards/mean 0.0` (correct: gsm8k exact match on a random-init fixture is 0),
  reached Final validation. A dataloader worker was then OOM-killed at teardown --
  host RAM pressure from Ray + vLLM + titan co-resident, not a pipeline defect.
* **QAT multimodal SFT.** MXFP4 weights / MXFP8 activations, multimodal model,
  through veRL on the torchtitan engine: `train/loss 12.05791 -> 11.87875`,
  `val/loss 11.53741`, with `MXFP4 QAT (K3 official scope): 12 routed-expert
  modules, MXFP8 activations on` and `text_only=False` (406 keys, 30 vision).
  Runner: `phase13_k3like_48b_posttrain/qat_mm_sft.sh`.
* **Weight-sync contract** against vLLM's official K3 implementation: 1032 tensors
  accepted, generation end to end, no shim, on a real source build.

### Fixes that got there (all in the fork, all real assumption mismatches)

| where | what |
|---|---|
| verl third_party + `vllm_async_server` | `VERL_VLLM_VERSION` override -- a source-built vLLM reports `0.1.dev1+g<sha>`, so every gate takes its oldest branch |
| verl `attention_utils` | fall back to transformers when `flash_attn` is absent; those four are index/gather utilities, not attention kernels |
| verl torchtitan engine | reach flavors defined as config_registry FUNCTIONS -- previously every debug/report-arch/QAT flavor was unreachable |
| verl `tokenizer` | accept `KimiK3Processor`: that match arm binds `get_rope_index`, and K3 is NoPE so there is nothing to bind |
| verl `multiturn_sft_dataset` | tolerant `patch_size` read -- `KimiK3VisionProcessor` nests settings under `media_proc_cfg`, and the config override cannot help because a default argument is evaluated eagerly |
| titan `state_dict_adapter` | handle a multimodal state dict at all; decide the prefix from the STATE DICT not the config; and the expert path must honour the wrapper prefix too |

The expert one is worth remembering: `_tt_key_to_hf` prefixes every key, but stacked
experts are split on a separate path hardcoded to `model.layers.{}...`, so a
multimodal model emitted experts as `model.*` and everything else as
`language_model.model.*`. That the failures were ALL and ONLY expert keys is what
pointed there rather than at the prefix logic, which was choosing correctly.

### Open

* **Multimodal GRPO.** vLLM's multimodal rollout now reaches backend selection --
  `tokenizer_mode=kimi_k3`, remote code loaded, processor built -- and stops at
  `No valid MLA prefill backend found` for the DEBUG model's head dimensions. A
  fixture limitation; those kernels target real K3 dims. Needs a real-dims model,
  an image-bearing RL dataset and a reward.
* Text GRPO beyond one step: multi-step stability, a task with non-zero reward,
  and weight sync (`get_per_tensor_param` -> vLLM) measured inside the RL loop.

---

## ViT CP -- landed and verified (`57c6728ed`)

Under CP the vision tower was **replicated**: text tokens were sharded but
`pixel_values` was not, so every CP rank encoded the whole batch's images and
discarded what its sequence shard did not need. Now each rank encodes a disjoint
round-robin slice and the features are shared.

**No new mesh needed**, which our notes had recorded as the blocker: the unit of
work is the IMAGE, so the assignment is per-batch while the group stays the static
`_cp_group`. Sizing needs no extra communication either -- `grid_thw` is
replicated, so every rank knows every output length up front and the exchange is a
fixed-shape `funcol.all_gather_tensor`, whose transpose is the reduce-scatter that
returns each rank the gradient for the images it encoded.

**Verified:** step-1 loss AND grad_norm bit-identical, in bf16 AND fp32, on the MoE
flavor AND the dense control. Published matrix legs are unaffected -- they run
`local_batch_size 1`, so the path never triggers.

**Two traps this hit, both worth carrying forward:**

1. The first run looked perfect and proved nothing -- the path never engaged, because
   `local_batch_size 1` means one image per microbatch and `len(counts) < cp_size`.
   Engagement is now asserted by log, not inferred.
2. **"step 1 matches, step 2 does not" is not evidence of a backward bug.** It looked
   like under-reduction. It is not: the `"fsdp"` mesh IS `dp_shard x cp` and
   `disable_fsdp_gradient_division` is applied, so tower gradients are summed across
   CP. A missing reduction would show as a factor-of-cp error; fp32 showed ~1e-5. And
   the dense control, with no routing to flip, shows the same pattern -- so it is
   reduction order amplified by Adam's first updates, where the update magnitude is
   ~lr almost independently of gradient scale. **Distinguishing a real reduction bug
   from that needs a dtype sweep and an architecture control, not more staring at the
   same two numbers.**

Related and also landed: ViT MLP TP (`54810694d`) and ViT attention head TP
(`e37faff3c`, with `kimi_k3_debugmodel_report_arch_vit4h` because the shipped debug
tower has 3 heads, which no TP degree divides).

---

## LoRA TP -- NOT fixed

Real defect, localised but not root-caused. No commits to `lora.py`.

**Established.** At steps 2-3, **22 of 24 testable Replicate-on-TP gradients disagree
across ranks.** The only two that agree at any step are layer 0's
`q_a_proj.lora_a` (3.80e-05) and `.lora_b` (2.07e-03). With LoRA off the same
architecture is clean: 40 replicated gradients checked, all agree.

**Why step 1 understates it.** `lora_b` is zero-init, so at step 0 nothing flows
into `lora_a` and its gradient is exactly zero on every rank; likewise every AttnRes
`*_res_norm`. Zero agrees with zero -- 14 of step 1's 15 "agreements" have
`ref_absmax = 0`. They were never testable. Run three steps.

**Probe bug found and fixed.** `compare_replicated_grads` recorded only
DISAGREEMENTS, so a compared-and-agreeing parameter emitted nothing, and absence
meant "never compared" in one place and "compared and agreed" in another. 145
enumerated, 130 records, and 26-11=15 silent agreements accounts for the gap. That
is what made `q_a_proj.lora_a` look absent and layer 0 look adapter-less; both of
those "findings" were withdrawn. The probe now records agreements with
`ref_absmax`.

**The hypothesis the shape supports.** Layer 0's `q_a_proj` is the one Replicate
parameter upstream of every TP collective -- it consumes the embedding output, which
no TP boundary has touched. Everything else is downstream of at least one. Clean at
the source, contaminated everywhere after the first boundary, says the contamination
enters at a TP module boundary and is inherited rather than produced independently
in each adapter. And LoRA-off being clean says the wrapper is what breaks the
boundary: `parallelize.py` redirects a Colwise/Rowwise style from the
`KimiLoRALinear` onto its inner `.base`, so the style's layout conversion happens at
the inner Linear's boundary and **the adapter branch runs outside it**. The wrapper
then adds a converted base output to an unconverted adapter output.

**Refuted:** that the Rowwise adapter's Partial intermediate is the cause. Forcing
it to Replicate left the three `q_a_proj.lora_b` deltas bit-identical, so that
branch never fires for `q_a_proj`; it also relabelled other adapters and took the
count from 11 to 19.

**The next measurement, and it is one measurement.** For a single Colwise-styled
LoRA module, record the placements of `base_out` and `lora_out` immediately before
they are summed. If they differ in a way the sum silently coerces, that is the
boundary. Probe: `phase13_k3like_48b_posttrain/lora_tp_replicate_probe.py`. Compare
only `Replicate`-on-TP values; never plain per-rank or Shard-placed locals.
